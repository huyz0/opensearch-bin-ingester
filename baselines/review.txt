# Review findings argued as non-defects. One per line:
#   <role>:<finding-id>  <why it is not a defect>
#
# The key carries the role -- `reviewer:R3` or `test-reviewer:R3` -- because
# the two reviewers number their findings independently, and a bare id staged
# for one of them used to silence the other's finding of the same number.
#
# An entry must be STAGED to count: an unstaged one suppresses a finding while
# leaving no trace of it. A growing list is itself a signal.
reviewer:M0.31-R3-1  Rule 12's remedy is 'split it'. The diff is check-gate-scope.sh, its test, the backlog row and this entry; splitting would put the negation test in a different commit from the two lines it constrains. The round count came from the subtlety of git's ignore semantics -- five wrong forms, each found by a reviewer running the gate rather than reading it -- not from commit size, which is what the rule's own M-1.1 precedent measures. Rounds 1-2 each found a real defect; round 3 found none, so the cap's purpose was served rather than evaded. Argued, not fixed, because the finding is about process rather than the change.
rounds:M4.21  Fourteen rounds against the cap of two, and this argues rather than splits because the diff cannot be decomposed: check-reviewed.sh, review_rounds.py, review.md and the two ReviewRoundCapTest files are one mechanism and its test, and a partial commit would ship an escape hatch either untested or undocumented. Rounds 1-2 found the original two remedy-message defects the task exists to fix. Rounds 3-13 each found a real defect in the fix for the previous round's defect -- two production bypasses (file-scoping, no-task verdicts, both with working exploits and now-closed by ReviewRoundCapArgumentValidityTest), one production gap (key anchoring, closed by ReviewRoundCapTest), and a long tail of prose making a claim the tree did not support, including twice by me: a measured number moved in the weakening direction and then mis-reported as fixed (round 6/7/8/9), and a wrong claim made unfalsifiable by deleting its number rather than correcting its mechanism (round 11/12). Round 14 passed both roles clean. This entry is itself the dogfood: it is the mechanism M4.21 built, used on the commit that built it, which is the reason this argues instead of skipping -- SKIP=check-reviewed would disable both-roles-present and no-unresolved-majors, and nine-plus M4 commits already used it silently. An argued entry stays in the tree, reviewed with the commit it excuses.
rounds:M0.53  Many rounds against the cap of two -- the count is left to `review_rounds.py`, because three drafts of this argument and of the backlog row carried one by hand and every one was stale by the next round -- argued rather than split, and the argument is about the DEFECT CLASS rather than the size. Every round but the first found the same shape of bug: a predicate asked of one end of a rename but not the other. Round 3 found that EVERY fixture pinned `git config diff.renames false`, so two fixes had been verified against a configuration nobody runs. Round 4 measured a WEAKENING relative to HEAD -- `git mv FooTest.java FooTest.java.bak` passed where HEAD refused -- because three subtly different path predicates were asked where the loop asks a fourth. Round 5 measured a SECOND weakening, in the other scanner: `tdd_scan` followed a rename from a source it had never gated, so adding `docs/drafts/FooTest.java` and then moving it into the test tier subtracted its ids from `fresh` and demanded no red record at all -- a two-commit, green-tree bypass of non-negotiable 3. Both scanners now ask ONE `reads()` predicate of BOTH ends. Round 6 put the question to ~45 differential fixtures -- both scanners, four `diff.renames` settings, both `--cached` and `CHECK_RANGE`, each run through HEAD's gate and this one -- and found no remaining input where this change is weaker than HEAD; its three majors were all outside the scanner logic. ⚠️ THIS ENTRY DOES NOT CLAIM CONVERGENCE, and an earlier draft of it did: it said each fix was smaller and in the same place than the last, and round 5 falsified that by finding a major in a file four rounds had already read. What is claimed is narrower -- that the remaining surface is one predicate now written once per scanner, and that a reader can check that claim by grepping `TEST_PATH.search`, whose only matches are the two `reads` definitions. ⚠️ AND THAT CLAIM WAS FALSE WHEN FIRST WRITTEN: round 6 ran the grep and found a third, spelled-out copy still live in `test_integrity`'s loop, which is exactly the stale-second-copy this entry says has been eliminated. It is folded into `reads` now, so the prescribed check returns the prescribed answer. ⚠️ SPLITTING WOULD SHIP THIS DEFECT CLASS RATHER THAN AVOID IT. The round-4 reviewer proposed three commits (rename-following / tier departure / package re-qualification); the round-5 reviewer, having measured the second weakening, disagreed and said so. Both defects lived in the RELATIONSHIP between a rename's two ends, and the two scanners share `rename_map`, `package_of` and `rekey` -- so a split puts the predicate in one commit and the call sites that make it correct in others, which is precisely the disagreement-between-predicates that rounds 4 and 5 both found. What no round has closed on is a `minor`; the eleven-round precedent rule 12 cites had rounds 9-11 doing exactly that.
rounds:M4.7a  Past the cap of two -- the count is left to `review_rounds.py`, because a count carried by hand in prose is stale by the round after it. Argued rather than split because the wire-format-change skill FORBIDS the split: "one commit changes the format, every reader, every writer, the fakes, the golden files, and the ADR", on the ground that a format change split across commits leaves the tree able to write bytes it cannot read. The diff is exactly that set and nothing else. ⚠️ EVERY ROUND FOUND A REAL DEFECT IN CODE, none in process and none in prose-about-review -- which is the distinction M0.53's row draws and the reason that task's tail was waste and this one's was not. Round 1: `SubscriptionHub` was updated for the KEY and not for the BYTES, so each run was paired with its own segment while every run kept the same `byte[]` -- and since `Push` carries the segment inline, that delivers another pod's records under the right label, silently. Round 2: the segment count became an allocation size, measured raising OutOfMemoryError past `recover`'s own `throws IOException`; and the count-bound fixture stopped at the count, so `remaining()` was 0 and the bound could not be told from an emptiness check. Round 3: the same allocation defect one field over, in the RUN count, reachable from the v0 path every bucket already holds -- fixed here rather than filed, because bounding one of two while a comment claimed the class was closed is worse than either. Round 4: passed both roles clean, no findings. ⚠️ THE FIXTURES WERE THE RECURRING WEAKNESS, not the format: every batched fixture was exactly two segments until round 3, so writing the count as a literal `2` survived three module suites, and both bound fixtures had zero trailing bytes. Arity and slack are now varied deliberately.
