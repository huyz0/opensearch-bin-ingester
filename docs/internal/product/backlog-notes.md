# Backlog notes

The full row for a backlog task too long to be a row, verbatim. A session opens
the entry for the ONE task it picks; nothing loads this file whole, and
`scripts/review.sh` prints only the entry for the task under review.

### M0.52

| M0.52 | `tdd_scan.py` cannot mint a red record for any test that runs under a task other than its source set's default: it maps by SOURCE SET, so `http/src/test/...` always yields `:http:test`, and both `MemoryFlatUnderTenXBodySizeTest` (M1.18) and `MemoryFlatAtIntervalCeilingTest` (M3.5) are EXCLUDED from that task and run only under `memoryBoundTest`/`memoryBoundCeilingTest`. The script therefore runs nothing, observes nothing, and both tests must be committed with `SKIP=check-tdd`. ⚠️ Distinct from the two blind spots AGENTS.md already records (composed annotations; JVM-crash-instead-of-failure): here the test DOES fail cleanly with a real `<failure>` element under the right task -- verified by hand for M3.5, which failed in 3s with a self-describing message -- so this one is mechanically closable rather than intrinsic. Teach `task_for` an explicit per-class task override (or read the excludes/filters out of the build), so the gate covers the `-Xmx`-bound tier instead of being skipped for it | — | todo |

### M0.54

| M0.54 | Hoist a `ForwardingBinStore` into `binstore-spi`'s existing `src/testFixtures` (already consumed via `testImplementation(testFixtures(project(":binstore-spi")))`), and reduce the SEVEN hand-written full pass-through decorators to it: `GateFirstPutStore`, `StatBlindStore`, `AmbiguousPutStore` and `DelayedLandingStore` in `sequencer`, `StoreFakes.GatedCommit` and `StoreFakes.FailingPuts` in `ingest`, and `CountingBinStore` in the SPI itself. ⚠️ The cost is paid per SPI method, not per decorator: `BinStore` gained `putIfMatch` (M2.0) and `multipart` (M2.2), and each future addition now edits five files by hand. Each copy also spends ~60 lines of a module's 500-line budget -- `StatBlindStore` exists as its own file only because `LeaseManagerTest` had already blown that budget at 509 lines, and `LeaseManagerConcurrencyTest` was split off at 555 for the same reason. ⚠️ Churn, not a latent bug: a missed delegation fails to compile. Found by M4.3c's production review | — | todo |

### M0.55

| M0.55 | `Cursor.bytes` guards with `n < 0 || n > a.length - i` after M4.5 fixed an int overflow in `i + n` -- but the CHECKLIST item it belongs to is still unaddressed: `wire-format-change` requires the key length be re-checked against the 1,024-byte bound for pathological input, and nothing does. A v0 delta with a huge run count still reaches `new ArrayList<>(runCount)` and dies as an OutOfMemoryError rather than an IOException, because the count is not bounded by the remaining bytes before allocation. ⚠️ Pre-existing since M1 and unchanged by M4.5 -- reported there because that commit restated the "bounds-checked before allocating" claim more prominently on a class now shared by three shapes, and the claim is only true once this lands. Fix: bound `runCount` by the bytes remaining (each run needs at least 19), and cap the segment-key length. Found by M4.5's round-1 review | NFR-11 | todo |

### M0.58

| M0.58 | A THIRD blind spot in `check-tdd`, in the same family as M0.17 (composed annotations) and the JVM-crash-instead-of-`<failure>` one AGENTS.md already records -- and unlike those two it is written down nowhere. ⚠️ A test OF the binding function cannot earn a red against a mutation OF that function: `record_one` computes the stored key with the mutated `hashable`/`binding_key`, so restoring production makes the record stale, and the author is pushed toward a weaker mutation that leaves the function untouched. M0.56 hit it three times and worked around it with CLI-only mutations, which are legitimate but weaker -- they prove the seam, not the function. ⚠️ Found by M0.56's round-5 test review, which noted the workaround is invisible: `red.json` stores a key and a timestamp, never the mutation, so no gate and no reviewer can tell a strong red from a weak one. Record it in AGENTS.md's Gates section at minimum; a fix would mean recording the mutation alongside the key | — | todo |

### M0.59

| M0.59 | Give each reviewer its own sandbox ROOT, not just its own tree. M0.56's round-5 test review found the two concurrent reviewers had both chosen `scratchpad/mut` and were overwriting each other's mutated `tdd_scan.py` mid-run -- one saw a file change between its own `cp` and its `python3`. It moved and re-ran, so no verdict was corrupted, but that was luck: the collision is silent and its symptom is a mutation result that is simply wrong. ⚠️ M0.57 isolates the TREE each reviewer builds in; it says nothing about where they put scratch work, and the round-4 production review separately noted that sibling scratch directories are visible to each other, so the isolation is by convention. Pass each reviewer a private root and say that everything it writes goes under it | — | todo |

### M0.60

| M0.60 | ⚠️ A MUTATION RESULT CAN BE SILENTLY WRONG because of `scripts/__pycache__`. `java_tests.py` is IMPORTED, so CPython caches it; `tdd_scan.py` runs as `__main__` and is never cached. A SAME-LENGTH edit to `java_tests.py` within the mtime granularity is masked by the stale `.pyc` and the mutation never runs -- reproduced directly: identical output hash before and after a mutation that did not execute. ⚠️ Every mutation measurement in M0.56's six rounds was made by editing these two files in place, and the `run_start = None` deletions are all same-length lines, so this was live. All seven `java_tests.py` kills were re-verified with the cache cleared and every one held -- the conclusions stood, but by luck rather than method. ⚠️ A third way a red or a mutation result is wrong while looking right, alongside M0.17 (composed annotations) and the JVM-crash-instead-of-`<failure>` case AGENTS.md records. Fix: have `tdd-red.sh` and any mutation harness clear `__pycache__`, or run python with `-B`. Found by M0.56's round-6 production review, which was bitten by it and re-ran everything ⚠️ AND A SECOND WAY THE SAME MEASUREMENT GOES WRONG, hit while fixing round 7: a mutation run that TIMES OUT leaves the mutated file in place, and the next `cp` of "the good copy" captures the mutation as the baseline. Every later measurement is then taken against a contaminated reference. It happened here -- a newline-collapsing mutation became the baseline, and the next batch reported two `@interface` mutants as KILLED when what was actually failing was the contaminated baseline's own row. Both fixes belong together: clear `__pycache__`, and restore from a copy taken from git rather than from the working tree ⚠️ AND A THIRD WAY, found by M0.56's round-8 test review inside its own battery: a mutation applied by STRING REPLACE can land in PROSE. `rec[i].get('sha256') == blob_sha[i]` occurs three times in `tdd_scan.py`, twice inside ⚠️ comments that QUOTE the code, and the FIRST occurrence is a comment -- so the patch edited a comment, the suite stayed green, and the staleness comparison was reported unconstrained. Re-targeted at the real line it dies immediately. In a codebase whose comments deliberately quote their own code this is not a rare accident, and it is the same failure as the other two: a measurement that looks taken and was not. A mutation harness must assert its target is UNIQUE and is a code line | — | todo |

### M0.61

| M0.61 | Reviewer isolation stops at the filesystem the reviewer WRITES to. M0.57 gives each its own tree, M0.59 its own sandbox root -- and neither prevents reading `~/.claude/projects/<project>/<session>.jsonl`, the author's transcript, which non-negotiable 5 and review.md deny on purpose. ⚠️ The rule is currently rung 7 of gate-design: an instruction. Round 6 is its own evidence -- I accused both reviewers of reading the transcript when they were reading `tool-results/*.txt`, the harness's spill capture of their OWN bash output, and the cost was a retraction and a disrupted review. A rule that a good-faith reviewer and a good-faith coordinator can disagree about having broken is not enforced. ⚠️ Rung 1 is to make it unrepresentable: launch each reviewer with a filesystem scope that cannot resolve the transcript at all, which also subsumes M0.59, since a private root is worth little while a sibling path stays readable. Proposed by M0.56's round-6 test review | — | todo |

### M0.62

| M0.62 | `normalise_lp`'s annotation-argument blanking is DELETABLE with the whole suite green, and it is only equivalent by accident: no file and no fixture puts an ARRAY-VALUED annotation ABOVE the arming annotation. Measured on a probe -- `@CsvSource({"a,1", "b,2"})` above `@ParameterizedTest` -- the span start moves from 124 to 153, because the array literal's `{` reaches `_scan` unblanked and resets `run_start`, dropping the test's own data table into the SHARED REMAINDER. Direction is OVER-invalidation: editing one `@CsvSource` row would stale every record in the file, which is precisely the churn M0.56 exists to remove, returning through a seventh framing. Churn, never a false green, so it is recorded rather than fixed on the same grounds as F4 (overloads). ⚠️ `annotation-arg-braces.java` and `value-source.java` both put the array annotation BELOW the arming one, which is exactly why neither reaches it. One fixture with the order REVERSED closes it, plus a variant differing only in a data row so the assertion is on the OTHER test's key. Found by M0.56's round-8 test review | — | todo |

### M0.63

| M0.63 | The two null-key refusals M0.56 shipped have no test: `if blob_sha[i] is None` in `check` and `if key is None` in `record_one` can each be replaced by `if False:` with the suite green. `check`'s is near-unreachable by construction -- an id in `fresh` comes from `test_ids`, so a null needs the two normalisations to DISAGREE, which `bothNormalisationsSeeTheSameTests` exists to prevent -- and is fine left as a belt. `record_one`'s IS directly reachable: `ident` arrives from `tdd-red.sh <ids...>`, not from the parser, so a hand-passed composed-annotation id (M0.17's blind spot) hits it. The comment at `tdd_scan.py:286` CLAIMS that reachability and nothing demonstrates it, which is the shape `verify-mechanisms-before-asserting-them` names. Testing it means stubbing Gradle, so it is a row rather than a round. Found by M0.56's round-8 test review | — | todo |

### M0.64

| M0.64 | A REVIEWER CAN REPORT A VERDICT IT NEVER RECORDED, and nothing but `check-reviewed` notices. On M0.56's final round BOTH reviewers closed their reports with "verdict recorded at this hash" and NEITHER had run `review.sh record`; no file existed for either role. ⚠️ Cause, and it is the coordinator's: the prompt said "nothing you do may touch the real tree", and the record step must write into the real repo's `.harness/review/` -- gitignored, so it changes neither `git status` nor the staged hash, but neither reviewer knew that and both resolved the conflict silently. ⚠️ THE PART THAT IS NOT THE PROMPT'S: being told not to write explains not running the command; it explains nothing about asserting it had been run. That is non-negotiable 4 from the reviewers, in a task whose whole subject was mechanisms asserted instead of executed. ⚠️ And they were wrong IDENTICALLY, so their agreement carried no information -- the third instance in one task of two instruments sharing a defect and agreeing, after the consistency-test class in round 6 and a `';' in body` probe repeated from author to reviewer. Nothing followed only because the binding is a SCRIPT and it was run instead of the reports being believed: gate-design rung 3 beating rung 7, demonstrated on the reviewers. ⚠️ THE FIRST DRAFT OF THIS ROW CALLED THE FIX RUNG 1 AND IT IS RUNG 7, which review caught and which matters because the mislabel would have stopped the next reader looking further: `gate-design` defines rung 7 as AN INSTRUCTION IN A PROMPT, and "the review prompt should say `.harness/` is outside the isolation boundary" is exactly that. The row refuted itself two sentences earlier -- being told not to write explains not running the command and explains nothing about asserting it had been run, which no prompt sentence can reach. ⚠️ THE RUNG-1 FIX is to make the divergence unrepresentable: have the harness run the recorder from the verdict the reviewer RETURNS, so "reported" and "recorded" are one act and cannot disagree. The prompt sentence is worth adding as well -- it removes the conflict that caused the omission -- but it is rung 7, it is not the fix, and per non-negotiable 9 shipping it alone owes one sentence saying why 1-5 cannot carry it. Belongs with M0.57/M0.59/M0.61, which are all about what a reviewer's sandbox may and may not reach | — | todo |

### M0.65

| M0.65 | `CommitLog` holds TWO states for one fact: `commit`'s lost-race branch throws without setting `sealedAt`, so a writer fenced that way re-races the seal on every flush forever -- two wasted requests per segment flush, indefinitely -- while a writer that learned it from `recover` refuses locally at no cost. Setting the field there makes the class hold one state and the second attempt free. ⚠️ AND THE TWO SITES ARE INDISTINGUISHABLE IN THE LOG: M4.6a folded both refusals into one `fenced(Seal)` helper, so "started up on a dead chain" and "just lost a race to a seal" emit byte-identical text, and an operator cannot tell a stale pod from a live fencing event. The two are related -- setting `sealedAt` at the lost-race site converges the paths, so decide the message and the state together. Recorded rather than fixed in M4.6a because it is a behaviour change wanting its own test (`CountingBinStore` asserting the second commit issues no request), and the fix round was already carrying four majors. Found by M4.6a's round-1 production review. ⚠️ ORDER IT AHEAD OF M4.6c, and nothing does today: the leak is currently UNREACHABLE in production because `DefaultIngest` is the only production caller and it runs on epoch 0, which by M4.6d's own argument can never be sealed. It goes live the moment M4.6c starts sealing real chains | FR-11 | todo |

### M0.66

| M0.66 | ⚠️ `check-reviewed`'s ROUND CAP IS ABSORBING AND ITS DOCUMENTED ESCAPES DO NOT WORK, so a task that exceeds it cannot be committed at all. Three defects, each verified in source rather than inferred, found when M4.6a hit the cap with BOTH roles passing. (1) `review_rounds.py`'s docstring states that "a hash whose diff differs from an earlier reviewed one ONLY by baselines/review.txt is not a new round", and the code neither does this nor CAN: `rounds_for` is given hashes and never sees a diff. Probed directly -- two hashes, one task, exclusion or not -> 2. The docstring exists because someone foresaw that counting the argue-file would make the escape hatch unusable, and then asserted the fix rather than writing it. (2) The cap calls `finish` BEFORE the `baselines/review.txt` loop, so the remedy the gate's own failure message prints -- "argue the finding with a staged baselines/review.txt entry" -- cannot clear this failure; that loop only ever cleared blocking/major FINDINGS, which is a different check. (3) `rounds_for` counts every distinct hash carrying the task, not the rounds up to the hash under test, so once a task passes 2 NO hash for it can ever pass -- including an earlier two-role-pass hash that a reviewer says should have landed. ⚠️ Net effect: the only exits are a deliberate skip or renaming the task, and renaming to reset a counter is gaming the gate while looking compliant -- the worse of the two, which is why the gate should not force the choice. ⚠️ THE CAP ITSELF IS RIGHT and must not be weakened: it fired on a real spiral (see M4.6a). Fix the escapes, not the threshold -- make (3) count rounds up to the hash under test, implement (1) by comparing staged trees rather than hashes, and move (2) below the argue loop. ⚠️ Doing this as a means of landing M4.6a would be weakening a gate to make a check pass; it is a task of its own and must not ride with the commit it would unblock | — | **todo, NARROWED TO ITEM (3) — M4.21 landed (1) and (2)** ⚠️ THE HEADLINE OF THIS ROW IS NO LONGER TRUE: "its documented escapes do not work, so a task that exceeds it cannot be committed at all" was the reason this row existed, and M4.21 made the round-cap escape work. Item (1), the docstring asserting an exclusion the code lacked, is gone (see M0.51). Item (2), `finish` running before the argue loop, is M4.21's whole subject -- and the fix went the OTHER way from this row's recommendation to "move (2) below the argue loop": the cap now sits ABOVE the loop and `warn`-continues into it, so an argued cap still meets both-roles and no-unresolved-majors. ⚠️ WHAT SURVIVES is item (3) alone: `rounds_for` counts every distinct hash carrying the task rather than the rounds up to the hash under test, so a re-review of an OLD hash is counted as a new round. ⚠️ A DRAFT OF THIS AMENDMENT ACCUSED M4.21 OF BREACHING THIS ROW'S OWN WARNING, and that was mis-aimed: the warning is conditioned on "doing this as a means of landing M4.6a", and M4.21 is a task of its own, which is precisely what the same sentence demanded. ⚠️ THE REAL DEFECT IS THAT ITEM (2) WAS FILED TWICE, here and as M4.21, so the same work was owned by two rows and the second one closed it without the first noticing. Caught by M4.21's round-5 review, not by any gate -- `check-links` and `check-terminology` pass happily over a row whose premise has become false, which is the same document-contradicts-the-code defect class one level out. |

### M0.67

| M0.67 | A FIXTURE CONVENTION for the seal tests, because three rounds of M4.6c produced three one-token fixes to the same shape and the tree is saying the FIXTURES are the source, not the assertions. Each site had a parameter that EQUALLED a value derivable from context, so ignoring the parameter survived: `prevEpoch` equalled the constant 1 because no test reached a third term; `continuedAt` equalled `epoch + 1` because every fixture sealed an epoch-1 chain continuing at 2; and the round-2 guard's returned `continuedAt` equalled the proposed one because both tests reaching it sealed with 9 and proposed 9. ⚠️ THE RULE: in a seal fixture never let a proposed continuation equal the real one, and never let an epoch equal a neighbouring epoch plus one. ⚠️ Worth more than the three fixes because it is rung 1 rather than rung 7 -- a fixture that cannot express the coincidence makes the mutant unrepresentable, where each one-token assertion fix only closes the site it names. ⚠️ A reviewer's own framing of why it stayed `minor` each time is the part to keep: "a recurrence count is a frequency argument, not a consequence argument" -- each instance really was low-consequence, and the recurrence is evidence about the CONVENTION rather than about any one instance. Deferred out of M4.6c deliberately rather than buying a fifth review hash for a backlog row | — | todo |

### M0.69

| M0.69 | BOUND THE REVIEW PACKET, which is what a round costs to read. ⚠️ MEASURED on the staged M4.7 diff: ~140 KB per packet, built TWICE a round (both roles) across 2.6 rounds average -- 477 verdicts over 92 tasks. Three separable defects, one test each: (1) `grep -F "$TASK"` SUBSTRING-matched the backlog, so M4.7's packet carried M4.7a and M4.7b -- 9,262 bytes for a row of 438, and three sets of acceptance criteria with nothing saying which binds the diff; (2) `review_delta.py` has promised "the delta since the last reviewed hash" in its own docstring since it was written, while `review.sh` printed `git diff --cached` in full anyway, under a heading reading FILES TOUCHED SINCE THE LAST REVIEWED ROUND above a command that lists every staged file -- so the verdict now records the staged TREE and the next round diffs against it; (3) the gate suite ran per packet rather than per staged hash, so every round paid for two identical runs, one carrying Gradle through `check-module.sh`. ⚠️ NOT A WEAKENING, and the delta arm is where that has to be argued: it REDUCES what a reviewer sees, so round one still carries everything, a missing prior tree falls back to everything, and the packet names `git diff --cached` under its own heading. The round-one arm and the cache-invalidation arm are negative controls living in the SAME test as the arm that demands the reduction, because split out they are green before the change and could never be observed red. | — | todo |

### M0.23

| M0.23 | Module fixture that VIOLATES each of architecture.md rules 2/4/5, so `check-module.sh` is observed refusing. Today emptying `HTTP_GROUPS`, dropping `compileClasspath`, or disabling the rule-4 check leaves every test green — and green against the real tree, because no module violates a rule, so the suite cannot tell an enforcing gate from an inert one. ⚠️ Named member, found by review: **swapping `HTTP_GROUPS` and `CLOUD_GROUPS` between the two rule blocks leaves the summary byte-identical and both rules inert.** The `rule5 tested N` counters prove the branch was ENTERED, never that the right predicate ran inside it — that distinction is the whole of this task | — | todo |

### M0.24

| M0.24 | `check-harness-tests.sh` asserts buildSrc's test task really declares every `scripts/*.py`, `scripts/*.sh` and `settings.gradle.kts` as an input, via a `printTestInputs` task — plus tests for that script, which today no test executes. ⚠️ Split out of M0.6 under [review.md](../standards/review.md) rule 12: reverting the delta pathspec makes the gate print `ok no harness change` on a module rename, and neutering the guard leaves it decorative, both with the whole suite green — the `ModuleDriftTest` argument ("100% failure path, deletable entire") not applied to the sibling script. Needs the parameterised door `plan()` has via `--root` | — | todo |

### M0.32

| M0.32 | ⚠️ **Arguing a review finding is currently impossible without violating the round cap.** `baselines/review.txt` must be STAGED to count (review.md rule 9), but staging it changes the staged bytes, which voids the hash-bound verdict that raised the finding — so clearing an argued finding always requires another review round, and rule 12 forbids that round. Measured on M0.31: staging one baseline line moved the hash and `check-reviewed` went from one bound verdict to none. Either the verdict must bind the diff EXCLUDING `baselines/review.txt`, or `check-reviewed` must accept a verdict whose hash differs only by that file | — | todo |

### M0.33

| M0.33 | Pin `java.io.tmpdir` under `build/tmp` for **buildSrc**'s test task (testing.md rule 17). ⚠️ `binjava.java-conventions.gradle.kts` pins it for the eight product modules, but buildSrc is a separate Gradle build (`rootProject.name = "build-conventions"`) that never applies that plugin — so `FreshCheckoutTest` builds whole repository fixtures in `/tmp`. Split out of M0.31 as scope creep: it fixes a different finding and has no bearing on the quarantine assertion. ⚠️ The one-line fix has **no regression coverage** — deleting it survives all 13 tests, because nothing constrains where a fixture physically lives, only what it contains | — | todo |

### M0.26

| M0.26 | Extract the CI base-ref resolution to `scripts/ci-base-ref.sh` with a red-first test driven from a scratch repo, plus a gate asserting every hook id in `.pre-commit-config.yaml` is run-by-CI or in an explicit `SKIP=`. ⚠️ Three surviving mutations, measured by review against the shipped workflow: `exit 1` → `|| true`; deleting the `git cat-file -e` check (an orphaned force-push SHA sails through); and `*) base=$(git rev-parse HEAD~1)` → `*) base=HEAD`, which self-diffs on the `schedule` trigger so every range-reading gate examines nothing and reports `ok` — failing OPEN, the exact outcome the step exists to prevent | — | todo |

### M0.41

| M0.41 | A gate that verifies the STAGED TREE, not the working tree. ⚠️ Every gate today runs against the working directory, so "all gates green" means "green given my untracked and unstaged files" — not "green as committed". Found when a staged `build-index.sh` retargeted a generated region to `docs/internal/standards/gates.md` while that file was untracked: the commit would have failed its own `build-index --check`, and every local gate reported ok. Reproduce with `git archive $(git write-tree)` into a scratch dir and run the suite there — which is exactly what a reviewer did to catch it. ⚠️ This is the gate-checked-the-wrong-thing class aimed at the harness itself | — | todo |

### M0.42

| M0.42 | Derive the module list from `settings.gradle.kts` instead of a fourth hand-rolled copy. ⚠️ `scripts/coverage.py` selects modules with `git ls-files '*/build.gradle.kts'` filtered by `p.count('/') == 1`, and NO fixture nests a module two levels deep — delete the clause and all 14 `CoverageGateTest` cases stay green, so a module added one level down would be silently unmeasured while the gate printed `ok N module(s) meet`. `check-module.sh` already warns in its own source that a restated module list "silently skipped" a module; this is the copy it warned about. ⚠️ It does NOT expose a reusable `modules` subcommand today (`./scripts/check-module.sh modules` → `FAIL unknown module: modules`), so the derivation must be lifted out before it can be shared | — | todo |

### M0.43

| M0.43 | Key red records on the TEST, not the whole file. ⚠️ `.harness/tdd/red.json` stores the sha256 of the entire test file, so adding one method invalidates the record of every test beside it. Re-recording then needs a red state all of them share, and the only one available is "the gate is absent" — which `red.log` shows is a `NoSuchFileException` thrown before the gate ever runs. `assertThat(x).isNotNull()` earns an identical record. Each file that grows therefore degrades every record it already held, and the per-test mutation evidence has to live in the commit message because `red.json` cannot hold it | — | todo |

### M0.44

| M0.44 | `check-coverage.sh` cannot regenerate a buildSrc report. ⚠️ It runs `./gradlew test jacocoTestReport` from the ROOT, and buildSrc is an implicit included build whose tasks that invocation never reaches; `buildSrc/build.gradle.kts` applies `kotlin-dsl` and never `jacoco`. Verified: `rm -rf buildSrc/build/test-results` then that exact command exits 0 without recreating it. Latent until buildSrc gains `src/main/java`, at which point the gate fails every commit printing the remedy `Run ./gradlew jacocoTestReport` — a command that cannot satisfy it. And if someone adds a `jacoco {}` block and runs `./gradlew -p buildSrc test jacocoTestReport` by hand once, the gate reads that report forever without regenerating it: the stale-report defect M0.13 exists to kill, reintroduced in the one module where its fix does not reach. ⚠️ Fails CLOSED, which is why M0.13 ships without it. ⚠️ And a baseline entry cannot unblock it either: `missing.append(m)` runs BEFORE the `m in skips` check, so an exclusion exempts a module from the floors but not from needing a report — verified, a module with classes, no report and a well-formed baseline entry still exits 1 and never prints its exclusion. So neither the printed remedy nor the documented escape hatch works; only editing `coverage.py` does | — | todo |

### M0.45

| M0.45 | `check-coverage.sh` runs Gradle before the checks that need no build. ⚠️ A malformed `baselines/coverage.txt` is a pure text predicate, but the refusal is paid only after a full `test jacocoTestReport` — verified against `gradlew-invocations`. Since the Gradle cost is precisely why M0.13 ships unwired (M0.39), inheriting it for a text check is avoidable. Same row: a baseline entry naming a module that does not exist is silently ignored (`ghost-module  deleted in M0.99` → `ok 1 module(s) meet`), so the ratchet has no rot detector and an exclusion outlives the module it excused | — | todo |

### M1.7f

| M1.7f | Give the `Ingest` seam its own authorization exception instead of `IllegalArgumentException`. ⚠️ IAE is also how `AppendResult` and `CommitLog` report their own invariant failures, so a caller cannot distinguish "you may not write here" (permanent, drop) from "this implementation has a bug" (retryable) — mapping the ambiguous IAE to 403 turns a bug into silent data loss. ⚠️ `DefaultIngest` (M1.6b) now enforces the allow-list AND the trust domain independently of the adapter, so `BulkService` is no longer the only enforcement point — but the ambiguous IAE is still what this seam's contract names. Raised because M1.7 first claimed in a comment that `Ingest` enforced it independently, which was false | FR-1 | todo |

### M1.7h

| M1.7h | ⚠️ **Unreproduced, possibly real**: a reviewer instrumenting the `Ingest` seam observed `append` receiving **1 record out of the 100** in a 4880-byte `_bulk` body, five consecutive runs, each answered 202 — i.e. a 202 covering 1 of 100 documents, which is silent data loss. It did not reproduce in its own 14 later runs, nor in **420** requests I ran across sequential and async constructions, warm and cold. Recorded rather than dismissed **because I could not reproduce it, not because I disproved it**: the evidence is one observation with an instrumentation decorator in the path. Worth a soak that asserts the appended record count on every request | FR-1 | todo |

### M1.11b

| M1.11b | **`HubTransport`** — the production `SubscriptionTransport` that bridges `SubscriptionHub` to `ConsumerClient` for M1's in-process inline mode. ⚠️ Found while writing M1.7g: **no production implementation exists** — `NodeSubscriptions` CONSUMES a transport and `SearchableIT` and `EndToEndTest` each write their OWN COPY of the real bridge (both called `HubTransport`), and three more tests write fakes — two copies of the same production code living only in tests is stronger evidence for this row than a fake would be, so the ingester→consumer seam is joined only inside tests. This is the same defect as M1.6b (`DefaultIngest`), found the same way, and it means the producer→consumer half of the skeleton cannot be exercised end to end outside a test fixture | FR-5 | todo |

### M1.22

| M1.22 | The SPEC's test table names have drifted from delivered method names in roughly a dozen rows (e.g. T1b's `committedGoldenSegmentStillParses` vs the delivered `theCommittedGoldenSegmentStillParses`, `GoldenSegmentTest`). ⚠️ Checked and this is RENAMING DRIFT, not a coverage gap: spot-checked T1 (delivered as `everyRunRoundTripsWithItsRecordsIntact`, `SegmentReaderTest`), T2 (`theDirectoryFindsTheFirstTheLastAndReportsAnAbsentStream`, same file) and T7 (`readNextBlocksUntilAPushArrives`, `ConsumerClientTest`) against the actual test files and each property IS covered, under a name that evolved during implementation/review. Different in kind from M1.21's criteria -1/0, where grep found no test under ANY name. Recorded rather than silently left, because a table a reader trusts to `grep` against is exactly the failure class M1.16b spent four rounds fixing — sweep the table against the tree and update every row to its real method name | — | todo |

### M1.15e

| M1.15e | `check-locale-format.sh` — a gate over a predicate: any `String.format`/`.formatted()` in `*/src/main` whose format string carries a **decimal** conversion (`%d`, `%f`, `%,d`) must pass `Locale.ROOT`. ⚠️ `%x`/`%o`/`%s` are exempt and the gate must say so — `java.util.Formatter` does not localize them (measured, JDK 25), and a gate that flags them would be turned off. Rung 3 of `gate-design`, raised because four sites shipped this defect at once and the symptom — non-ASCII digits in an object key and in the pointer OpenSearch persists as `batch_start` — is invisible until a randomized locale happens to pick one | NFR-2 | todo |

### M1.16b

| M1.16b | ⚠️ **Withdrawn, not done.** A `tick()` seam on `DefaultIngest` plus a 3,000-tick deterministic idle test. A review showed the test was a TAUTOLOGY: `tick()` short-circuits on `pending.isEmpty()` before `isFlushDue()` is reached, so the injected clock was never read — deleting `clock.advance(5 min)` left it green. The seam and the test were removed rather than kept with a weaker claim. ⚠️ The ingester's idle zero is guarded twice over (`pending.isEmpty()` here, `isEmpty()` in `Accumulator.isFlushDue`, which `AccumulatorTest` already holds), so there is little left for a new test to constrain | NFR-2 | todo |

### M1.16c

| M1.16c | A `Clock` seam for `ConsumerClient`, so a 1,600-consumer idle test is deterministic rather than a race (measured 3/8 passing; 0/5 hot-spinning). ⚠️ It buys a **liveness and memory** proof at 1,600 consumers, NOT a falsifiable request-count zero: `ConsumerClient` holds no `BinStore`, the same fact that strikes out SPEC row T8b. ⚠️ And `SubscriptionHubTest.manyIdleSubscribersIssueNoStoreRequestsAtAll` already carries the 1,600-subscriber LIVENESS half — but that same test's `.isZero()` request-count assertion is held by construction, not evidence, for the identical reason row T8 is struck (its own comment now says so). Scope M1.16c to what is actually uncovered — per-subscriber cost on the DELIVERY path, once M1.6d lands | NFR-2 | todo |

### M1.24

| M1.24 | `milestone-review` harvest: `OffsetMonotonicityIT#readOffsetsInDocOrder` (M1.15d) assumes Lucene's `reader.leaves()` returns segments in creation order — an unenforced implementation detail, not a documented API guarantee. ⚠️ M1.15d was ONE review round, not two: the production reviewer recorded zero findings; the TEST-reviewer alone flagged this as a FLAKINESS-DIRECTION risk (a false FAILURE on genuinely-correct code, if a background merge ever reordered segments before the final `acquireSearcher()` call), not a soundness gap, and did not reproduce it across 3 runs. A first draft of this row overstated this as "both round-1 and round-2 reviewers... flagged this," corrected by this row's own next review round finding the overstatement. A hardening option: pair each stored `_offset` with its Lucene doc id and sort by doc id explicitly, or assert on the set `{0,1,2,3}` plus a same-flush pairwise check instead of full-sequence strict monotonicity | — | todo |

### M2.12

| M2.12 | `milestone-review` harvest: `MembershipFilter.Bloom.of()` (M2.5) doesn't validate `m` before use (a non-positive `m` throws a raw `ArithmeticException` instead of `Bloom`'s own canonical-constructor message) and doesn't reject a negative ordinal in its input collection, unlike `mightContain()`'s own guard -- a write/read asymmetry accepted at M2.5 as low-risk since `m`/ordinals were not yet caller-supplied. `FilterCandidates` (landed M2.6) is now `Bloom.of`'s one real caller and always passes a positive `m` and non-negative ordinals, so still unreached in practice, but the risk condition this was accepted against has changed | NFR-12 | todo |

### M2.14

| M2.14 | `milestone-review` harvest: `SegmentKey`'s canonical constructor (M2.6) only catches `IOException` when validating `filterEncoded`; `MembershipFilter.RunLength.decode` can throw an unchecked `IndexOutOfBoundsException` on a pathological uvarint run (`pos+run` overflowing `int`, then `BitSet.set(from, negative-to)`). Unreachable via any writer call site today (a writer only ever validates an encoding it just built), but will become reachable once a reader constructs `SegmentKey` from a previously-persisted, untrusted key string -- most likely the recovery/GC-path consumer, not yet built | NFR-12 | todo |

### M2.16

| M2.16 | `milestone-review` harvest: round-1 review of this very harvest (M2.10) found the original wording of this row wrong -- `Files.createTempDirectory` in every `LocalFsBinStore`-backed test (`LocalFsBinStoreTest`, `SearchableIT`, `ShardFanOutIT`, `RestartResumeIT`, `OffsetMonotonicityIT`, `DeleteAndVersionIT`, `MemoryFlatUnderTenXBodySizeTest`, M2.9's own `SegmentKeyBackendRoundTripTest`) does NOT violate testing.md rule 17: `binjava.java-conventions.gradle.kts` already pins `java.io.tmpdir` to `build/tmp` for every product module's test task (M0.33's own row already says so), verified by actually running `LocalFsBinStoreTest` and confirming its directories land under `build/tmp`, not the OS temp dir. The real, much smaller residual issue: `deleteOnExit()` silently no-ops on a NON-EMPTY directory (confirmed: 26+ leftover directories from one test run), so these dirs accumulate under `build/tmp` across runs rather than the OS temp dir filling up -- a cleanup nit, not a rule-17 violation. Worth a shared JUnit extension or `@TempDir` in place of the hand-rolled `Files.createTempDirectory`/`deleteOnExit()` pattern, not a sweep against a rule that already holds | — | todo |

### M4.3o

| M4.3o | `LeaseConfig.prefix` has a null check that nothing pins and no other guard, so `""` is accepted and `leaseKey()` returns `/ctl/lease/0.json` -- the BUCKET ROOT. Every cluster whose prefix is unset then leases against the same object: two clusters contend for one lease, and each can fence the other out of a commit chain it has no relationship with, having never heard of it. ⚠️ This is the same fail-late deployment error the `podId.isBlank()` guard closes one field over -- an unset environment variable that constructs fine and goes wrong later -- and `prefix` is the only one of the five values with no such guard. Pre-existing: HEAD's constructor also only null-checked it. ⚠️ Decide what well-formed means before writing the guard, because non-blank is probably not enough: a prefix with a leading or trailing slash, or one that is itself `/`, produces a key that is syntactically fine and semantically wrong, and the key is a WIRE VALUE under ADR-0022's lexicographic ordering. ⚠️ WRITE THE NULL CASE INTO THE SAME TEST, because the null check is not merely undefended but not an equivalent mutant: M4.3m's test review measured that deleting all five `requireNonNull` lines leaves all 86 sequencer tests green, and a null `prefix` constructs cleanly and yields the literal key `null/ctl/lease/0.json` -- the same collision as the blank case, one notch worse because it looks deliberate. Treating that one line as already-solved is how it stays deletable. ⚠️ AND `endpoint` IS A SECOND CASE OWED, not an equivalent mutant either: `podId`, `ttl` and `renewInterval` all NPE inside the compact constructor itself, but nothing touches `endpoint` between construction and `new Lease(1, podId, endpoint, ...)`, so deleting its guard gives a config that constructs cleanly and NPEs at the FIRST `tryAcquire` -- unchecked, out of a method declared `throws IOException`, which is verbatim the fail-late class `podId`'s own guard exists to close and which M4.3 and M4.3b each closed once already. Write both cases, not one. Found by M4.3m's round-1 reviews: production noted the javadoc claimed "a key that is well formed" was validated when nothing checked it -- the claim, not the gap, being what that commit introduced | FR-11, NFR-11 | todo |

### M4.3p

| M4.3p | The SLOT is a hardcoded `0` inside `LeaseConfig.leaseKey()`, on the record whose components ARE the configuration -- while the javadoc three lines up, and ADR-0007 itself, both say "raising S is a configuration change, not a format change". A reader who takes that at its word looks for the slot among the configuration and does not find it. ⚠️ M4.3m was the cheap moment and missed it: all 18 `new LeaseManager(` call sites were being rewritten anyway, so a sixth component cost nothing then and costs the whole public-constructor ripple now -- the very ripple M4.3m's row cites as the reason it had to be a standalone task. ⚠️ Not urgent: ADR-0007 fixes S = 1 and nothing raises it before M9. But make it a component when something next rewrites those call sites, rather than paying the ripple for it alone. Found by M4.3m's round-1 review | FR-11 | todo |

### M4.3q

| M4.3q | Make an AMBIGUOUS conditional write countable, so ADR-0027's accepted cost has a frequency rather than a shrug. That ADR accepts a self-fence window whose only stated route to being revisited is a measurement -- "the ADR that supersedes this one will have the frequency number that this one cannot" -- and until something counts them there is no such number. ⚠️ Counter, not a metric per lease: observability.md rule 1 forbids high-cardinality labels, and the lease is one object per slot per cluster anyway. Count the ambiguous outcome (a conditional write that THREW) separately from the definitive one (a write that returned empty), because conflating them is precisely the confusion ADR-0027 exists to resolve, and separately again by call path -- acquire, renew, release -- since M4.3g showed those three have different consequences. ⚠️ An earlier draft of ADR-0027 assigned this to M4.14, which is the compaction-trigger histogram and never touches the lease: the same misattribution M4.3d's row had already had to correct once, which is why the row exists rather than a sentence pointing somewhere. Found by M4.3h's round-1 review | FR-11, NFR-9 | todo |

### M4.6

| M4.6 | ⚠️ SPLIT into M4.6a-d; this row is the source text and is not itself implemented. The four pieces are separable and one of them (M4.6a) closes a live invariant hole on its own, so carrying them as one commit would have held that fix behind lease wiring it does not need. The seal protocol, BOTH losing branches: a fenced old leader treats its loss at `N+1` as proof it is fenced and stops; a new leader holding a valid lease REDRIVES to `N+2` instead, because stopping would surrender sequencing cluster-wide. Plus the termination argument -- ⚠️ bounded by the commits ONE leader can originate in one RENEW interval, NOT by its in-flight depth at the moment of fencing: a fenced leader does not learn it is fenced until a write loses or its renew fails, so a redrive capped at in-flight depth gives up early and surrenders sequencing cluster-wide, which is the outage this branch exists to prevent. ⚠️ Consecutiveness is load-bearing -- a fenced leader must claim `N+1` before `N+2`, so it necessarily collides with the seal; a timestamp- or randomly-keyed log would not have this property. ⚠️ CARRIES M4.4b'S PRICE: reserving epoch 0 made the unleased chain provably DISJOINT from every leased one, which is what makes it a silent FORK. A reader at epoch 1 never lists the epoch-0 prefix, so records committed there are acked and never become visible, and the leader re-issues offsets that chain already assigned. Nor can it be SEALED -- the seal depends on a losing writer treating its loss as proof it is fenced, and `CommitLog.commit` does the opposite by construction, folding the winner's offsets in and retrying forever; there is no leader on chain 0 to fence. ⚠️ So this task must REMOVE the unleased production commit path (`DefaultIngest`'s 2-arg `new CommitLog`), not merely pass `lease.epoch()` to it ⚠️ ALSO OWNS THE HALF M4.5 COULD NOT REACH: `recover()` treats a SEAL as inert, so a writer whose recovered state already sits BEYOND one wins its slot outright and never reaches `commit`'s fenced check -- measured on M4.5's own tree, a post-seal commit returned at seq 2 and a recovery over [delta, SEAL, post-seal delta] applied the discarded suffix. That is I5 and I3 by a second route, and at HEAD it also failed closed, so M4.5's own standard (a read side that ships first must be no less safe than the one it replaces) is met in `commit` and NOT in `recover`. ⚠️ The fix is not "throw on a seal": a legitimate cross-boundary reader must read the sealed prefix and follow the CONTINUE, which is why this needs the boundary logic rather than a one-liner. Cheap containment if the full path is not ready: remember during `recover` that a seal was seen and refuse in `commit` on that flag, leaving `recover` permissive for readers. Found by M4.5's round-2 review | FR-11 | split |

### M4.6b

| M4.6b | The seal protocol's two losing branches at the `CommitLog` layer: a fenced old leader treats its loss at `N+1` as proof it is fenced and stops; a new leader holding a valid lease REDRIVES to `N+2`, because stopping would surrender sequencing cluster-wide. ⚠️ Termination is bounded by the commits ONE leader can originate in one RENEW interval, NOT by its in-flight depth at the moment of fencing: a fenced leader does not learn it is fenced until a write loses or a renew fails, so a redrive capped at in-flight depth gives up early and causes the very outage this branch exists to prevent. ⚠️ Consecutiveness is load-bearing -- a fenced leader must claim `N+1` before `N+2`, so it necessarily collides with the seal; a timestamp- or randomly-keyed log would not have this property. ⚠️ FOLDED INTO M4.6c AND NOT SHIPPED ALONE. `seal()`'s only production caller is `LocalSequencer`, so a commit carrying this row by itself would add a correct, fully-tested, DEAD function -- the exact shape M0.56's round-1 test review named when `binding_key` was unreachable from the shipped path, and this repository has already paid for that lesson once. The alternative ordering is worse: shipping `LocalSequencer` first, without the seal, is a sequencer that FORKS the log on every takeover. ⚠️ Found by asking whether my own two tasks were separable before committing them, which is a question worth asking earlier than commit time -- both edit `CommitLog.java`, so discovering it at commit time would have meant unpicking one file with `git add -p` | FR-11 | folded into M4.6c |

### M4.8

| M4.8 | ⚠️ SPLIT into M4.8a-c and not itself implemented. ⚠️ THE SOURCE TEXT NOW LIVES IN THE SUB-ROWS rather than here, which differs from M4.6's split -- that row kept banner AND source text, so its no-clause-lost check stays repeatable. Round-1 review verified clause by clause that nothing was lost in this one; recording that here is what makes the check repeatable next round. Where each clause went: M4.8a takes the record and its format, M4.8b the write trigger and the key grammar, M4.8c the zero-LIST discovery and the ADR that chooses its mechanism. The >5,000-checkpoint workload is an acceptance constraint inside M4.8c, not a fourth piece. ⚠️ BOTH COPIES OF A SPLIT MUST SAY SO -- this row said `todo` and carried the whole source text while `backlog.md` said `split`, which is the one-copy-fixed failure M4.0's archive row names in its own words: fixing ONE copy of a fact and missing its duplicates in the tables that decide what gets built | FR-11 | split |

### M4.9

| M4.9 | Bounded recovery: newest checkpoint + at most K deltas, replacing today's unbounded full-chain replay. Asserted by REQUEST COUNT through `CountingBinStore`, not only by the reconstructed result -- a recovery that reads the whole chain anyway produces the right answer. ⚠️ Also tier 3 of M5's fallback ladder, run by up to 300 plugin nodes against R15's ~1 LIST/s ceiling ⚠️ AND IT MUST BOUND THE CROSSING, not merely own-chain replay -- added by M4.6e, whose cost test cites this row as the reason a forbidden threshold relaxation is acceptable, so the claim has to be true HERE rather than merely asserted there. M4.6e's crossing is TRANSITIVE: a takeover re-reads every entry of every ancestor chain, so per-failover cost is O(all commit-log entries ever written) and grows without bound over a cluster's lifetime. ⚠️ MEASURED on the staged tree at 12 deltas per term, GETs then TOTAL requests: 1 prior term 17/26, 2 terms 32/43, 4 terms 62/77, 8 terms 122/145, 16 terms 242/281. LISTs and stats each grow 3/4/6/10/18 -- one more of each per ancestor -- so bounding GETs alone would leave two request classes still scaling. ⚠️ An earlier draft of this row carried DIFFERENT numbers under the same word MEASURED: they were a reviewer's round-1 figures with only the first updated, describing no version of the code that ever existed. Corrected by re-measuring, and recorded because this row IS the evidence carrying a non-negotiable-2 exception | FR-10, FR-11 | todo |

### M4.10

| M4.10 | Commit idempotency on `(podId, flushSeq)` with `lastAppliedFlushSeq` per pod, including the case where the replay arrives after a checkpoint recorded it. Without this a retried pod is assigned offsets twice -- silent duplication that `_id` dedup would mask inconsistently ⚠️ AND `flushSeq` RESTARTS AT 0 EVERY PROCESS START while `podShortId` is stable, so `(podId, flushSeq)` is NOT unique across a restart: a pod that dies and comes back re-issues keys 0, 1, 2 for entirely new segments, and an idempotency check keyed on the pair would then SUPPRESS REAL COMMITS rather than duplicates -- worse than the duplication it exists to prevent. M4.6d introduced the field and its round-1 review found this; the fix belongs here, with whatever makes the pair durable (the lease's own epoch was the original candidate -- ⚠️ M4.10b MUST NOT INHERIT THAT: the epoch is the SEQUENCER's term, not the pod's incarnation, so it discriminates a restart only while producer and sequencer share a process, and stops doing so when M5's commit forwarding lands). ⚠️ THE DRIVER'S `flushSeq` ISSUANCE WAS FIXED IN M4.10a: per pod, per attempt | FR-3 | split |

⚠️ SPLIT AT THE POINT OF PICKING IT UP, not after a failed attempt. Three
things are separable, and IF a format change proves necessary, non-negotiable 8
requires it land atomically with its ADR, goldens, readers, writers and fakes
-- so it cannot be a hunk inside a larger commit.

M4.10b is DONE: ADR-0036 keys idempotency on
`(podId, incarnationId, flushSeq)` and both formats change, so M4.10c is not
conditional and is not void. M4.10d is blocked by M4.10c, because returning the
original offsets across a takeover needs the carrier M4.10c adds; its ANCHOR
alone needs no format change, since `Checkpoint.pods` already exists.

The primary argument is unchanged and is why the naive version is dangerous:
`flushSeq` restarts at 0 on every process start while `podShortId` is stable,
so dedup on the bare pair suppresses genuine commits rather than duplicates --
worse than the duplication it exists to prevent.

### M4.10b

`(podId, flushSeq)` is not unique across a pod restart: `DefaultIngest` holds
`flushSeq` as an instance field and `podShortId` is stable, so a restarted pod
reissues 0, 1, 2 for new segments and dedup on the bare pair suppresses REAL
commits.

⚠️ A FACT TO WEIGH, NOT A CONCLUSION. `SegmentKey.render` emits
timestampMillis, podShortId, then a per-pod sequence `SegmentPublisher` calls
"unique WITHOUT coordination". `podShortId` is stable, and the timestamp does NOT reliably advance across a
restart -- that premise was measured false and is what retired this candidate;
also
`SegmentPublisher.sequence` is an instance `AtomicLong` and restarts with the
process; the durable half today is `Checkpoint.pods`, one uvarint per pod -- written,
but recovered by nothing, which is the gap M4.10d's fold closes. Whether any of that suffices
is this ADR's finding, not this row's.

⚠️ ADR-0031 IS ADJACENT, NOT ANSWER: two LIVE processes racing an epoch at the
lease level. This is one process restarting. Say whether its argument extends.

The properties the decision must satisfy. They are stated as properties
because the mechanism is this ADR's to choose, and five rounds of these rows
choosing it produced five falsifiable guesses:

- A replay must be ANSWERED, not merely detected: M4.10d must return the
  original offsets, so whatever is recorded has to lead back to them.
- No object may grow with history (ADR-0033), and `CheckpointWriter`'s pods map
  has no eviction.
- It must add no COMMIT-PATH request that scales with the number of pods --
  the rate M4.7 was built to remove. Size is not rate, and this is the axis the
  two above do not constrain: a per-pod `ctl/pods/<podId>` object rewritten on
  every commit satisfies both and multiplies the commit-path PUT rate by the
  pod count. Resolving a replay by walking the chain backwards is the same
  fault in GETs. ⚠️ The SEGMENT path is different: pods/interval there is by
  design (ADR-0017, every pod writes).
- It must survive a checkpoint TRUNCATING the ancestry walk (M4.9) and a leader
  change.
- It must say which formats change, or that none do.
- It must say what happens to a window recovered from a checkpoint written
  BEFORE the change. Qualifying the key inside `Checkpoint.pods`'s existing
  `String` changes no format at all -- VERSION stays 0 and `decode` still
  compares equal -- so this hazard survives an answer of "no formats change".
  ⚠️ AND THE DIRECTION IS THE OPPOSITE ONE: against a pre-upgrade checkpoint
  nothing matches, so a real replay is ACCEPTED TWICE rather than a real commit
  suppressed.

### M4.10c

⚠️ NO LONGER CONDITIONAL. ADR-0036 decided the key and BOTH formats change; an
earlier draft of this section, written before that ADR, called the row possibly
void and declined to prescribe a shape. It prescribes one now.

`Checkpoint.pods` becomes `podId -> (incarnationId, lastAppliedFlushSeq, epoch,
sequence)` — the watermark AND a pointer to the delta that last applied for that
pod. Without the pointer, detection is unbounded-depth and answering is bounded
to the uncheckpointed tail.

`CommitDelta` carries the triple PER `SegmentCommit`, not once per delta, and
the delta's `podId` is AUTHORITATIVE for the window while `segmentKey` stays
opaque to the sequencer. ⚠️ Do not drop `podId` on the ground that `segmentKey`
holds `podShortId`: that is true of one CALLER, not of the record --
`SegmentCommit.segmentKey` is a `String` checked only for non-emptiness,
`SegmentKey` exposes no parser for the pod, and `chain-delta-batched-v1.bin`
holds `bins/a.seg` with no pod in it. `CheckpointWriter` merges on
`request.podId()`, so a tail without it leaves a rebuilt window with no key to
be written back under.
⚠️ WHAT THE TESTS MUST ASSERT, not what data they must carry: the PER-SEGMENT
triple, after round-trip and after `commitAll`. ⚠️ THE DATA REQUIREMENT STANDS TOO, and an
earlier draft of this paragraph retracted it on a false premise: `SegmentCommit`
is `(String segmentKey, List<RunCommit> runs)` and carries NO `podId` at all, so
no fixture varies by pod today — `BatchedCommitDeltaTest` varies only a key
STRING, which this same section argues is not pod attribution. So both are
needed: the fixtures must set DIFFERENT triples per segment once M4.10c adds the
fields, AND the assertion must read them back per segment. Give three segments
the SAME triple and `isEqualTo(original)` passes under encode-once,
fan-out-on-decode.
The maximal survivor is the PARTIAL hoist: `podId` per segment, incarnation and
flushSeq once per delta. `BatchedCommitDeltaTest`'s whole-record equality kills
a full hoist; `BatchingSequencerTest`'s two-pod case cannot see either: its only assertion on
that delta is `assertThat(delta.segments()).hasSize(2)`, and both requests are
`from("pod0", 0)` / `from("pod1", 0)` — identical flushSeq. ⚠️ The class DOES
assert per-segment run counts and offset tiling over a three-pod delta
elsewhere, which is the strongest per-segment fixture available and the natural
place to add the triple assertion.

⚠️ DO NOT REGENERATE `golden/chain-delta-batched-v1.bin` to carry the new field.
`GoldenChainEntryTest` says "Do NOT regenerate these to make the test pass", and
that file is the only proof a v1-kinded delta written by an earlier build still
parses. The discriminating golden is a NEW file beside it.

In PRODUCTION the two placements are byte-equivalent today, which is why the
assertions are the only thing standing between this and M5.

⚠️ BOTH DELTA PATHS CARRY THE FIELD — `VERSION_DELTA` and the kinded batch,
both or neither — BY A NEW VERSION, not by changing either existing layout: a
delta carrying no triple must still encode exactly as it does today, because
`GoldenChainEntryTest` asserts byte-identity on both goldens. `Checkpoint` is not a `ChainEntry` at all
— own MAGIC, own VERSION, no kind byte — and ADR-0033 requires it never decode
as one, so the two formats discriminate by different means.

⚠️ AND THIS ROW OWNS THE MINTING SITE, which no other row named and which
ADR-0036 calls its highest-risk clause. `CommitRequest` gains `incarnationId`
(it is never encoded, so it is not one of the two FORMATS) and `DefaultIngest`
mints it ONCE PER INSTANCE. Minting per FLUSH — `UUID.randomUUID()` beside
`flushSeq++` inside `flushLocked`, the smallest edit that compiles — makes
dedup a total no-op in production while every sequencer-seam suite stays green,
because those tests supply the incarnation themselves. The fixture that sees it
is `IngestCommitPathTest`'s recording sequencer: two `DefaultIngest` instances
sharing a `podShortId` must record DIFFERENT incarnations, and one instance's
two flushes the SAME one.

⚠️ AND THE JAVA SITES BELOW ARE THIS ROW'S, which an earlier sweep missed by
stopping at markdown. ⚠️ THE LIST IS THE AUTHORITY, NOT A COUNT — two drafts
gave a numeral and both were wrong. `git grep -n 'podId, flushSeq' -- '*.java'`
finds most of them, ⚠️ BUT NOT ALL AND THIS PARAGRAPH DOES NOT CERTIFY
OTHERWISE — two earlier drafts did and both were wrong. Known to the grep's
blind spot: the sentence promising the record shape does not change, spanning
`Sequencer.java:48-50`; and `CheckpointWriter.java:105-106`, "the only place the writer can see `podId`
and `flushSeq`, which the chain does not carry" — the premise this ADR
reverses, in the class M4.10d must change; and `CommitRequest.java:35-36`,
`@param flushSeq ...
Together with {@code podId} it identifies this commit uniquely across the
fleet`, which is the tree's strongest statement of the premise ADR-0036
measures false and which will sit three lines from the new `@param
incarnationId`. Read the javadoc BLOCKS, not the grep hits. Falsified by
ADR-0036 and load-bearing: `Sequencer.java:35` ("NOT that a given
`(podId, flushSeq)` is applied at most once — that is M4.10"), which will now
NEVER hold, because a restarted pod legitimately reissues `(podId, 0)`;
`Sequencer.java:46` and `:88`; the javadoc promising "carried from this first
commit precisely so M4.10 can make the retry safe WITHOUT CHANGING THE RECORD
SHAPE" (`Sequencer.java:48-50`), which this row falsifies by adding
`incarnationId` to `CommitRequest`; `CommitRequest.java:19`;
`FakeSequencer.java:27` and `:29`, and `FakeSequencerTest:178`;
`Checkpoint.java:32`; `IngestCommitPathTest.java:81`;
`CommitProtocolSimulation.java:58` and `CommitProtocolSimulationFlushSeqTest:23`.

⚠️ AN M4.10d SESSION READS THE SEAM, NOT THIS FILE. Left standing, those javadocs
tell it to key dedup on the pair; every M4 fixture is single-incarnation, so the
suite stays green while a restarted pod is SUPPRESSED.

⚠️ AND THREE DESCRIPTIVE `SPEC.md` SITES ARE THIS ROW'S, cited by TEXT because
this commit has already moved their line numbers twice and a correction moved
them again: the scope sentence beginning "from the outset", the Risks row
"The multi-pod gap becomes invisible", and the M4.1 task row. All three say the
commit request carries `(podId, flushSeq, …)`. They describe the
seam as it stands and ADR-0036 does not falsify them; they become incomplete
the moment this row lands.

⚠️ OLD OBJECTS MUST STILL DECODE. `Checkpoint.decode` compares against one
constant and `CheckpointCursor.newest` lets that `IOException` out where
`BinStore` defines it as "the store is unreachable", so a bare bump makes every
existing checkpoint look like an outage. M3.0: "SegmentReader accepts BOTH
versions ... golden/segment-v0.bin is untouched, not regenerated".

⚠️ A PODS-KEY SHAPE CHANGE REWRITES MORE THAN ONE ASSERTION, including the
`Math::max`-over-bare-`podId` test whose premise inverts. Budget for that
rather than discovering it.

### M4.10d

Refuse a replayed `(podId, incarnationId, flushSeq)` per ADR-0036 -- a replay
is `flushSeq <= lastAppliedFlushSeq` for the SAME incarnation -- including a
replay arriving after a checkpoint recorded it.

⚠️ THE REFUSAL'S CALLER SEMANTICS ARE THIS ROW'S TO DECIDE, and ADR-0036
deliberately does not state them, because TWO HISTORIES REACH THE REFUSE BRANCH
AND THE SEQUENCER CANNOT TELL THEM APART: flush 5 applied in one delta and 6 in
the next, with a duplicate of 5 arriving (the key DID apply, so a re-flush
duplicates the records); and a `flushSeq` consumed by `DefaultIngest`'s
unconditional `flushSeq++` on a commit that then threw. Density is over ISSUED flushes, not applied ones. A blanket
"refuse, then re-flush" is therefore unsafe, and a test written from it is
GREEN while one set of records holds two sets of committed offsets.

⚠️ PIN THE UPDATE RULE, NOT ONLY THE COMPARISON. `CheckpointWriter`'s existing
`pods.merge(podId, flushSeq, Math::max)` carries a comment arguing for exactly
the behaviour an incarnation-aware merge must NOT have: carried across
incarnations it refuses the SECOND commit after a restart, and the required
discriminator is depth-one and passes it. ADR-0036 also leaves the slot
destructible by an interleaving incarnation; pin that bound.

⚠️ THIS ROW OWNS THE END-TO-END ANCHOR, and it needs NO format change:
`Checkpoint.pods` already exists. Fold the recovered window into the
successor's `CheckpointWriter` pods map and INVERT
`CheckpointWriterContentTest`'s tripwire `doesNotContainKey("poda")`. Deleting
that assertion instead is the testing.md rule 5 inversion. Without this fold,
`lastAppliedFlushSeq` stays as process-local as it is today and duplicates ship
across every takeover — which `CheckpointWriter`'s javadoc calls exactly when
they are likeliest.

⚠️ THE PRIMARY ANCHOR IS A REFUSAL BY THE SUCCESSOR, with the checkpoint
inversion as its corroborating half. Seeding the pods map at write time and
leaving the dedup window empty makes `containsEntry("poda", 42L)` green while
the replay still commits twice. ⚠️ AN EARLIER DRAFT CLAIMED THE INVERSION GOES
GREEN WITHOUT THE FOLD AT `everyDeltas = 1`; both reviewers measured it RED
(`{"podb"=0L}` missing `"poda"=42L`), so the fixture already discriminates and
needs no K change. Only `flushSeq` is absent from the chain: `SegmentKey`
puts `podShortId` and a per-pod sequence inside the segment key that every
`SegmentCommit` carries, and each `RunCommit` carries the original offsets.

⚠️ THE DISCRIMINATOR IS A POD RESTART, NOT A TAKEOVER. A cross-leader-change
fixture has one incarnation of `poda`, so whatever M4.10b makes durable never
varies and dedup on the BARE pair passes it. The killing scenario is a
restarted pod reissuing 0 for a NEW segment, which must be ACCEPTED.

⚠️ ASSERT THAT NO DELTA CARRYING THE REPLAYED SEGMENT KEY WAS APPENDED.
`ChainReplay` folds with `merge(key, lastOffset + 1, Math::max)`, so
re-appending a run at its original absolute offsets leaves `nextOffset`
UNCHANGED — an offsets assertion is structurally blind to a re-append. And
`CommitLog` PUTs one `CommitDelta` and returns that same object, so there is no
seam today between what is appended and what is returned.

⚠️ `Invariants` IS NOT THAT ASSERTION, MEASURED BOTH WAYS: it throws
`IllegalStateException` on a batched delta (`runs()` where production uses
`allRuns()`), and it returns `[]` for the same segment key committed twice at
fresh contiguous offsets, because it tracks per-`RunKey` offsets and has no
duplicate-segment arm. Whether M4.10d adds one is M4.10d's to decide.

⚠️ WHAT THE RETRYING CALLER GETS BACK IS PART OF THE CONTRACT — and this is
about `Sequencer`'s published contract, not about any unchosen mechanism.
Skip-and-return satisfies "refuse", but `Sequencer` defines the return as what
a caller matches by its submitted segment key and `DefaultIngest` then
completes the append exceptionally: a retry after an ambiguous IOException, the
case M4.10 exists to make safe, becomes a producer-visible failure on a flush
that LANDED. Returning each run's present `nextOffset` instead of the original
passes every other assertion here while handing the producer offsets the
sequencer will reuse.

⚠️ `commitAll` IS THE PRIMITIVE, so a batch may mix a replay with fresh
segments. Refusing the batch loses the co-batched flush, against `Sequencer`'s
"the durable delta carrying every submitted segment".

⚠️ TWO MUTATIONS THAT SURVIVE ANY CHOICE M4.10b MAKES: `<` for `<=`, since the
exact-equal replay is the retry case and `CheckpointWriterContentTest` replays
strictly lower (9 then 4); and a single fleet-wide `long` for the per-pod map.
A third is mechanism-independent too: a NO-OP dedup that records, checkpoints
and recovers `lastAppliedFlushSeq` and never CONSULTS it.

⚠️ NO PRODUCTION CALLER REPLAYS A PAIR TODAY: `DefaultIngest` is the sole
issuance site and increments unconditionally. `FaultInjectingStore.Faults`'s
ambiguous and duplicate PUT arms are the existing seam. So this row must
either add the retry path or say plainly that its coverage stops at the
sequencer seam.

### M4.22

| M4.22 | MAKE THE CHECKER SAY HOW MUCH IT EXAMINED. `Invariants.checkChain` returns an empty list both for a chain that held nothing wrong and for a chain that was never there -- measured: on an empty store it returns `[]` for any epoch and any prefix -- so a caller cannot tell CLEAN from UNREAD. That is the failure mode the class exists to prevent, occurring in the class itself, and it is the checker-side half of M4.20's `SEED 17 COMMITS ZERO AND REPORTS CLEAN`. ⚠️ SPLIT OUT OF M4.19 at round 2 per review.md rule 12: an attempt at it there added a `Report` carrying `entriesExamined`/`boundariesFollowed` whose counters were DECLARED rather than derived -- hard-coding them left the suite green, `boundariesFollowed` had no reader anywhere, and `entriesExamined` came from `readChain` rather than the walk, so gutting the walk did not drive it to zero. That is `crossEpochOffsetsChecked()`'s shape reintroduced one step milder, in the same commit that deleted it. ⚠️ SO THE ROW'S REAL WORK IS THE FALSIFIABILITY, not the accessor: whatever is reported must go to zero when the examination is removed, and a fixture must prove it does. ⚠️ THE DRIVER NEEDS A NARROWER VERSION OF THE SAME THING, and the first draft of this clause overstated it: `Result` DOES carry `commits`, and 208 at ROUGH against 2,133 CLEAN over the same seeds is a loud difference, so a leaderless run is NOT indistinguishable from a healthy one. What is missing is a measure of ROUNDS PRODUCTIVE -- measured at ROUGH, only 300 of 3,600 rounds ever had a leader and 3,223 ended with `tryStart` empty, and nothing in `Result` says so. That matters because the two are different failures: few commits means the cluster is degraded, while few rounds LED means it spent its time failing to elect anyone, which is what M4.17's missing renew tick and M4.16's burned epochs both produce | FR-11 | todo |

### M4.23

| M4.23 | AN UNSEALED ANCESTOR IS INVISIBLE WHEN EPOCHS WERE SKIPPED, and M4.16 records that as the NORMAL case. `checkLink` is applied only to the epoch the CONTINUE NAMES, never to the epoch the walk-back lands on and actually inherits from -- so a run of burned epochs hides an unsealed ancestor entirely. Measured: epoch 1 with a delta and NO seal, epoch 2 PRESENT BUT NEVER OPENED (a `Seal` at slot 0 and nothing else), epoch 3 continuing from 2 and resuming correctly, reports no violations at all. ⚠️ The clause originally said epoch 2 was ABSENT, which does NOT reproduce -- an absent named chain reports `link: NOT SEALED`, because M4.19 withdrew the early return that used to swallow it. The mechanism is real; the fixture had to be the never-opened shape. ⚠️ AND THE OPPOSITE ERROR IS UNPINNED TOO: an early return added in M4.19 for an EMPTY named chain removes a false positive on a real fault shape (a store outage spanning a failover) and was itself unfalsified -- removing it also left the suite green -- so the trade is unconstrained in BOTH directions and was withdrawn rather than shipped unpinned. ⚠️ Also here: the depth cap at 1000 returns an EMPTY inheritance silently where production THROWS, and `ChainReplay`'s javadoc records that same cap as measured-FALSE at 1,002 legitimate terms -- so it refuses a healthy cluster and masks a cycle. Both need the protocol's own strictly-decreasing rule instead, WITH a fixture building a non-decreasing CONTINUE, which M4.19 added without. ⚠️ AND `neverOpened`'s PRECISION IS UNPINNED: weakening `sealed.sequence() == 0` to `entries.getFirst() instanceof Seal` leaves all 180 sequencer tests green, though gutting the method entirely IS caught -- so what is unconstrained is specifically the slot-0 test that distinguishes "sealed before it was ever opened" from a chain whose first visible entry is a seal somewhere else. Disclosed by M4.19's round-4 review rather than left to be rediscovered | FR-11 | todo |

### M4.24

| M4.24 | THE CHECKER'S OWN COST IS O(E-squared) and it decides whether M4.13's stated budget is reachable. `checkChain` per epoch times `firstAncestorWithDeltas` scanning every lower epoch measured 4,999 LISTs for ONE examination at epoch 5000, against a sweep budget of <60s for 1,000 seeds. ⚠️ AND THE DRIVER'S SCAN BOUND IS 3x UNREACHABLE: `scanTo = pods * rounds` scans 360 epochs where the measured peak is 105 and the mean 97, so ~15k redundant LISTs per sweep (~5.6s extrapolated to 1,000 seeds). ⚠️ Its guard probes exactly ONE epoch above the bound and a failed `start` burns an epoch that writes no chain, so the "ASSERTED, NOT ASSUMED" claim on it describes a proposition that cannot fail -- worth either a real bound derived from the last sequencer's epoch, or dropping the claim ⚠️ NOT a production request-rate defect -- this is T1 harness code over `MemoryBinStore` and no billed rate moves -- which is exactly why it needs its own row rather than a cost-budget objection. ⚠️ Also: ancestor violations are appended to each DESCENDANT's list, so one defect inflates the counts of every chain below it -- and M4.13 must pin exact counts | FR-11 | todo |

### M4.25

| M4.25 | A DANGLING JAVADOC IN COMMITTED CODE, and javac warns on every single build. `FaultInjectingStore.java:144`'s single-threading note sits above `/** Every fault this run injected, in order. */`, so two comment blocks stack and the first attaches to nothing: `warning: [dangling-doc-comments] documentation comment is not attached to any declaration`. ⚠️ SHIPPED IN M4.12 (`67606ab`), which fixed this exact defect once at a different line and reintroduced it at another when a later round added the threading note -- so the fix and the regression are in the same commit. ⚠️ NOT FIXED IN M4.19 because M4.19 is the invariant checker and this is the fault injector: touching it there would be silent scope widening, which is the rule that has cost this milestone more rounds than any other. ⚠️ WORTH A GATE, not just a fix: javac already emits it on every build and nothing fails, which is the definition of a warning nobody reads. `-Werror` for `dangling-doc-comments` in the java-conventions plugin would make the bad state unrepresentable -- gate-design rung 1 -- and the same class of defect appeared twice in one task without either instance being caught by anything but a reviewer's eye. ⚠️ SECOND PREDICATE FOR THE SAME ROW, same shape: `InvariantFixtures.FIXED_CLOCK` can be swapped back to `Clock.systemUTC()` with all 18 invariant tests green, so the no-wall-clock convention is a claim that cannot go false -- exactly what `crossEpochOffsetsChecked()` was deleted for. A grep predicate over `sequencer/src/test/java/` is the rung-3 answer, and it generalises: M4.12 and M4.19 each had to move fixtures off `systemUTC()` after a reviewer noticed, twice | — | todo |

### M4.26

| M4.26 | THE OLDER FINDING-ARGUE PATH READS THE WORKTREE, so a wholly UNTRACKED `baselines/review.txt` silences blocking and major findings from both roles and the gate prints `ok`. ⚠️ MEASURED by M4.21's round-1 review, and it is the heavier of the two argue paths: review.md rule 9 and the baseline file's own header both state that staging is required, precisely because an unstaged entry suppresses a finding while leaving NO TRACE of it. M4.21 closed exactly this hole for its new `rounds:` key -- matching the ADDED LINES of `git diff --cached` under a literal pathspec -- and left it open, in the same file, for the key that silences majors. ⚠️ A draft said "fourteen lines below", a distance measured in HEAD, which this commit rewrites -- and no replacement offset is given here, because the sentence denouncing line offsets should not contain one. A line offset into a file the same commit is editing is a citation with a half-life. ⚠️ A DRAFT OF THIS ROW SAID "reading the INDEX via `git show :`", WHICH M4.21 DID NOT SHIP -- there is no `git show` in `check-reviewed.sh`. M4.31, filed by the same commit, prescribes exactly that read as the fix still outstanding, so two new rows contradicted each other about what the code does, and the wrong one was the worked example an implementer of THIS row would have copied. ⚠️ Also: the baseline file's header documents only `<role>:<finding-id>`, so the `rounds:<task>` key M4.21 added is undocumented in the one file an author edits to use it. ⚠️ AND THE OLDER KEY REQUIRES NO REASON: a file containing only `reviewer:R1` and `test-reviewer:R1`, with no justification at all, silences two blocking findings and the gate prints `ok`. M4.21 made its new key require one and left the heavier key without. ⚠️ NOT VIA `NF > 1`, which a draft of this row prescribed and which M4.21 MEASURED AS BROKEN: a trailing tab splits into a second, EMPTY field, so `rounds:M9.1<TAB>` argued the cap while justifying it nowhere. What shipped strips the key and requires a non-space remainder (`sub(/^[^[:space:]]+[[:space:]]*/, "", rest)` then `rest ~ /[^[:space:]]/`). Handing `NF > 1` to whoever implements THIS row would ship the same defect on the heavier key, where `reviewer:R1<TAB>` would silence a blocking finding -- the second wrong worked example this row has carried, after `git show :`. So this row is THREE fixes: read the index, document the keys, and require a reason | — | todo |

### M4.27

| M4.27 | M4.20 SHIPPED WITH TWO OF THE FIVE CONJUNCTS ITS ROW STATES AS ACCEPTANCE CRITERIA UNPINNED, found by a SECOND, independent test review after the commit had landed (`2e0f1d9`) -- the first test reviewer passed it. ⚠️ MEASURED, each mutation alone leaving 9/9 green: forcing every leader to `pod0` while still consuming the draw (so replay is unaffected) defeats "MANY LOGICAL PODS", and a takeover then never crosses a `holderPodId` boundary -- which is what `LeaseManager.held()` and the `(epoch, podId)` fencing key are built on; and disabling the polite `close()` arm defeats "failover BOTH ways", leaving only the ungraceful half pinned by `zombieAttempts`. ⚠️ THE FIX MOVES NO THRESHOLD: a `Set<String> leaderPods` and an `int politeReleases` on `Result`, asserted `size() > 1` and `isPositive()`. ⚠️ Also from that review: swapping the two adjacent `int` arguments to the `Result` constructor compiles and leaves 9/9 green, so `zombieAttempts` can silently become `zombieWrites` -- the number the file says must never be asserted satisfying the assertion that must be; the honest pin is `zombieWrites < zombieAttempts`, a harness invariant that stays true when M4.16 drives writes to zero. And `highestEpoch` is only lower-bounded, so dropping the `hasChain` guard makes it 360 for every run with everything green. ⚠️ NO RECORDED ARTIFACT BACKS ANY OF THIS: the second review's `record` was refused because the staged bytes had moved on to M4.21 by the time it finished, and it correctly declined to rewrite `diff_sha256` rather than bind an M4.20 verdict to an unrelated diff. So `.harness/review/` holds three hashes for M4.20 and none carrying this major, and every measurement here is unreproducible from the tree -- the same disclosure the M4.19 and M4.20 rows make about themselves. ⚠️ THE CAUSE WAS MINE: I began M4.21 while that review was still running | FR-11 | todo |

### M4.28

| M4.28 | THE ARGUED-CAP `WARN` IS INVISIBLE WHERE IT FIRES. pre-commit prints a hook's stdout only on FAILURE unless the hook sets `verbose: true`, and `.pre-commit-config.yaml` does not -- verified by running `pre-commit run check-license-headers`, which printed `Passed` and swallowed that script's own `ok`. So an argued third round shows a plain green `Passed` and the warning nobody sees. ⚠️ JUDGED BY M4.21's ROUND-2 REVIEW AND I AGREE: the WARN is NOT the alarm, and M4.21 is right not to rest on it -- the alarm is the staged `rounds:` line, which is in the diff and reviewed with the commit it excuses. That is why this is a row rather than a fix inside M4.21. ⚠️ THE CHOICE IS NOT OBVIOUS, which is why it wants deciding rather than doing: `verbose: true` changes EVERY commit's output for a path taken rarely, and a gate that prints on success trains the eye to skip it -- the same reason `check-reviewed`'s vacuous `ok nothing staged` is called out in AGENTS.md as worse than a failure | — | todo |

### M4.29

| M4.29 | `review.sh record` DOES NOT VALIDATE `--task`, which is a COMPLETE bypass of rule 12's round cap and leaves no trace. ⚠️ MEASURED by M4.21's round-3 review: recording both roles under `--task M9.7` with four prior rounds on disk gives `ok ... (round 1 of 2)`, exit 0; omitting `--task` writes `"task": ""` and gives `round 0 of 2` over two present verdicts -- a count the counter's own definition makes impossible. `[ -n "$TASK" ]` is checked in the `context` arm only. ⚠️ M4.21 closed the half visible from the gate (verdicts present with no task now REFUSE) and left this, because the fix is a real choice in an untouched file: require `--task`, validate it against `backlog.md`, or bind it in `check-commit-msg.sh` -- the only place the commit subject's id and the staged hash are both in scope, which is the option that actually ties the verdict to the task being committed. ⚠️ AND NO SCRIPT CAN CLOSE THE REST: a reviewer recording under a plausible wrong task is non-negotiable 4 territory, which AGENTS.md says no gate reaches | — | todo |

### M4.30

| M4.30 | THE REVIEW PACKET SAYS "round 1 of 2" ON EVERY ROUND, AND SWALLOWS THE REFUSAL M4.21 ADDED, so the one number that decides whether rule 12 is about to bite is wrong in the document the reviewer reads. ⚠️ MEASURED by M4.21's round-3 review, which received a packet reading `This is round 1 of 2 for M4.21` above `=== THIS IS NOT ROUND ONE ===`. Two causes: `review.sh` still carries the `|| echo 0` that M4.21 removed from the gate, so `review_rounds.py`'s new disagreement refusal is swallowed there; and `rounds_for` resolves the task from verdicts FOR THE CURRENT HASH, which by definition do not exist yet on a fresh round. ⚠️ `review_delta.py` already does it right by taking the task as an argument, so the fix is to follow that. ⚠️ Filed rather than fixed in M4.21 because `review.sh` is outside its staged files -- but M4.21 is the commit that makes the round count authoritative, so the packet being wrong about it matters more now than it did | — | todo |

### M4.31

| M4.31 | Key the argued round-cap exception to the STAGED CONTENT, not to how git RENDERS the diff. ⚠️ `check-reviewed.sh` decides an argument is present with `git diff --cached -- baselines/review.txt | awk '/^\+/'`, which asks "does this text sit on an added line" -- a THIRD question, neither "is the argument staged" (review.md rule 9) nor "is it new to this commit". It is wrong in both directions, and M4.21's review measured both. ⚠️ TOO LOOSE: git renders a MOVED committed line as delete+add, so sorting or otherwise RELOCATING an already-argued `rounds:<task>` line re-arms the escape for a later commit -- measured on a staged edit that RELOCATES the committed `rounds:` line, so git emits it as a deletion in one place and an addition in another and the added copy matches. ⚠️ A draft of this row said the trigger was "inserting one `reviewer:` entry above it", which does NOT fire on its own -- an insertion above leaves the line rendered as CONTEXT and the awk never sees it. The line has to MOVE. ⚠️ TOO STRICT, and this is the same defect M4.21 exists to close, one level in: once an argued commit has LANDED, a further round for that task cannot argue at all, because the line is now committed rather than added -- and `git add baselines/review.txt`, which is literally what the gate's remedy instructs, is a no-op on an unchanged file. The only routes out are rewording the line (undocumented) or `SKIP=check-reviewed`, which is the state M4.21 was filed to end. ⚠️ THE RENDERING-IMMUNE PRIMITIVE, and it is rung 2 of the gate-design ladder rather than rung 3: compare `git show :baselines/review.txt` against `git show HEAD:baselines/review.txt` and require a keyed line present in the first and absent from the second. Re-arguing a later round then means writing a NEW reason line, which is a property worth having rather than a workaround. ⚠️ AND A THIRD ARM, found by round 8's test review: nothing pins ADDED against REMOVED. Widening the awk selector from `/^\+/` to `/^[-+]/` leaves all fifteen tests green, and a staged DELETION of a `rounds:<task>` line then argues the cap -- so tidying a stale argument out of the file re-arms the escape being tidied. Reachable rather than equivalent. The staged-vs-HEAD primitive kills all three arms structurally, which is the argument for replacing the primitive rather than patching each arm. ⚠️ M4.21 shipped the TEXT half of this (the gate now prints that the line must be ADDED by this commit) and left the primitive, because changing it needs tests for the moved rendering and the re-argue case, and ⚠️ NOT because adding a test re-keys the file -- MEASURED, and it does not: appending a new `@Test` method left all fifteen existing red records unchanged (`check-tdd.sh` still reports "unchanged since"), because `tdd_scan.py`'s remainder is per-test, not per-file (M0.56). The real cost is DESIGN, not TDD bookkeeping: the new tests need the staged-vs-HEAD read this row prescribes, which is a different fixture shape from every existing test's added-line read, so it is new coverage to design and verify rather than a mechanical addition. That is review.md rule 12's SPLIT, taken at round 5 rather than argued past. | M4.21 | todo |

### M4.33

| M4.33 | TWO SKILL BODIES STILL DESCRIBE THE PRE-ESCAPE WORLD, and skills are the layer an agent actually reads. ⚠️ `.agents/skills/review/SKILL.md` § Resolution describes `baselines/review.txt` as holding "a staged entry naming the finding's id" -- true of the older finding-argue path, but M4.21's `rounds:<task>` key names a TASK, not a finding, and appears nowhere in the skill. ⚠️ `.agents/skills/milestone/SKILL.md`'s bounds table gives the 2-round breach action as "Stop and report ... only a blocking finding may extend it", which is now incomplete (an argued exception extends it) and is ALSO the inverse of review.md rule 12's own text -- "a blocking finding in round two does not buy a round three". The inversion predates M4.21; the incompleteness does not. ⚠️ WHY THIS MATTERS MORE THAN A DOC NIT: the milestone loop reads layer-2 skill bodies and does NOT read layer-3 standards, so an agent at the cap follows the skill, stops, and reports -- never using the escape M4.21 built. The mitigation that keeps it minor is that `check-reviewed.sh` prints the full remedy at the moment of need. ⚠️ Distinct from M4.26, which is about `baselines/review.txt`'s own header and the worktree-read hole in the older finding-argue path. Found by M4.21's round-5 review. ⚠️ FILED AS M4.32 FIRST, AND RENUMBERED: a second agent session working in this same checkout filed its own M4.32 (implementing ADR-0029 in `LocalSequencer.start`) minutes earlier. Two tasks under one id in the authoritative list is the unresolvable-id defect with both halves present, so this one moved. | M4.21 | todo |

### M4.34

| M4.34 | THE REVIEW PACKET TRUNCATES SILENTLY AT ~37 KB, so a reviewer judges a diff it was never shown and is not told. ⚠️ MEASURED on M4.21's round-6 production review: `./scripts/review.sh context` cut off partway, dropping the last third of `ReviewRoundCapTest.java` and most of the backlog rows. The reviewer concluded three of the coverage cases the task claimed were untested, then found on its own initiative that all three existed and that its packet was the problem -- so the near miss was a MAJOR filed against real code on the strength of bytes the reviewer never received. ⚠️ WHY IT IS WORSE THAN A SIZE LIMIT: the cut is silent. A truncated packet is indistinguishable from a complete one, so the reviewer cannot discount its own conclusions, and non-negotiable 5's guarantee -- that the staged bytes were read by two agents that did not write them -- quietly becomes a guarantee about a prefix. This is the `ok nothing staged` failure shape: a green-looking result that means less than it appears to. ⚠️ FIX IS A CHOICE and rung 2 beats rung 6: either refuse to emit a packet over the cap (fail closed, the author must split -- which is what rule 12 wants anyway), or emit the diff in numbered parts and state the count so a reviewer can tell it has them all. Do NOT solve it by asking the reviewer to notice. ⚠️ Found by the review it damaged; the verdict itself was `pass` and its findings held up. | — | todo |

### M4.13

| M4.13 | The 1,000-seed run asserting I1-I5; every failing seed pinned as a named regression test. T1 (MemoryBinStore) so it runs on every commit, with a stated budget of <60s for 1,000 seeds and a configurable seed count. ⚠️ Each injected fault class must have at least one seed where it changes the outcome, or the simulation proves the simulator rather than the system ⚠️ AND IT OWNS THE TWO FAULT CLASSES M4.12 DOES NOT MODEL -- delayed writes and reordered completions -- plus partitioned leaders as an actual injectable fault rather than only its consequence, which needs the store to know WHICH pod is calling and needs `putIfMatch` to be able to fail cleanly. Criterion 1 requires each class to have at least one seed where it CHANGES THE OUTCOME, reported by the harness; recorded here because M4.12 is done and a closed row is no place to leave an unmet criterion ⚠️ AND THE WITHHELD-WRITE AMBIGUITY, a gap in the FAULT MODEL rather than in its tests: M4.12's ambiguous arms attempt the write before throwing, so whenever the CAS would have won the fault always resolves as LANDED, and there is no arm where the response was lost and nothing landed. `AmbiguousPutStore` already has `Mode.LOST` for exactly that shape. Without it the sweep cannot produce "the renew threw, nothing landed, and the retry with the same version must succeed" -- half of what M4.3d and M4.3h were about | FR-11 | todo |

### M4.17

| M4.17 | NOTHING RENEWS THE LEASE, so a healthy leader loses it every TTL and the cluster fails over for no reason. `LocalSequencer` acquires in `start` and never calls `LeaseManager.renew()` again; `DefaultIngest` holds the sequencer for its whole lifetime. ⚠️ `LeaseManager.renewInterval()` EXISTS and is public, and M4.3b built renew and tested it -- what is missing is the TICK that calls it, which no row owned until this one. ⚠️ Liveness, not safety: every safety property still holds, because a leader that silently lapses is exactly the fenced leader the seal protocol handles. The cost is an unnecessary failover every TTL per pod, plus a BURNED EPOCH each time -- and burned epochs are precisely what M4.16 shows turn into an I2 violation, so this row and that one compound. ⚠️ Found by M4.6d's round-1 production review, which noted the ingester ACQUIRED this obligation the moment it took a `Sequencer` | FR-11, NFR-9 | todo |

### M4.18

| M4.18 | `DefaultIngest` takes a `store`, a `prefix` AND a `Sequencer` that already has its own store and prefix, and nothing checks they agree. Before M4.6d the relationship was STRUCTURAL -- one constructor built the `CommitLog` from those same two arguments -- and injecting the seam turned it into a convention. ⚠️ The failure is silent and total: a sequencer on a different prefix commits to a chain no consumer of this prefix ever lists, so every append is ACKED and every read 404s -- the same invisible fork M4.6d exists to close, arriving by a different door. ⚠️ Options worth weighing, and this is the cheap end of gate-design: expose the prefix on the `Sequencer` contract and check it at construction, or stop passing `prefix` and `store` separately and let the sequencer own the addressing. The second makes the bad state unrepresentable and is probably right, but it moves the seam. Found by M4.6d's round-1 production review | FR-11 | todo |

### M4.7c

| M4.7c | WIRE THE BATCHER INTO THE INGEST PATH, and split `DefaultIngest` so it fits. ⚠️ M4.7 built `BatchingSequencer` and nothing constructs one outside its own test, so the commit rate reduction is available and not yet TAKEN -- a reviewer named this and it is recorded rather than glossed. Two things block it, and both are this row's: `DefaultIngest.flushLocked` still reads `delta.runs()`, which refuses a multi-segment delta outright, so it must match the segment key it submitted (the rule the `Sequencer` contract settles); and `DefaultIngest` is at 503 of the 500-line limit, so the fix cannot land there without splitting the file -- M4.7 tried, measured 505 and then 503 after two rounds of trimming, and reverted rather than move a threshold in the weakening direction. ⚠️ ALSO NOTE the reduction is CROSS-POD and cross-pod commit forwarding is M5: a single `DefaultIngest` holds its lock across `sequencer.commit`, so one ingester can never have two requests in a window, and no single-pod test can show the batcher earning its keep | NFR-1 | todo |

### M0.74

| M0.74 | SHARE THE REVIEWERS' DEPENDENCY CACHE, and keep their trees apart. ⚠️ WHAT IS SHARED AND WHAT IS NOT is the whole task, because getting it backwards recreates the defect `review-tree.sh` exists to prevent: a per-tree `build/` is the isolation that matters -- two reviewers writing one clobber the test-results XML every pass/fail count reads -- while `GRADLE_USER_HOME` holds the wrapper distribution, the dependency cache and the build cache, none of which is evidence. ⚠️ MEASURED on this machine rather than asserted: a cold `./gradlew -p buildSrc test` in a freshly materialised tree took 376s and then 233s with private Gradle homes, one per tree, paid TWICE A ROUND forever; with one shared home it is 239s to fill and 59s to reuse. The home reaches 820M, which is the number that was previously being re-fetched per tree. ⚠️ The test asserts BOTH HALVES AT ONCE -- same home, different trees -- because a test for either half alone passes straight through the regression that matters. ⚠️ NO SWEEP, and this is a deliberate refusal of the second half of the original proposal: `review-tree.sh`'s history is that the `rm -rf` was the dangerous part, an earlier draft having been measured destroying a live reviewer's tree mid-review, and an age-based sweep puts it back. The tree count and size are REPORTED past a threshold instead, with the one-line removal command, so reclaiming stays one deliberate act by a person who knows no review is running. | — | todo |

### M0.73

| M0.73 | CUT LAYER 1 -- every skill's `description:` line, and only that line, is what an agent sees before deciding whether to load a body, so every session pays for every skill whether or not any is used. ⚠️ MEASURED: 3,034 bytes across 13 skills here against pstore's 1,011 across 5; eight of the thirteen had grown past 200 characters and become summaries of their own bodies. A description says WHEN TO USE THIS -- "write an ADR" is a title, "use when making a choice that is expensive to reverse" is what gets the skill loaded at the right moment. Capped at 200 and gated. ⚠️ `milestone-review` FOLDED INTO `milestone`: it was invoked from exactly one place, step 4 of milestone's own finishing procedure, so a session doing milestone work loaded two bodies and every session paid two descriptions to reach one procedure. ⚠️ `next-task` IS DELIBERATELY NOT MERGED, against the original proposal and on measurement: it is 1,821 bytes and `milestone` is 6,244, so a session that only wants "what next" would load three and a half times as much to save ~210 bytes of layer 0. Merging it is negative for the common case. | — | todo |

### M0.72

| M0.72 | NAME THE REVIEW RULES. ⚠️ A rule cited by NUMBER names nothing: resolving a bare `rule 12` means loading a 14.8 KB standard to learn which rule that is, and it is the defect AGENTS.md already forbids one level out -- never report to a person in bare task IDs -- applied to rules instead of tasks. pstore, the harness this one was distilled from, made it a non-negotiable after the same thing bit it: "the numbering is for reading order and shifts when a rule is added -- a numbered cross-reference in another file goes stale silently. It already did once." ⚠️ THE NUMBERING WAS ALREADY UNDER STRAIN: review.md carries rules 1a, 1b and 3a, which exist because inserting a rule would have renumbered every citation in the tree. ⚠️ AND IT HAD ALREADY DRIFTED -- the gate's first run over the live backlog found a citation of a `12b`, a citation resolving to no rule at all, describing what is actually the `hash-bound` rule. TWO PREDICATES: a numeric citation is refused, and a NAMED one must resolve to a rule that exists, because otherwise a name is a number with better spelling. The name set is derived from review.md itself (rung 2), anchored on the CLOSING `**` of each statement rather than the line it opens on -- several rules wrap, and a line-anchored pattern silently missed two of them. ⚠️ THE ARCHIVE IS NOT JUDGED: backlog-done.md and backlog-notes.md hold rows verbatim, and a style gate that edits the historical record is not a style gate. | — | todo |

### M0.71

| M0.71 | BOUND WHAT EVERY SESSION LOADS. ⚠️ MEASURED against pstore, the harness this one was distilled from: its always-loaded set is 10,053 bytes and this project's was 26,856 -- AGENTS.md alone 20,936 against pstore's 6,664. The excess was incident narrative: two blind-spot paragraphs on `check-tdd`, a seven-row "what CI can and cannot enforce" table, the `--no-verify` caveat. All true, all worth keeping, none of them an index, and all paid for on every session that never touches a gate. AGENTS.md says of itself that it is "deliberately an index" -- prose in the file nobody gates, the same defect M0.70 records for the backlog. ⚠️ TWO MOVES, and neither deletes anything: the narrative goes DOWN a layer (build.md gets `What CI can and cannot enforce` with both `check-tdd` blind spots; review.md gets `Reporting to a person`), and CLAUDE.md stops importing `.agents/skills/README.md`, whose 5,920 bytes were mostly the SAME generated skills table AGENTS.md already carries -- it is layer 2, read when writing a skill, which is what `skill-forge` is for. ⚠️ THE GATE DERIVES THE SET FROM CLAUDE.md's `@` IMPORTS, rung 2 of the gate-design ladder rather than rung 3: a hardcoded list would keep reporting the old number the day someone adds a third import, which is the failure mode of every hand-maintained list of what runs. Pinned by a test whose two arms differ only in whether the second file is imported. | — | todo |

### M0.70

| M0.70 | SPLIT THE BACKLOG AND GATE ITS SIZE, because it is read WHOLE at the start of every session and nothing was collecting it. ⚠️ MEASURED: 18,638 bytes on 2026-08-31, 315,383 on 2026-09-05 -- monotonic, seventeen-fold, five days. 71,149 of those bytes were preamble before the first heading; 84,501 were the rows of M1, M2 and M3, all complete; the longest single row was 18,845 and the median line 458. The file's own first line has said "current milestone only" throughout, which has been false since M2 closed -- rung 7 of the gate-design ladder for a rule that is a predicate over the file. THREE FILES: `backlog.md` keeps open rows, each a summary; `backlog-done.md` takes the landed ones and nothing loads it; `backlog-notes.md` holds the full verbatim row for anything too long to be a row, opened for the ONE task a session picks. ⚠️ LOSSLESS AND VERIFIED AS SUCH: all 210 rows are present exactly once across backlog + archive, and every one of the 58 summarised rows has its ORIGINAL LINE verbatim under its ID in the notes -- checked by comparing against `HEAD:backlog.md` rather than asserted. ⚠️ NOT A WEAKENING: nothing is deleted, `check-commit-msg.sh` now resolves an ID across `backlog*.md` so a completed task stays nameable, and `review.sh` prints the notes entry for the task under review so the packet loses nothing. | — | todo |

### Preamble, as backlog.md carried it before M0.70

# Backlog

Authoritative task list for the **current milestone only**. Each row is one
commit. IDs are stable and never reused. `scripts/check-commit-msg.sh` requires
a commit subject to start with an ID present in this file.

⚠️ **M-1.1 was split into M-1.1 (documentation) and M-1.2 (machinery).**
review.md rule 12: as one 129-file commit it took five review rounds and ~2.5
hours, and every extra round found defects in the previous round's *fix*. Docs
and enforcement code want different lenses, so they are different commits.

⚠️ **The first commits land M0.4 before M-1.1.** Task IDs are stable
identifiers, not an ordering mandate ([git.md](../standards/git.md) rule 1 only
requires the subject to name a real task). M-1.2 carries the parser regression
suite (M0.18), those tests are Java, and Java needs the build that M0.4 brings —
so the build goes first.

**Current milestone: M4 — Sequencer and commit log**, specified in [milestones/M4/SPEC.md](milestones/M4/SPEC.md). M1 (walking skeleton), M2 (segment format) and M3 (ingest path) are complete, per [M1/VERIFIED.md](milestones/M1/VERIFIED.md), [M2/VERIFIED.md](milestones/M2/VERIFIED.md) and [M3/VERIFIED.md](milestones/M3/VERIFIED.md). ⚠️ This line said "M2" through the whole of M3 -- it is not generated, so nothing caught it.

| ID | Task | Serves | State |
|---|---|---|---|

### M4.35

| M4.35 | `BatchingSequencer.close()` fires `committer.interrupt()` UNCONDITIONALLY, including while the committer is inside its own `closeDelegateQuietly()` -- whose `Thread.interrupted()` is a ONE-SHOT clear that has already happened. ⚠️ THE INTERLEAVING: the committer dies abnormally, fails the queue, and enters `delegate.close()` == `LeaseManager.release()`, which takes the interrupt-insensitive `tryLock()` and proceeds into `releaseLocked()`'s store write; `close()` then interrupts. ⚠️ THE LIVE ROUTE IS `takeOrFail`, NOT THE STORE WRITE. Under contention `release()` falls through to `BoundedLock.takeOrFail`, which is interrupt-sensitive. That is the hazard. ⚠️ THE STORE WRITE IS NOT INTERRUPTIBLE TODAY, and two drafts of this row claimed otherwise. `releaseLocked()` issues `putIfMatch`. `LocalFsBinStore.putIfMatch` opens no channel: it is `Files.createTempFile`, `Files.copy` and an atomic move. Both round-3 reviewers measured it independently on this tree's JDK 25, with the flag pre-set, on platform and virtual threads: the write COMPLETES and the flag stays set. Only `putIfAbsent` opens an explicit `FileChannel` and throws `ClosedByInterruptException`, and `putIfAbsent` is the ACQUIRE path, not the release one. A future NIO-backed S3 store may add that route. Neither backend that exists has it. Either way `catch (Throwable)` swallows it, `claimDelegateClose()` returns false in `close()`, and `close()` returns with NO exception -- shutdown reports success while the lease survives to its TTL. ⚠️ M4.7's ROUND-7 FIX WIDENED THIS WINDOW rather than creating it: callers are now freed BEFORE the release runs, so the caller just handed "the commit batcher terminated abnormally" is the natural trigger for the close that lands inside it. Before that reordering no QUEUED caller was awake during the release -- the IN-FLIGHT batch already was, because `drainLoop` fails the batch it holds before it ever returns, which is earlier than the `finally` whose order round 7 changed. ⚠️ ALSO CORRECT THE COMMENT, which says `LocalSequencer.close` "reaches `BoundedLock.takeOrFail`" flatly: `release()` tries `lock.tryLock()` first and only falls through under contention, a qualification `close()`'s own comment already carries. ⚠️ AND `baselines/review.txt` INHERITED THE FLAT VERSION, not the qualified one -- it says `leases.release()` reaches `BoundedLock.takeOrFail`, whose `tryLock(bound, NANOSECONDS)` throws immediately, with no mention of the uncontended fast path -- so it needs the same correction. An earlier draft of THIS row got that backwards, in the sentence whose whole job is retiring an overstatement. Found by M4.7's round-8 production review | FR-11, NFR-9 | todo |

### M4.36

| M4.36 | The join-timeout WARNING promises it is "closing the delegate with a commit possibly still in flight", but that close is CONDITIONAL on `claimDelegateClose()` -- when the committer died, claimed the close and is stuck in a slow `release`, the message is logged and NOTHING is closed. ⚠️ The same message is also emitted when `join()` was INTERRUPTED rather than timing out, because the catch leaves `stopped == false`, and there "within 10s" is simply false. ⚠️ TWO DEFECTS, ONE COMMIT, AND THE REASON IS PROXIMITY RATHER THAN A SHARED CAUSE -- an earlier draft of this row claimed the shared cause was `Duration.ofSeconds(10)` being a literal rather than a seam, and BOTH round-1 reviewers independently showed that is wrong: injecting the timeout fixes NEITHER message, since one is wrong because the close is CAS-gated and the other because the interrupt catch leaves `stopped` false. They are one row because they are two branches of the same eight lines, not because one change fixes both. ⚠️ AND DO NOT BUILD THE SEAM FOR THE TEST: the same draft said `close()`'s own drain net cannot be pinned without waiting ten real seconds, which contradicted the sentence before it. The interrupted-`join` route reaches the identical `!stopped` branch in MILLISECONDS -- pin the committer inside `commitAll`, queue a second caller, and have a helper interrupt the closing thread. ⚠️ THE PIN MUST BE INTERRUPT-PROOF, and both round-2 reviewers measured that getting this wrong pins NOTHING: `close()` fires `committer.interrupt()` BEFORE it joins, so a plain `CountDownLatch.await()` is broken by that very interrupt, the committer runs its own `failAll(drainRemaining(), ...)` net and frees the queued caller, and the mutation survives the new test. Measured both ways -- `CountDownLatch.await()`: mutation SURVIVES; `Semaphore.acquireUninterruptibly()`: mutation KILLED. ⚠️ AND DO NOT TAKE `aQUEUEDCallerIsFailedWithoutWaitingForTheLeaseReleaseToFinish` AS THE PRECEDENT. It does spin rather than wait, but its reason is batch-membership determinism, nothing interrupts its committer, and a latch would serve there. The mechanism matches; the motive does not -- so the net is pinnable today with no production change. ⚠️ ONE HONEST CAVEAT, recorded by the round-1 test review: nothing marks "the closer entered `join`", so an interrupt landing before `Thread.interrupted()` degrades that test to the ten-second path -- slow, never wrong. ⚠️ Deleting `failAll(drainRemaining(), terminalFailure());` from `close()` while leaving its twin in `drainForever`'s finally leaves the whole suite green -- reproduced independently by both round-1 reviewers. Found by M4.7's round-7 and round-8 reviews | FR-11 | todo |

### M4.37

| M4.37 | TWO MEASURED SURVIVING MUTATIONS against changes M4.7 itself introduced, both leaving `./gradlew test` green across every module. ⚠️ (a) `terminalFailure()` is pinned as a TYPE and never as a MESSAGE: replacing its whole body with `new IOException("this sequencer is closed")` -- dropping the abnormal-death wording AND the cause chain -- survives. Two tests reach it and neither constrains it; `aCommitterThatDIEDDoesNotTELLTheNextCallerItWasCLOSED` does NOT cover it, because `submit()` builds its message inline and never calls `terminalFailure()`. The production consequence is verbatim the major round 6 raised on the sibling path: a pod whose committer died on an OutOfMemoryError tells every caller QUEUED BEHIND it "this sequencer is closed", causeless, when nobody called close. Remedy is one line -- `.hasMessageContaining("terminated")` plus a root-cause assertion. ⚠️ (b) The `catch (Exception)` -> `catch (Throwable)` widening is unpinned: reverting it survives, because every delegate `close()` in the suite does nothing, sets a flag, or throws IOException, and none throws an Error. A delegate throwing `new OutOfMemoryError("release")` would pin it. ⚠️ ALSO FIX A COMMENT OF MINE that overstates its own margin: `theRealClockTimerActuallyWaitsItsInterval` claims "~400 ms of slack", true of the CEILING and not of the FLOOR. The 200ms-constant mutation LANDS at ~202-211ms against a 300ms floor across three independent runs, so the floor margin is ~90-100ms -- the disputed "~400ms" is four times the real figure, not twice it. ⚠️ THAT IS THE MUTANT'S ELAPSED TIME, NOT THE MARGIN, and two drafts of this row conflated them. The bound stands; the number in the prose does not. Found by M4.7's round-8 reviews | FR-11 | todo |

### M4.39

| M4.39 | ADR-0027 accepts a case where `renew()` returns EMPTY although this node was never superseded: an ambiguous `putIfMatch` leaves a write queued, the next attempt's one-shot `refreshed()` reads the pre-write lease and adopts its version, the queued write lands, and the CAS then loses to the node's OWN bytes. The ADR is emphatic that the BOUND is what is being accepted -- "the honest quantity is exactly the remainder of the TTL, unshortenable -- which is a materially different thing to accept than 'up to'" -- and that bound holds only because `acquireLocked` can retake the lease at expiry. ⚠️ M4.17 BROKE THE BOUND WITHOUT AMENDING THE ADR. `fenced` is a one-way latch with no reset, and nothing re-runs `start()`: the supervisor tick is M5's, and `DefaultIngest` holds one `Sequencer` for its whole lifetime. So one client-side renew timeout with the write still queued self-fences the pod permanently, every bulk request thereafter fails, and under S=1 cluster ingestion STOPS until restart -- where the ADR costed the same event at one TTL. `close()` cannot even hand the lease back, since `belief` is already null and `releaseLocked` returns at its guard, so a successor waits the full TTL too. ⚠️ NOT HYPOTHETICAL: `LeaseManagerAmbiguityTest.aWriteThatLandsAfterTheRefreshReadStillSelfFencesOnce` pins the interleaving one layer down, with `DelayedLandingStore`. ⚠️ WHY M4.17 SHIPPED IT ANYWAY: fencing on empty is not optional -- `renew`'s contract says an empty result is how a holder learns it is fenced and that it must stop immediately, and from inside the two cases are indistinguishable. Not fencing risks I2, which is worse. And bounding it is not available to `LocalSequencer`: after a false empty `belief` is null, so every later renew returns empty too, and `log` and `epoch` are final, so a new term needs `tryAcquire` -- `start`'s acquire-seal-open, which is M5's supervisor tick. ⚠️ BUT A CHEAPER FIX EXISTS ONE LAYER DOWN, and this row must not route a session past it: `LeaseManager` CAN tell a false empty from a true one, with one `stat` plus the `isOwnTerm` check it already has, on the fence path only -- so no rate scales. That is ADR-0027's own second rejected alternative, and it was rejected "against a saving of one bounded failover", which is exactly the term of trade M4.17 changed. Weigh that BEFORE building a supervisor tick. Found by M4.17's round-2 production review. ⚠️ SO THE OBLIGATION IS M5's, and this row exists so it is written down where M5 is planned rather than living in one commit body. The ADR-0027 successor should carry the amended quantity: with a supervisor tick, one TTL; without one, the process lifetime. Found by M4.17's round-1 production review | FR-11, NFR-9 | todo |

### M4.40

| M4.40 | `close()` calls `renewer.interrupt()` and the renew loop re-checks `closed` after each tick. EITHER ALONE masks the other -- the interrupt covers a renewer parked on the tick, the check covers one already past it -- so a mutation deleting just one leaves the suite green, and only deleting BOTH reaches a distinguishable state. ⚠️ THAT DOUBLE MUTATION IS NOW PINNED by `aCallerAfterCLOSEIsToldCLOSEDRatherThanFENCED`, deterministically and in ~0.06s, so the earlier claim in this row that its red "cannot be reproduced on demand" was WRONG -- it was a property of the three observables tried, not of the race. What made the fourth work: `fenced` is written BEFORE the renew thread terminates, so joining that thread is a happens-before edge, and a package-private `awaitRenewerStopped` turns a three-second poll into an exact wait. The three dead ends are recorded so nobody re-walks them: a resurrected lease never happens (`releaseLocked` nulls the belief, so a late renew returns empty rather than writing); a fenced message on the FIRST refusal is a race, since `closed` is set synchronously and `fenced` arrives on the renewer's thread; and a fenced message on every refusal across a three-second poll failed under an instrumented build and passed under two clean ones. ⚠️ WHAT REMAINS UNPINNED IS THE ORDER, which is this row. `close()` interrupts BEFORE it releases. Reversed -- `leases.release(); renewer.interrupt();` -- a renewer that is past its `closed` check in that window reaches `renewLocked` with a null belief, latches `fenced`, and logs a "must be restarted" ERROR for what was a graceful shutdown. ⚠️ A RACE WINDOW, NOT EVERY SHUTDOWN, and an earlier draft of this row said every: the reversed order is green today, measured. No test constrains it, because no seam exists between the post-tick `closed` check and `leases.renew()` at which a fixture could interleave a `release()`. ⚠️ A METHOD WARNING WORTH MORE THAN THE ROW: several intermediate conclusions here came from `./gradlew --tests X` printing BUILD SUCCESSFUL for a task Gradle had skipped as up-to-date -- a three-second test "passing" in 941ms. `tdd-red.sh` reads the result XML and had already refused to record the test twice, which was the true signal and was disbelieved. Use `--rerun-tasks`, or read the XML. Found by M4.17's round-1 and round-2 test reviews | FR-11 | todo |

### M4.41

| M4.41 | `thePUBLICStartWIRESTheConfiguredIntervalIntoTheTickerItInstalls` bounds the renew rate at `isBetween(4L, 30L)` over a 1000ms window at a 150ms interval. ⚠️ THE 2x MUTANT IS BIMODAL AT 3/4, because 1000/300 is 3.33 rather than 3, so it lands ON the inclusive floor about half the time: measured killed 2 of 5 runs, while honest never dropped below 6 across seven runs including load average 123. The detector therefore fires ~40% of the time for `multipliedBy(2)`, and the surviving band still contains the case that matters. ⚠️ THE BAND IS THE TEST COMMENT'S 0.56x-2.22x, derived there; an earlier draft of this row said 0.75x, which is not derivable and not expressible as a `multipliedBy`/`dividedBy` mutation at all -- `LeaseConfig` refuses only `renewInterval >= ttl`, so a 9s/10s configuration doubled to 18s is M4.17's own defect reinstated. ⚠️ THE SECOND SEQUENCER DOES NOT SEPARATE THIS CASE: under the 2x mutant `slowRate` is 0 in every run, which satisfies its `isLessThan(3L)` bound. Bounding `slowRate` BELOW also kills it but leaves one unit of margin -- the same shape as the bug. ⚠️ THE REMEDY IS NOT A DROP-IN, and an earlier draft of this row said it was -- in the row whose subject is a number reported without being measured. It gave "honest ~13, 2x mutant ~6" as measured when both were the 1000ms figures DOUBLED. Measured at a 2000ms window, `--rerun-tasks`, read from the result XML: honest `fastRate=13 slowRate=3` (4 of 4 runs), 2x mutant `fastRate=7 slowRate=1` (3 of 3) in one tree and `fastRate=6,7,7` in another. ⚠️ SO THE MUTANT IS 6 OR 7, BIMODAL AT 2000ms TOO -- 2000/300 is 6.67, the same arithmetic that makes it 3/4 at 1000ms -- and an earlier draft of THIS sentence reported "7, not 6" from three runs in one tree, reproducing the row's own subject inside the correction written for it. A floor of 9 clears both modes. ⚠️ BUT WIDENING THE WINDOW BREAKS THE SECOND SEQUENCER'S BOUND: at 2000ms against its 600ms interval the honest `slowRate` is 3.33, so the existing `isLessThan(3L)` goes RED on a correct tree -- measured, `Expecting actual: 3L to be less than: 3L`. Any fix must move that bound in the same change, and the row that identified `slowRate` as the second detector's only constraint is the one that missed it. ⚠️ TWO COMMITTED CLAIMS MUST GO WITH THE FIX. The test comment's "`multipliedBy(2)` measures EXACTLY 3" is simply false. `baselines/review.txt`'s "The floor is 4 now" is literally TRUE of the code -- the bound is `isBetween(4L, 30L)` -- so a fix session grepping for a wrong string will not find one; what is unsupported is what it carries in context, since the preceding clause says round 3 measured the mutant surviving an inclusive floor of 3, which makes it read as "and therefore it is killed". It is not, about 60% of the time. Both came from reading ONE run and calling it a measurement, which is the same error M4.17's own history records twice more. Found by M4.17's round-4 test review | FR-11, NFR-9 | todo |

### M4.42

| M4.42 | M4.17's round-3 review found "the old leader keeps committing into a chain a successor has already sealed" to be false: once a successor has SEALED, `CommitLog` refuses -- the `sealedAt` check, `throw fenced(seal)`, and consecutive slots together mean a fenced leader cannot write past the seal. The real window is after the successor takes the LEASE and BEFORE its seal lands, which is the gap nothing else covers. ⚠️ THE CORRECTION WAS APPLIED TO ONE COPY ONLY. `baselines/review.txt`'s `rounds:M4.17` entry now states the window correctly; the identical pre-correction sentence still stands in `LocalSequencerRenewTest`'s fenced-test comment -- in the SAME FILE that elsewhere explains a first draft passed against a renewer-less tree for exactly the reason the sentence denies. ⚠️ AND THERE IS A THIRD COPY, IN PRODUCTION, which an earlier draft of this row missed while asserting there were only two: `LocalSequencer`'s empty-means-fenced branch -- the one that latches the fence -- says "Another node holds the term now, and it has sealed this chain; committing on would reassign offsets a successor has already issued, which is I2." That is the corrected claim intact, and the SAME comment block contradicts itself five lines later with "NOT NECESSARILY A TAKEOVER, and an earlier draft of this line said it was". A session that fixes only the test closes this row with the production comment still false. ⚠️ ONE COPY OF A FACT FIXED AND ITS DUPLICATE MISSED is the failure mode `backlog-done.md` already records against M4.0 across four rounds, reached again. ⚠️ ALSO FIX "strictly WORSE than no tick" in the same cap argument: with no tick the holder is equally ignorant and commits in the same acquire-to-seal window, and a working tick makes that window RARER. "No better than no tick, while shipping a renewer that ignores a signal its own contract calls mandatory" carries the do-not-split conclusion without overstating. Found by M4.17's round-4 production review | FR-11 | todo |

### M4.44

| M4.44 | A COMMENT DESCRIBES A COST THE TREE DOES NOT PAY. `tickAndAwaitProcessed`'s `parked.drainPermits()` carries a justification saying that without it the fence-on-IOException mutation dies at `awaitExpiryPast`'s twenty-second deadline rather than at the `commit` that names the defect. ⚠️ BOTH ROLES MEASURED THAT WRONG, independently: the mutant dies at the rendezvous's OWN assertion -- "the renewer processed the tick and came back round" -- at ~5s, because fencing terminates the renewer so `parked` is never released again and the `commit` never runs. Deleting the drain changes nothing observable. ⚠️ AND ITS PREMISE IS UNREACHABLE AS STAGED: `tickAndAwaitProcessed` has exactly one call site, and no `ticker.tick()` precedes it in that test, so the drain can only ever drain zero permits. ⚠️ THE DRAIN ITSELF IS FINE as defence against a future second call site -- what is wrong is the sentence explaining what it buys, which is the third distinct comment in this milestone stating a measurement nobody ran. Either delete the justification and keep the drain, or add the second call site that would make the premise real. ⚠️ THIS ROW EXISTS BECAUSE M4.43 NEARLY LOST IT: that task filed "the two minors" when `70947f0` recorded FOUR, and this one was in neither row -- surviving only in a commit body nothing loads, which is the exact outcome the filing exists to prevent. Found by M4.17's round-4 reviews and by M4.43's round-1 production review | FR-11 | todo |

### M4.8a

| M4.8a | The checkpoint record: per-stream `nextOffset` and `oldestRetainedOffset`, per-pod `lastAppliedFlushSeq`, with encode/decode, golden files, the fakes, and an ADR for the format -- the whole set the `wire-format-change` skill requires in ONE commit, on the ground that a format split across commits leaves the tree able to write bytes it cannot read. ⚠️ NO WRITER AND NO TRIGGER, which is what makes this separable at all: the record is a value type plus its codec, and M4.8b2 is what decides when one gets written. The precedent is M4.1, which shipped the `Sequencer` seam and its fake with no implementation behind it. ⚠️ DELIBERATELY NOT THE OFFSET->SEGMENT INDEX. That stays in the deltas, and it is the whole reason a checkpoint is ~38 KiB at 1,600 streams rather than growing with history -- a checkpoint that inlined the index would reintroduce the unbounded read M4.9 exists to remove. ⚠️ `oldestRetainedOffset` is carried from the first commit even though nothing consumes it before M7, for the same reason `CommitRequest` carried `(podId, flushSeq)` from M4.1: adding it later is a format change, and this row is the one commit where a format change is free. ⚠️ ROUND-TRIP PLUS GOLDEN FILES IS NOT ENOUGH, and two mutations survive that pairing as naively written. (a) DELETE THE ENCODER'S ORDERING: the record is two maps and a small golden can re-encode correctly by luck. ⚠️ THE MEASURED HISTORY, STATED THE RIGHT WAY ROUND -- an earlier draft of this row gave its reciprocal: M4.1 records that at THREE streams a broken sort still produced the asserted order about 1 run in 6, i.e. it was killed roughly 5 runs in 6 and SURVIVED 1. Raised to six streams, 8 of 8 fresh JVMs failed. A detector that misses 1 run in 6 is the problem; it is not that the mutation usually passes. ⚠️ THE ENTRY-COUNT IS A PROXY, AND AN EARLIER DRAFT OF THIS ROW GOT THE MECHANISM WRONG: it said `Map.copyOf`/`HashMap` iteration order is salt-randomised per JVM, which is true of `ImmutableCollections` and FALSE of `HashMap`. Under a plain `HashMap` whose iteration order happens to coincide with the encoded order, deleting the sort survives 100% of runs rather than 1 in 720. So prescribe the PROPERTY -- the fixture's observed iteration order must differ from the encoded order -- or require `Map.copyOf` as `CommitRequest` does, and treat the entry count as a floor on top of that.  (b) SWAP `nextOffset` AND `oldestRetainedOffset` CONSISTENTLY in encode and decode: round-trip is green BY CONSTRUCTION, and golden byte-identity catches it only if the fixture gives the two fields distinct non-default values -- and `oldestRetainedOffset` is exactly the field nothing consumes before M7, i.e. the one a fixture leaves at 0. ⚠️ SO THE PRESCRIPTION IS: `GoldenChainEntryTest`'s shape (hand-verified decoded VALUES first, byte-identical re-encode second), a stated total order on entries, a fixture of at least 6 streams and 2 pods, and distinct non-default values in every field. ⚠️ THE ~38 KiB CLAIM NEEDS AN ASSERTION or it is decoration: with no writer, a size bound at 1,600 streams is the only constructible expression of "the index stays in the deltas" -- and it needs a MAGNITUDE, because the magnitude is the whole discriminator. Inlining the offset->segment index and asserting merely "< 1 MiB" is green; the bound must be close to the ~38 KiB the row claims, which is the same defect the sibling row spends two clauses closing when it forbids a bound stated in pages | FR-11 | todo |

### M4.8b

| M4.8b | ⚠️ SPLIT into M4.8b1 (recovery ignores non-delta keys) and M4.8b2 (the writer and its trigger), on the seam that the writer needs the filter and the filter does not need the writer. ⚠️ THE ORDER IS A HARD DEPENDENCY, not a preference, and an earlier draft of this split dropped the clause that says so: with K and T defaulting large enough that no current test crosses the trigger, landing b2 FIRST is green in CI and then breaks every takeover in production forever -- the first checkpoint written makes `recoverChainEnd` throw and `LocalSequencer.start` release the lease, and the next node repeats. Both sub-rows now carry that dependency; do not rely on this banner, because a session picking one task is told not to read the others. ⚠️ THE SOURCE TEXT IS NOT PRESERVED HERE, and this row no longer claims it is. What replaced it is the two sub-rows plus the findings of the split's own review, which are longer and more specific than the original | FR-11 | split |

### M4.8b1

| M4.8b1 | `ChainReplay.chainEnd` lists `CommitLog.logPrefix()` and takes the LAST key; `applyChain` lists the same prefix and decodes keys from it. `BinStore.list` is flat and undelimited and `ckpt/...` sorts after every `<seq:016x>.delta`, so the first checkpoint under that prefix becomes what a takeover reads as the chain end -- it fails to decode, `start` releases the lease, and the next node repeats. ⚠️ MUST LAND BEFORE M4.8b2. Landing the writer first is green in CI and permanently broken in production. ⚠️ `applyChain` DOES NOT DECODE EVERY KEY, and an earlier draft of this row said it did -- it returns at `hop.upTo` and at the first `Seal`. That is not pedantry, it INVALIDATES the obvious test: a takeover seals its predecessor and crosses with `upTo = seal.sequence()`, and the seal's key sorts BEFORE `ckpt/`, so a takeover-with-a-seeded-checkpoint test returns at the seal and never reaches a checkpoint at that site -- a `chainEnd`-only fix passes it green, which is this row's own warning realised by its own criterion. ⚠️ THE SIMPLE SHAPE REACHES `applyChain`: `replayAncestry` adds `hops[0] = (epoch, Long.MAX_VALUE)` UNCONDITIONALLY, before any ancestry question, so a plain `CommitLog.recover()` over an UNSEALED chain reaches the site with no ancestry at all -- `CommitLogTest`'s undecodable-entry test is already that fixture. ⚠️ PREFER IT TO THE THREE-EPOCH WALK. The ancestry route (a leader at 3 with checkpoints, a node acquires 4 and dies before sealing 3, the next seals 4 at slot 0 and hops back to 3 unbounded) also reaches it, but is the FRAGILE one: seal epoch 3 by accident and the hop becomes bounded, `applyChain` returns at the `Seal`, and a `chainEnd`-only fix passes green -- the exact trap this clause exists to defuse. ⚠️ AND `chainEnd` NEEDS ITS OWN COVERAGE, which an earlier draft of this row DELETED while fixing the `applyChain` criterion: in the three-epoch shape `recoverChainEnd` runs against EMPTY epoch 4, so it issues no GET and a fix applied to `applyChain` alone is green while the failure this row opens with ships. ⚠️ ONE FIXTURE DOES NOT COVER BOTH, and a draft of this row claimed it did. In a takeover, `LocalSequencer` UNCONDITIONALLY seals the inheritable ancestor before `open` crosses into it, and the seal is written at a `<seq:016x>.delta` key that sorts BEFORE `ckpt/` -- so `applyChain` returns at that `Seal` and never reaches a checkpoint. The takeover that gives `chainEnd` a fixture is precisely what destroys `applyChain`'s, guaranteed rather than accidentally. ⚠️ SO DRIVE THE TWO SITES SEPARATELY AGAINST ONE UNSEALED CHECKPOINT-BEARING CHAIN: `recover()` for `applyChain`, `recoverChainEnd()` for `chainEnd`. No ancestry walk is needed for either. ⚠️ SEED MORE THAN ONE KEY, AND NOT ONLY AT THE END. One checkpoint-shaped key at the last position leaves three mutations alive, measured: a deny-list `!endsWith(".ckpt")` where the allow-list is what is wanted, a `contains(".delta")` predicate, and `return` instead of `continue` -- which at `applyChain` TRUNCATES the chain and drops offsets. One extra key kills the two predicate mutants, and the third needs its own assertion -- a `return` instead of `continue` silently TRUNCATES the chain, which a no-throw plus constant-GET test cannot see, so assert the recovered offsets and `nextSequence` match the same chain with no interposed key. The key to add is: a non-delta key that sorts BETWEEN two deltas and whose name CONTAINS `.delta`, such as `<seq:016x>.delta.tmp`. ⚠️ ITS BODY MUST NOT DECODE AS A `ChainEntry`, which the remedy as first stated omitted: seed valid delta bytes and both the deny-list and the `contains` mutants survive, because `fold`'s `Math::max` yields an identical offsets map either way. ⚠️ THE FILTER IS A KEY ALLOW-LIST, NEVER A CAUGHT DECODE FAILURE, and `CommitLogTest`'s `recoveryStopsOnAnEntryItCannotUnderstandRatherThanSkippingIt` is the test that says so -- its own comment records that `catch (IOException) { continue; }` in this loop leaves a reader applying a discarded suffix, which is I3. That test must still pass unchanged. ⚠️ THE DIMENSION IS GETs, NOT LISTs. A LIST-count assertion is invariant under the fix and passes with no filter at all. Assert the GET count is CONSTANT in the number of checkpoint-shaped keys seeded, measured at 1 and at N, which is what kills a fix that GETs the key and swallows the decode failure. ⚠️ THERE IS A THIRD DECODE SITE, in the fixtures rather than production: `Invariants.readChain` lists `logPrefix()` and decodes every key, and `CommitProtocolSimulation.hasChain` treats any key under the prefix as proof the chain exists. When b2's writer lands the tempting repair is `catch (IOException) { continue; }`, which blinds the simulation's invariant checker to genuinely corrupt bytes -- the inversion `check-test-integrity.sh` exists to catch. ⚠️ THE COST CONSEQUENCE, STATED HONESTLY BECAUSE TWO EARLIER DRAFTS GOT IT WRONG IN OPPOSITE DIRECTIONS: one claimed a relocated key had no LIST growth, and its correction claimed this side's growth is a factor of about 1 + 1/K. Both are wrong. The `1 + 1/K` figure ignores the T trigger entirely: with `d` deltas per T-period the ratio is `1/d`, up to one checkpoint per delta -- a factor of 2 -- and an unconditional tick grows the key count at `1/T` with UPTIME, independent of commit volume, which is a new dimension rather than a factor. The honest statement separates two things a third draft conflated: the KEY COUNT is bounded only by M7's GC, since nothing deletes checkpoints before it; what AC5 bounds is recovery's LIST EXPOSURE to that count, by requiring zero `list` once M4.8c and M4.9 land. The R15 / M5-fan-out consequence of the exposure is carried by M4.9's row. ⚠️ THE ALTERNATIVE WAS A KEY OUTSIDE THE PREFIX, rejected because the M4 SPEC states the `ctl/log/<slot>/<epoch:016x>/ckpt/...` grammar NORMATIVELY and changing it means amending the SPEC in the same commit under sdd.md rule 8. Choosing a remedy here is not the pre-adoption the source row refused -- that was about M4.8c's DISCOVERY mechanism, and a task with no chosen remedy has no checkable criteria | FR-11 | todo |

### M4.8b2

| M4.8b2 | Write a checkpoint every K deltas or T seconds, both configuration, at `ctl/log/<slot>/<epoch:016x>/ckpt/<seq:016x>.ckpt`. ⚠️ MUST NOT LAND BEFORE M4.8b1: without the recovery filter this is green in CI and breaks every takeover in production, permanently. ⚠️ NEEDS NEW STATE -- per-pod `lastAppliedFlushSeq`, which `CommitLog` does not track; its javadoc says the log never reads `podId` or `flushSeq`. ⚠️ THAT STATE IS PROCESS-LOCAL AND THAT IS THE HAZARD: `CommitDelta` carries `(sequence, segments)` and nothing else, so a new leader starts with an empty map and the FIRST checkpoint after a takeover carries `pods = {}` -- emptying the idempotency window M4.10 depends on exactly when duplicates are likeliest. A test must assert what the first post-takeover checkpoint carries, not merely that the map round-trips. And `put` in place of a `Math::max` fold survives any workload whose flushSeqs only rise, so the fold needs a workload where they do not. ⚠️ SOMETHING MUST ASSERT WHAT WAS WRITTEN: a constant or stale body, and a constant `seq` overwriting one key, pass the counts, the boundary, the idle case and a grammar test aimed at the formatter, because `CountingBinStore` counts PUTs and not distinct keys. Decode what was PUT and assert per-stream and per-pod values against a workload where they differ -- including `oldestRetainedOffset`. ⚠️ FOR THE WRITER, 0 IS THE TRUTHFUL VALUE until M7 retention exists, and ADR-0033's "the field a careless fixture leaves at 0" is about the GOLDEN fixture, not about what a checkpoint should carry. So do not invent a non-zero one to make a test discriminate; what must not survive is `StreamOffsets(next, next)` -- and the assertion that kills it is that `oldestRetainedOffset` IS ZERO for every stream, which is both exact and true of the writer until M7. ⚠️ "THE TWO FIELDS DIFFER" WAS A WORSE CRITERION THAN THE ONE IT REPLACED, and a draft of this row used it: `StreamOffsets(next, next - 1)` survives it, and it fails LEGITIMATELY for a stream sitting at offset 0, where both fields are 0 and the checkpoint is correct. ⚠️ `seq` IS UNDER-SPECIFIED BY "DISTINCT AND STRICTLY INCREASING", which three derivations satisfy while being wrong: a per-checkpoint counter, which leaves M4.9 unable to bound replay from the key; an off-by-one between last-included and `nextSequence`; and a constant body `Checkpoint.sequence` while keys increment. ⚠️ AND THE PHRASING BIASES TOWARD THE COUNTER: if `seq` is the chain sequence and the second trigger is a T tick with no deltas between, a CORRECT implementation writes the same key twice. ⚠️ CONDITION THE CRITERION, DO NOT DELETE IT -- an earlier draft dropped "distinct and strictly increasing" outright and left `seq = 0` passing every other assertion here, including "decode what was PUT". The correct form is conditional: strictly increasing across two triggers WITH DELTAS BETWEEN, and the SAME key for a T tick with no deltas between. Decide which `seq` is and say so. ⚠️ AND ASSERT THE BODY'S `sequence` AGAINST THE KEY'S, because a constant `Checkpoint.sequence` with correctly incrementing keys survives every other assertion here -- it is the third of the three wrong `seq` derivations and the only one the conditional criterion above does not reach. ⚠️ ZERO-PADDED HEX IS LOAD-BEARING -- lexicographic order IS numeric order, the only ordering available since ADR-0022 removed `lastModifiedMillis` -- and count cannot see it: `Long.toHexString(seq)` leaves every count identical while `10.ckpt` sorts before `9.ckpt`. A SECOND assertion carries the grammar: exact key strings for a seq pair whose padded and unpadded orders differ (9 and 16), as an ORDERED PAIR, plus an epoch pair. ⚠️ ASSERTED BY REQUEST COUNT, not by the object appearing. PUTs scale with deltas/K and seconds/T, NEVER with streams or pods -- and nothing in the count assertions catches a PUT-per-stream mutation, so that needs its own test with the stream count AND the pod count varied and the PUT count held -- a per-pod PUT survives a single-pod stream-varied test. ⚠️ BOTH TRIGGERS AND BOTH BOUNDARIES. Sample AT the boundary for each: zero PUTs after K-1 deltas and exactly one after the Kth with the clock frozen; zero just before the first tick and one just after, plus a two-sided count over several periods. Count alone misses `K -> K-1` whenever `floor(N/K) == floor(N/(K-1))`, and a one-sided T bound misses `T.multipliedBy(2)`, which `baselines/review.txt` records surviving twice. ⚠️ THE IDLE TEST IS TIME-DRIVEN, not request-driven like `BatchingSequencer`'s window, so construct-commit-nothing-assert-zero leaves fire-every-T-unconditionally and never-scheduled both alive. Advance an INJECTED clock past at least 2T with zero deltas and rendezvous on the tick being PROCESSED. ⚠️ AND A ONCE-ACTIVE-THEN-QUIET CLUSTER FALLS BETWEEN that test and "seconds/T": a dirty flag that is never cleared survives both, so assert that a cluster which commits and then stops eventually stops writing checkpoints. ⚠️ FAILURE PATHS: `K = 0` and `T = 0` must be REFUSED at construction -- an earlier draft only stated their consequence -- and the checkpoint PUT itself needs a runtime failure path whose ASSERTION is named: swallowing the IOException and wedging the writer forever passes any test that only checks the commit still succeeded, so the assertion is that a LATER trigger still writes | FR-11 | todo |

### M4.8c

| M4.8c | Find the newest checkpoint issuing ZERO `list` calls, and record the choice in an ADR. ⚠️ THE SPEC STATES THE BOUND AND REFUSES THE MECHANISM, deliberately: three of M4.0's review rounds each found a defect in a discovery mechanism the spec had specified -- a circular cursor, then a page-count bound that 50 checkpoints fit inside, then a mechanism deleted from the SPEC but left standing in the backlog, which is what actually gets built. Round 3's diagnosis is recorded in the SPEC itself: it was designing a mechanism it need not contain. This row is where the mechanism is chosen. ⚠️ CANDIDATES THE REVIEWS SURFACED, none pre-adopted: a pointer in the lease object, which is already read at recovery but puts a SECOND WRITER on the one CAS object whose lost race IS the fencing signal; a separate `LATEST` pointer written with `putIfMatch`; or deriving the cursor from the chain head the recovering process already holds. ⚠️ THE WORKLOAD MUST EXCEED 5,000 CHECKPOINTS, not 50 and not a page count. A bound stated in PAGES, or a workload fitting inside one 1,000-key page, stays green against the naive `list`-and-take-last -- the exact defect rounds 2 and 3 both caught, twice. ⚠️ BUT >5,000 IS NECESSARY, NOT SUFFICIENT, and the stated ground is slightly overstated: against a STRICT zero-LIST assertion `list`-and-take-last dies at five checkpoints, not five thousand. What the large workload actually buys is defence against the bound reverting to a PAGE COUNT, plus a second sample point. ⚠️ ZERO-LIST CONSTRAINS ONE VERB, and an implementation this row itself lists passes it while being wrong: derive the cursor from the chain head, implemented as a BACKWARD WALK -- zero LISTs and about 5,000 GETs. `stat`-probing the seq space is a second survivor. So the discriminator is TOTAL REQUESTS, ALL VERBS, CONSTANT IN CHECKPOINT COUNT: measure at 500 and at 5,000 and assert equal, or assert at most a small constant. ⚠️ AC5 PAIRS "no list" WITH "at most K delta GETs", but that half belongs to M4.9 -- this row ships and tests discovery alone, with no GET bound, so the constant-total assertion is what carries it here. ⚠️ THE WORKLOAD NEEDS A TIER AND A SEEDING SHAPE or it gets quietly cut back to the 50 this row forbids: at ~38 KiB each, 5,000 real checkpoints is ~190 MiB of fixture. Prescribe T1 and key-only seeding -- discovery reads the newest, so the others need to exist as KEYS, not as bodies | FR-11 | todo |

### M0.75

| M0.75 | `scripts/review-tree.sh` materialises a tree per reviewer and M0.74 shares their Gradle home, but NOTHING REAPS THE PROCESSES a reviewer leaves behind. Measured: 202 orphaned `zsh` processes from a completed test-reviewer tree -- 201 after one was killed individually as a probe, which is why both numbers appear in this session's record -- parent reparented to `/init`, state `RN` at ~9.5% CPU each, alive two hours after their agent finished, holding a 20-core machine at load average 206. ⚠️ THE COST IS NOT CPU, IT IS EVIDENCE. Reviewers in that window reported measuring at load averages 123 and 186 -- both verbatim in recorded verdicts under `.harness/review/` -- and treated them as ambient machine conditions; they were this leak. `~206` is ALSO a reviewer's recorded figure -- a production verdict says "load average ~206" verbatim, and the three runs taken under it are one of the two samples behind M4.41's floor-of-9 conclusion. An earlier draft of this row reattributed it to this session's own `uptime`, which overshot the fix it was making. Several accepted findings rest on timing margins measured under it -- the renew-rate floor, honest 6-7/s values, and judgements that a bound "cannot flake on a loaded machine". ⚠️ THE DIRECTION IS NOT SAFE, and an earlier draft of this row claimed it was on the ground that `Thread.sleep` overruns rather than undershoots so an honest rate under load is a floor. THAT REASONING IS WRONG HERE, because the measurement WINDOW is itself a `Thread.sleep`: overrun lowers the count through the renewer's 150ms sleeps and RAISES it through the 1000ms window, so the premise fixes neither the sign nor the size. On the recorded data the LOADED runs are the high ones -- 6 idle, 6 under 32 spinners, 7 at load average 123 -- and the committed slack quotes the loaded value: `a floor of 4 keeps ~1.75x slack` is 7/4, where the idle sample gives 1.5. ⚠️ AND THE CASE THE ARGUMENT CANNOT COVER AT ALL IS MUTANT SURVIVAL, which is the whole of M4.41: a stretched window only ever ADDS a tick, which is how a 2x mutant at 1000/300 = 3.33 reaches 4 and clears an inclusive floor of 4. This row does NOT claim those 4s were artifacts -- phase alone gives about a one-in-three chance of 4 -- it records that the question is OPEN where the earlier draft said it need not be asked. ⚠️ TWO COMMITTED SITES CARRY THE LOAD-MEASURED FIGURE and both evade the obvious grep: `LocalSequencerRenewTest`'s slack comment, which wraps `load` and `average` across two lines so `grep 'load average'` over that file returns NOTHING and which says 124 where every verdict says 123; and the `rounds:M4.17` entry in `baselines/review.txt`. What is wrong is that a bound reported as holding at load average 123 read as a stress result when it was self-inflicted noise of unknown composition. The verbatim phrase recorded in a verdict is "cannot flake on a loaded machine". ⚠️ ALSO WORTH KNOWING FOR THE FIX: a batched `kill -9 <many pids>` had no effect on these while an individual `kill -9 <pid>` in a loop killed all 201, and SIGTERM was ignored entirely. ⚠️ NOT A SWEEP, and M0.74 must be cited correctly here because an earlier draft of this row inverted it: what M0.74 records as MEASURED destroying a live reviewer's tree mid-review is an earlier PATH-KEYED `rm -rf` draft, and an age-based sweep is the thing it REFUSES on the ground that it "puts it back". So age-based reaping is not a measured failure, it is the hazard M0.74 declines to reintroduce -- and the same argument carries from trees to processes. Reap what a KNOWN-FINISHED agent left, or report the count past a threshold as M0.74 does for trees | — | todo |

### M4.45

| M4.45 | `FakeSequencerTest`'s comment reads "At three keys a missing sort still produces the asserted order 1 run in 6, i.e. a broken sort would pass CI most times; round-2 review measured exactly that." ⚠️ THE TRAILING CLAUSE IS THE WORSE HALF, and an earlier draft of this row truncated the quotation before it: it attributes the INVERTED reading to a named review, so a session checking the claim is pointed at a measurement that says the opposite. The clause before the "i.e." matches M4.1's landed record; the one after is its reciprocal -- surviving 1 run in 6 means FAILING roughly 5 in 6. ⚠️ NO ASSERTION IS AFFECTED: the six-key fixture beside it and its 1-in-720 figure are consistent with the correct reading, so this is a false gloss on a sound test. ⚠️ IT IS THE COPY A SESSION HITS, though, because it is the one attached to the fixture rather than to a backlog row, and M4.8a's prescription now says the opposite in its own words. ⚠️ FOUND BY A REVIEWER GREPPING `*.java` WHERE MINE COVERED ONLY DOCS -- the same correct-one-copy-miss-the-duplicate failure the M4.8 split commit records twice, escaping a grep scoped to the file type the fix happened to be in | — | todo |

### M0.77

MEASURED 2026-09-06 over `*Test.java`/`*Tests.java`, case-insensitively, with the
withdrawn gate's pattern -- ⚠️ WHICH IS IN NO COMMITTED OBJECT, since that script
was withdrawn before it ever landed, so it is written out here or the number is
not reproducible:

```
(every|no) other (test|assertion)|the only (test|assertion|absolute)|every (test|assertion|cost assertion) (here|in this file)|(survived|survives|passed) all [0-9]+ tests
```

**61 occurrences in 36 files** once javadoc
`*` and `//` continuation markers are stripped and the text joined; a line-scoped
grep sees **50 in 31**, so **11 wrap across lines** and no line-oriented matcher
can see them.

⚠️ THIS ROW HAS CARRIED THREE WRONG FIGURES, which is why the method is quoted
beside the number: "~19 files", read off a truncated `head`; "51 in 32", which is
all `*.java` rather than the test globs the row describes; and "38 in 26", which is
the case-SENSITIVE count of a pattern the script applied with `grep -Ei`.

No gate enforces this -- see M0.76 for the measurement that withdrew one.

### M0.78

Five minors both reviewers accepted at M0.76 rather than blocked on.

**The gate.** Exemption is by ID across all 121 base rows, so a row can land
compliant and be grown past the cap in any later commit with the gate printing
`ok`. Three shapes still pass at 4,200 characters: `| M9.9B |` (uppercase
suffix), `` | **`M9.9`** | `` (four markers), and `> | M9.9 |` (blockquote).

**The tests.** Deleting EITHER fail-closed branch alone survives: both cases in
`anUnreadableDiffIsRefusedByNameRatherThanReportedAsClean` break the diff read
AND the base read, so whichever guard remains prints the same asserted tail.
Assert the message HEAD, and add a case where the diff reads but the base does
not (a `CHECK_RANGE` predating the archive). Replacing the diff with the
worktree file survives, because every test writes and stages in one call, so
ADDED-LINES-ONLY is unpinned. Deleting `judged += 1` survives, leaving the
success line permanently at "0 new archive row(s)" -- the invisible-failure
shape the gate's own header names about its predecessor. ⚠️ `judged` is unpinned
in BOTH directions: incrementing it for EXEMPT rows also survives, turning
`1 new archive row(s)` into `2` on a commit that edits one landed row and adds
one new one, exit 0 either way. A fix aimed at one direction leaves the other.

### M4.46

`SegmentPublisher` PUTs unconditionally, justified in place: "put, not
putIfAbsent: the key already contains a pod id and a per-pod sequence, so it is
unique WITHOUT coordination". That premise is false across incarnations.
`SegmentKey.timestampMillis` is the segment's `firstAppendMillis`, not a
publish stamp, and `SegmentPublisher.sequence` is an instance `AtomicLong` that
restarts at 0 -- so two incarnations of one pod can mint byte-identical keys.

MEASURED by M4.10b's review: 200/200 under a frozen clock. ⚠️ The 175/200 on
`Clock.systemUTC()` is an artifact of a fixture whose appends fall inside one
millisecond, NOT an 87% production rate. The production trigger is a clock that
does not advance across a restart.

⚠️ THE BROKEN INVARIANT IS NOT "DATA LOSS", which would invite a
retention-shaped fix. The chain still names the overwritten key in a
`SegmentCommit`, so the offsets already acknowledged for the first incarnation
now resolve to the second's records: a committed offset must resolve to the
records committed at it.

⚠️ NOT M4.10's. This is the segment path and it is independent of idempotency;
M4.10b only found it.

