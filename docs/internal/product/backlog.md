# Backlog

Open tasks only. Each row is one commit, and each row is a SUMMARY -- the
reasoning behind a long one is under its ID in
[backlog-notes.md](backlog-notes.md), opened for the task a session picks
rather than for all of them. Completed rows move to
[backlog-done.md](backlog-done.md), which nothing loads.
`scripts/check-session-load.sh` enforces all three.

IDs are stable and never reused. `scripts/check-commit-msg.sh` requires a
commit subject to start with an ID present here or in the archive.

**Current milestone: M4 — sequencer and commit log**, specified in
[milestones/M4/SPEC.md](milestones/M4/SPEC.md). M1, M2 and M3 are complete.

## M0 — harness

| ID | Task | Serves | State |
|---|---|---|---|
| M-1.2 | Harness machinery: `AGENTS.md`, skills, adapters, the gate scripts, pre-commit wiring, and the parser regression suite | — | todo |
| M0.8 | M1 spec written and reviewed | — | **spec written, review pending** |
| M0.9 | Index registration API design: `numPartitions`, partitioning mode, replication mode | FR-16 | todo |
| M0.17 | `java_tests.py`: recognise project-defined composed test annotations (`@Test public @interface X`), closing the blind spot named in AGENTS.md § Gates | — | todo |
| M0.52 | `tdd_scan.py` cannot mint a red record for any test that runs under a task other than its source set's default: it maps by SOURCE SET, so `http/src/test/...` always yields `:http:test`, and both `MemoryFlatUnderTenXBodySizeTest` (M1.18) and `MemoryFlatAtIntervalCeilingTest` (M3.5) are EXCLUDED from that task and run only under `memoryBoundTest`/`memoryBoundCeilingTest`. [notes](backlog-notes.md#m052) | — | todo |
| M0.54 | Hoist a `ForwardingBinStore` into `binstore-spi`'s existing `src/testFixtures` (already consumed via `testImplementation(testFixtures(project(":binstore-spi")))`), and reduce the SEVEN hand-written full pass-through decorators to it: `GateFirstPutStore`, `StatBlindStore`, `AmbiguousPutStore` and `DelayedLandingStore` in `sequencer`, `StoreFakes.GatedCommit` and `StoreFakes.FailingPuts` in `ingest`, and `CountingBinSt ... [notes](backlog-notes.md#m054) | — | todo |
| M0.55 | `Cursor.bytes` guards with `n < 0 || n > a.length - i` after M4.5 fixed an int overflow in `i + n` -- but the CHECKLIST item it belongs to is still unaddressed: `wire-format-change` requires the key length be re-checked against the 1,024-byte bound for pathological input, and nothing does. [notes](backlog-notes.md#m055) | NFR-11 | todo |
| M0.58 | A THIRD blind spot in `check-tdd`, in the same family as M0.17 (composed annotations) and the JVM-crash-instead-of-`<failure>` one AGENTS.md already records -- and unlike those two it is written down nowhere. [notes](backlog-notes.md#m058) | — | todo |
| M0.59 | Give each reviewer its own sandbox ROOT, not just its own tree. M0.56's round-5 test review found the two concurrent reviewers had both chosen `scratchpad/mut` and were overwriting each other's mutated `tdd_scan.py` mid-run -- one saw a file change between its own `cp` and its `python3`. [notes](backlog-notes.md#m059) | — | todo |
| M0.60 | ⚠️ A MUTATION RESULT CAN BE SILENTLY WRONG because of `scripts/__pycache__`. `java_tests.py` is IMPORTED, so CPython caches it; `tdd_scan.py` runs as `__main__` and is never cached. A SAME-LENGTH edit to `java_tests.py` within the mtime granularity is masked by the stale `.pyc` and the mutation never runs -- reproduced directly: identical output hash before and after a mutation that did not execute. [notes](backlog-notes.md#m060) | — | todo |
| M0.61 | Reviewer isolation stops at the filesystem the reviewer WRITES to. M0.57 gives each its own tree, M0.59 its own sandbox root -- and neither prevents reading `~/.claude/projects/<project>/<session>.jsonl`, the author's transcript, which non-negotiable 5 and review.md deny on purpose. ⚠️ The rule is currently rung 7 of gate-design: an instruction. [notes](backlog-notes.md#m061) | — | todo |
| M0.62 | `normalise_lp`'s annotation-argument blanking is DELETABLE with the whole suite green, and it is only equivalent by accident: no file and no fixture puts an ARRAY-VALUED annotation ABOVE the arming annotation. [notes](backlog-notes.md#m062) | — | todo |
| M0.63 | The two null-key refusals M0.56 shipped have no test: `if blob_sha[i] is None` in `check` and `if key is None` in `record_one` can each be replaced by `if False:` with the suite green. `check`'s is near-unreachable by construction -- an id in `fresh` comes from `test_ids`, so a null needs the two normalisations to DISAGREE, which `bothNormalisationsSeeTheSameTests` exists to prevent -- and is fine left as a belt. [notes](backlog-notes.md#m063) | — | todo |
| M0.64 | A REVIEWER CAN REPORT A VERDICT IT NEVER RECORDED, and nothing but `check-reviewed` notices. On M0.56's final round BOTH reviewers closed their reports with "verdict recorded at this hash" and NEITHER had run `review.sh record`; no file existed for either role. [notes](backlog-notes.md#m064) | — | todo |
| M0.65 | `CommitLog` holds TWO states for one fact: `commit`'s lost-race branch throws without setting `sealedAt`, so a writer fenced that way re-races the seal on every flush forever -- two wasted requests per segment flush, indefinitely -- while a writer that learned it from `recover` refuses locally at no cost. Setting the field there makes the class hold one state and the second attempt free. [notes](backlog-notes.md#m065) | FR-11 | todo |
| M0.66 | ⚠️ `check-reviewed`'s ROUND CAP IS ABSORBING AND ITS DOCUMENTED ESCAPES DO NOT WORK, so a task that exceeds it cannot be committed at all. Three defects, each verified in source rather than inferred, found when M4.6a hit the cap with BOTH roles passing. [notes](backlog-notes.md#m066) | — | **todo, NARROWED TO ITEM (3) — M4.21 landed (1) and (2)** ⚠️ ... |
| M0.67 | A FIXTURE CONVENTION for the seal tests, because three rounds of M4.6c produced three one-token fixes to the same shape and the tree is saying the FIXTURES are the source, not the assertions. [notes](backlog-notes.md#m067) | — | todo |
| M0.74 | SHARE THE REVIEWERS' DEPENDENCY CACHE, keeping their trees apart. MEASURED on this machine: a cold `buildSrc test` in a fresh tree took 376s and 233s with private Gradle homes; sharing one, 239s to fill it and 59s to reuse. [notes](backlog-notes.md#m074) | — | todo |
| M0.75 | REVIEW TREES LEAK THEIR PROCESSES, and the leak silently corrupts timing evidence. Orphaned `zsh` processes from a COMPLETED reviewer spun at ~9.5% CPU each for two hours at load average ~206, while reviewers reported measuring at load averages 123 and 186 believing those ambient. [notes](backlog-notes.md#m075) | — | todo |
| M0.73 | CUT LAYER 1, which every session pays for every skill whether or not one is used: 3,034 bytes across 13 skills against pstore's 1,011 across 5. Descriptions capped at 200 chars and `milestone-review` folded into `milestone`. [notes](backlog-notes.md#m073) | — | todo |
| M0.72 | NAME THE REVIEW RULES, because a number names nothing and goes stale silently. a numeric citation was used 20 times across scripts, skills, tests and backlog rows, and one of them -- `rule 12b` -- already resolved to nothing. [notes](backlog-notes.md#m072) | — | todo |
| M0.71 | BOUND WHAT EVERY SESSION LOADS, and derive the set from CLAUDE.md's imports rather than listing it. 26,856 -> 16,678 bytes; AGENTS.md says of itself that it is "deliberately an index", which was prose in the file nobody gates. [notes](backlog-notes.md#m071) | — | todo |
| M0.70 | SPLIT THE BACKLOG AND GATE ITS SIZE. It is read whole at every session start and grew 18,638 -> 315,383 bytes in five days, monotonically; its own first line said "current milestone only", which is prose in the file nobody gates. [notes](backlog-notes.md#m070) | — | todo |
| M0.69 | BOUND THE REVIEW PACKET, which is what a round costs to read. ⚠️ MEASURED on the staged M4.7 diff: ~140 KB per packet, built TWICE a round (both roles) across 2.6 rounds average -- 477 verdicts over 92 tasks. [notes](backlog-notes.md#m069) | — | todo |
| M0.15 | Wire `tdd-red.sh` to the real Gradle test runner — delivered **inside M-1.2**: fixing review finding T1 (any non-zero Gradle exit was recorded as a red run) required running Gradle and parsing its JUnit XML, so the wiring is the fix | — | todo |
| M0.18 | Parser regression suite in `buildSrc/src/test/java`: one fixture per bug review found, exec'ing the real scripts. Lands **inside M-1.2** with the machinery it constrains | — | todo |
| M0.23 | Module fixture that VIOLATES each of architecture.md rules 2/4/5, so `check-module.sh` is observed refusing. Today emptying `HTTP_GROUPS`, dropping `compileClasspath`, or disabling the rule-4 check leaves every test green — and green against the real tree, because no module violates a rule, so the suite cannot tell an enforcing gate from an inert one. [notes](backlog-notes.md#m023) | — | todo |
| M0.24 | `check-harness-tests.sh` asserts buildSrc's test task really declares every `scripts/*.py`, `scripts/*.sh` and `settings.gradle.kts` as an input, via a `printTestInputs` task — plus tests for that script, which today no test executes. [notes](backlog-notes.md#m024) | — | todo |
| M0.32 | ⚠️ **Arguing a review finding is currently impossible without violating the round cap.** `baselines/review.txt` must be STAGED to count (review.md rule fixed-or-argued), but staging it changes the staged bytes, which voids the hash-bound verdict that raised the finding — so clearing an argued finding always requires another review round, and rule 12 forbids that round. [notes](backlog-notes.md#m032) | — | todo |
| M0.33 | Pin `java.io.tmpdir` under `build/tmp` for **buildSrc**'s test task (testing.md rule 17). ⚠️ `binjava.java-conventions.gradle.kts` pins it for the eight product modules, but buildSrc is a separate Gradle build (`rootProject.name = "build-conventions"`) that never applies that plugin — so `FreshCheckoutTest` builds whole repository fixtures in `/tmp`. [notes](backlog-notes.md#m033) | — | todo |
| M0.25 | Run the commit-msg-stage gates in CI. ⚠️ `--hook-stage commit-msg` alone FAILS on 100% of runs (`--commit-msg-filename` is required — measured), and `--all-files` is ignored for that stage, so it must loop over `git rev-list <base>..HEAD` writing each `%B` to a temp file. Until it lands `check-test-integrity` does not run in CI at all | — | todo |
| M0.26 | Extract the CI base-ref resolution to `scripts/ci-base-ref.sh` with a red-first test driven from a scratch repo, plus a gate asserting every hook id in `.pre-commit-config.yaml` is run-by-CI or in an explicit `SKIP=`. [notes](backlog-notes.md#m026) | — | todo |
| M0.27 | CI job for L1 unit tests. ⚠️ All eight product modules have zero test sources, so the job recorded a green `L1 0s within 300s` having executed nothing. `check-harness-tests.sh` carries the antidote ("reported 0 tests — it did not run"); L1 must inherit it. Lands with the first product test | — | todo |
| M0.28 | Teach `review-lenses.sh` and `which-standards.sh` that `.github/workflows/` is executable enforcement, not prose — with a red-first T0 test. ⚠️ Confirmed live by two reviewers on M0.7: the code-detection regex does not match `.yml`, so this workflow was routed to "documentation consistency — No code changed. Do NOT review for code defects" and never to the vacuous-pass lens, even though it is unattended shell. Both reviewers had to be told to ignore the packet lens | — | todo |
| M0.30 | Residual hardening in `check-gate-scope.sh` and `FreshCheckoutTest`, all found on M0.29/M0.31 passing rounds: `run()` lacks `pipefail` so a failing producer `tar` exits 0; `run()`/`trackedIn()` do not scrub `GIT_DIR`/`GIT_INDEX_FILE` as `gate()` does, so an absolute value makes the post-condition compare a list to itself | — | todo |
| M0.39 | Delta-scope `check-coverage.sh` so a docs-only commit makes no Gradle call (build.md's invariant), WITH tests for both directions. ⚠️ Written and removed from M0.13 under rule 12: every `CoverageGateTest` case runs at `GATE_SCOPE=full`, so the branch never executed and reverting `--diff-filter=ACMRD` — the fix that makes `git rm SomeTest.java` visible — left all 8 tests green | — | todo |
| M0.40 | Test the JaCoCo wiring in `binjava.java-conventions.gradle.kts` against REAL Gradle. ⚠️ `CoverageGateTest` stubs `./gradlew`, so deleting `mustRunAfter(tasks.withType<Test>())` or the `check` → `JacocoReport` `dependsOn` leaves the suite green — and those two lines are the fix for the stale-exec-data and report-never-generated defects that two rounds found only by hand-reproducing them in a throwaway project | — | todo |
| M0.41 | A gate that verifies the STAGED TREE, not the working tree. ⚠️ Every gate today runs against the working directory, so "all gates green" means "green given my untracked and unstaged files" — not "green as committed". [notes](backlog-notes.md#m041) | — | todo |
| M0.42 | Derive the module list from `settings.gradle.kts` instead of a fourth hand-rolled copy. ⚠️ `scripts/coverage.py` selects modules with `git ls-files '*/build.gradle.kts'` filtered by `p.count('/') == 1`, and NO fixture nests a module two levels deep — delete the clause and all 14 `CoverageGateTest` cases stay green, so a module added one level down would be silently unmeasured while the gate printed `ok N module(s) me ... [notes](backlog-notes.md#m042) | — | todo |
| M0.43 | Key red records on the TEST, not the whole file. ⚠️ `.harness/tdd/red.json` stores the sha256 of the entire test file, so adding one method invalidates the record of every test beside it. Re-recording then needs a red state all of them share, and the only one available is "the gate is absent" — which `red.log` shows is a `NoSuchFileException` thrown before the gate ever runs. [notes](backlog-notes.md#m043) | — | todo |
| M0.44 | `check-coverage.sh` cannot regenerate a buildSrc report. ⚠️ It runs `./gradlew test jacocoTestReport` from the ROOT, and buildSrc is an implicit included build whose tasks that invocation never reaches; `buildSrc/build.gradle.kts` applies `kotlin-dsl` and never `jacoco`. Verified: `rm -rf buildSrc/build/test-results` then that exact command exits 0 without recreating it. [notes](backlog-notes.md#m044) | — | todo |
| M0.45 | `check-coverage.sh` runs Gradle before the checks that need no build. ⚠️ A malformed `baselines/coverage.txt` is a pure text predicate, but the refusal is paid only after a full `test jacocoTestReport` — verified against `gradlew-invocations`. Since the Gradle cost is precisely why M0.13 ships unwired (M0.39), inheriting it for a text check is avoidable. [notes](backlog-notes.md#m045) | — | todo |
| M0.50 | The round cap cannot prevent the round it caps. ⚠️ `check-reviewed.sh` checks verdict EXISTENCE first and `finish`es on failure, so the cap at round 3 is never reached until verdicts for round 3 already exist — it refuses the commit only after the review has been paid for in full. The refusal belongs in `review.sh context`, which must decline to build a packet past the cap and say "split it, or argue the finding". ⚠️ Found on M0.49, whose round-2 MAJOR could not be fixed without paying for a round 3 that the gate would then reject | — | todo |
| M0.21 | `review.sh` refuses a verdict whose index moved while the review was in flight — review.md rule hash-bound was broken five times in one session, and a prose rule broken five times is a predicate written as English (non-negotiable 9) | — | todo |
| M0.20 | `scripts/check-cross-refs.sh` — every `M<n>` and `R<n>` cited anywhere resolves to a roadmap row / a `cost.md` rule, and no row under `## Completed` contradicts its own State cell. Review found all three by hand | — | todo |
| M0.19 | `scripts/check-catalog-only.sh` — a module may not declare a literal dependency coordinate, only a catalogue alias or `project(...)`; the licence gate's completeness rests on it | — ⚠️ AND `prefixFor` KEYS ON THE JAR FILENAME, NEVER THE GROUP, so a future `framework-<version>.jar` from any other group inherits the committed `framework Apache-2.0` line and passes green with its own licence never declared -- if it were GPL-2.0-only the deny list would never see it. Keying on `group:artifact` belongs here. | — | todo |
| M0.77 | SWEEP THE PRE-EXISTING ASSERTION INVENTORIES -- "every other test here", "the only assertion". MEASURED 2026-09-06: 61 occurrences in 36 files; a line-scoped grep sees 50 in 31. Quote the method with the number -- this row has carried three wrong figures. No gate: see M0.76 and [notes](backlog-notes.md) | — | todo |
| M0.78 | HARDEN `check-archive-row-size.sh` and its test, from five minors both reviewers ACCEPTED at M0.76: three row shapes that still evade the cap, a landed row that can be GROWN past it, and three mutations its tests do not kill. [notes](backlog-notes.md) | — | todo |
| M0.79 | LAND THE `brevity` SKILL AND ITS ADAPTER, which arrived in the working tree from outside any task and which the generated-index gate then blocks every commit on. Skill body, `.claude/commands/brevity.md`, and the `reviewer`/`test-reviewer` agent files pointed at it for OUTPUT STYLE ONLY -- the finding format (`file:line`, severity, concrete failure scenario) is explicitly not overridden | — | todo |

## M1 — walking skeleton

| ID | Task | Serves | State |
|---|---|---|---|
| M1.1d | `MemoryBinStore.list(prefix, startAfter, 0)` throws `IndexOutOfBoundsException` from a method contracted on `IOException` — `page.get(-1)` when `maxKeys <= 0`. ⚠️ M1.3b's LIST ceiling reaches exactly this when its budget hits zero. Also: the key-limit message prints `key.length()` characters while the check measures UTF-8 bytes | FR-8 | todo |
| M1.1f | Conformance leftovers, none blocking: a zero-length object never round-trips; `minPartSize` and `batchDelete` are returned by production and read by no assertion; and `CyclicBarrier.await()` in the concurrency case has no timeout, so a backend that stalls hangs the suite instead of failing it | FR-8 | todo |
| M1.3b | `GoverningBinStore`: ratio-to-expected, LIST ceiling, priority classes; writes never refused | FR-21 | todo |
| M1.6c | Durable ordinal registry behind `StreamResolver`: resolve an index NAME to its stream id, and survive a restart. ⚠️ M1 supplies the mapping from the caller, which is why `StreamResolver` exists as a seam at all — the producer writes `POST /logs/_bulk` while the plugin derives its stream from the OpenSearch index UUID, and something must join them. ⚠️ Raised because the `// SKELETON:` marker on that seam named no task, and the M1 SPEC requires a marker the milestone review can actually check | FR-1 | todo |
| M1.6d | A dropped inline push must degrade to LAG, not to record loss. ⚠️ ADR-0004 says a lagging consumer falls behind and recovers from the log — **nothing in M1 implements that**: `DefaultIngest` drops a push past its byte budget and `ConsumerClient` drops on a full queue, and neither end detects an offset gap or reads the commit log. So a drop is currently silent record loss for that subscriber. M1.6b counts drops (`droppedPushes()`) so the loss is at least visible; this row closes it | FR-5 | todo |
| M1.7c | Per-request authentication on the `_bulk` endpoint: resolve a `Credential` to a `Principal` via `CredentialSource`. ⚠️ M1.7 ships a FIXED principal — SPEC § Scope puts authentication out of M1 deliberately, and this row exists so the deferral is scheduled rather than merely mentioned. Raised because M1.7 first cited **M1.8** for it, which is the accumulator task and already done: the sentence read as a plan and scheduled nothing | FR-1 | todo |
| M1.7d | A `BulkParseException` names the line it was on. ⚠️ On a 10,000-action body the shape alone does not locate the error, and the javadoc claimed a line number it never carried | FR-1 | todo |
| M1.7e | Licence intake for the OpenSearch test-framework jars: 151 problems from `./gradlew dependencyLicenses` (HdrHistogram, RoaringBitmap, jackson-*, httpclient5, jna, joda-time and ~40 more). ⚠️ **Pre-existing, not introduced by M1.7** — Helidon was taken in fully (mapping, `helidon-LICENSE.txt`, SPDX line, 37 pinned SHAs, zero problems). ⚠️ `check-dependency-licenses.sh` iterates only prefixes already in `SPDX.txt`, so an undeclared dependency passes it VACUOUSLY: the pre-commit gate is green while `dependencyLicenses` is red | — | todo |
| M1.7f | Give the `Ingest` seam its own authorization exception instead of `IllegalArgumentException`. ⚠️ IAE is also how `AppendResult` and `CommitLog` report their own invariant failures, so a caller cannot distinguish "you may not write here" (permanent, drop) from "this implementation has a bug" (retryable) — mapping the ambiguous IAE to 403 turns a bug into silent data loss. [notes](backlog-notes.md#m17f) | FR-1 | todo |
| M1.7h | ⚠️ **Unreproduced, possibly real**: a reviewer instrumenting the `Ingest` seam observed `append` receiving **1 record out of the 100** in a 4880-byte `_bulk` body, five consecutive runs, each answered 202 — i.e. a 202 covering 1 of 100 documents, which is silent data loss. It did not reproduce in its own 14 later runs, nor in **420** requests I ran across sequential and async constructions, warm and cold. [notes](backlog-notes.md#m17h) | FR-1 | todo |
| M1.10b | `CommitLog.commit` retries an unbounded number of times. ⚠️ Found by a mutation that HUNG the audit rather than failing it: with `apply()` not advancing `nextSequence`, the loop re-reads a taken slot forever. The shipped code cannot livelock — `apply` always advances past the winner — but nothing bounds the loop, so any future condition that stops it advancing becomes a spin rather than an error. Add a retry ceiling that fails loudly, and a test that asserts the ceiling fires | FR-3 | todo |
| M1.11b | **`HubTransport`** — the production `SubscriptionTransport` that bridges `SubscriptionHub` to `ConsumerClient` for M1's in-process inline mode. [notes](backlog-notes.md#m111b) | FR-5 | todo |
| M1.22 | The SPEC's test table names have drifted from delivered method names in roughly a dozen rows (e.g. T1b's `committedGoldenSegmentStillParses` vs the delivered `theCommittedGoldenSegmentStillParses`, `GoldenSegmentTest`). [notes](backlog-notes.md#m122) | — | todo |
| M1.15e | `check-locale-format.sh` — a gate over a predicate: any `String.format`/`.formatted()` in `*/src/main` whose format string carries a **decimal** conversion (`%d`, `%f`, `%,d`) must pass `Locale.ROOT`. ⚠️ `%x`/`%o`/`%s` are exempt and the gate must say so — `java.util.Formatter` does not localize them (measured, JDK 25), and a gate that flags them would be turned off. [notes](backlog-notes.md#m115e) | NFR-2 | todo |
| M1.16b | ⚠️ **Withdrawn, not done.** A `tick()` seam on `DefaultIngest` plus a 3,000-tick deterministic idle test. A review showed the test was a TAUTOLOGY: `tick()` short-circuits on `pending.isEmpty()` before `isFlushDue()` is reached, so the injected clock was never read — deleting `clock.advance(5 min)` left it green. The seam and the test were removed rather than kept with a weaker claim. [notes](backlog-notes.md#m116b) | NFR-2 | todo |
| M1.16c | A `Clock` seam for `ConsumerClient`, so a 1,600-consumer idle test is deterministic rather than a race (measured 3/8 passing; 0/5 hot-spinning). ⚠️ It buys a **liveness and memory** proof at 1,600 consumers, NOT a falsifiable request-count zero: `ConsumerClient` holds no `BinStore`, the same fact that strikes out SPEC row T8b. [notes](backlog-notes.md#m116c) | NFR-2 | todo |
| M1.16e | A gate for ADR-0023's check: no class under `plugin/src/main` or `client/src/main` imports `binjava.binstore`. ⚠️ Rung 3 of `gate-design` -- a predicate over files in the tree -- and the same shape `check-module.sh` already enforces for rule 4 (nothing depends on `http`). Until this lands the check is run by hand; the ADR records the rule but nothing enforces it | NFR-2 | todo |
| M1.24 | `milestone-review` harvest: `OffsetMonotonicityIT#readOffsetsInDocOrder` (M1.15d) assumes Lucene's `reader.leaves()` returns segments in creation order — an unenforced implementation detail, not a documented API guarantee. [notes](backlog-notes.md#m124) | — | todo |
| M1.19b | Metrics with the allow-list labels; in-memory per-index counters; `/admin/cost` top-K | NFR-16 | todo |
| M1.19 | Gate benchmarks: JMH `-prof gc`, allocation-per-record bounds, inside L1's budget | NFR-1 | todo |
| M1.20 | GH Actions: L1 always; L2/L3 by path filter, label and nightly | — | todo |
| M1.20b | `check-module.sh` resolves only `runtimeClasspath` and `compileClasspath`, so **no test-scope module dependency is covered by any gate**. ⚠️ `testImplementation(project(":http"))` added to `plugin` would pass green at `GATE_SCOPE=full` — putting Helidon in the OpenSearch JVM, which is the exact thing architecture.md rule 4 exists to forbid and which M1.7g's javadoc leans on. Found while adding a legitimate test-scope dependency and asking whether the gate saw it: it does not. Rule 4 is currently held by prose | — | todo |
| M1.20c | A gate for UNTRACKED source files. ⚠️ An untracked `*Test.java` runs on every local `./gradlew test` and **no gate sees it** — not `check-tdd` (it reads the staged diff), not `check-file-size`, not `check-test-integrity`. Found when a review agent left two scratch probes (`ZDebugProbeTest`, `ZDebug2Test`, one containing a `Thread.sleep`) in `http/src/test`: they joined the suite, ran green, and were invisible to every check. The failure that matters is the inverse — a local file making a red suite look green | — | todo |

## M2 — segment format

| ID | Task | Serves | State |
|---|---|---|---|
| M2.10 | `milestone-review` harvest: `MemoryBinStore.putIfMatch`'s version-match check (M2.0) already correctly uses `Version.equals()` (a record, value equality by construction), not `==` -- but no test constructs a FRESH `Version` instance carrying the same token as the current one, so a regression to reference equality would pass every existing test silently (`LocalFsBinStore` is only accidentally immune, since `readMeta` always reconstructs a new instance from disk). Not a live defect; a cheap regression test worth adding | FR-8 | todo |
| M2.11 | `milestone-review` harvest: `IndexOrdinalRegistry.ordinalFor()` (M2.3) holds its lock across the WHOLE read-modify-CAS-retry cycle including the network calls, serializing a burst of many distinct new-index registrations on one shared instance to one in flight at a time. Accepted at M2.3 as a latency tradeoff, not a request-rate defect (ADR-0008 frames bursty registration as rare) -- revisit if a later milestone's throughput makes concurrent new-index bursts common | FR-8 | todo |
| M2.12 | `milestone-review` harvest: `MembershipFilter.Bloom.of()` (M2.5) doesn't validate `m` before use (a non-positive `m` throws a raw `ArithmeticException` instead of `Bloom`'s own canonical-constructor message) and doesn't reject a negative ordinal in its input collection, unlike `mightContain()`'s own guard -- a write/read asymmetry accepted at M2.5 as low-risk since `m`/ordinals were not yet caller-supplied. [notes](backlog-notes.md#m212) | NFR-12 | todo |
| M2.13 | `milestone-review` harvest: `FilterCandidates` and `BloomConstructionTest` (M2.6) each independently declare the same Bloom sizing constants (4.79 bits/item, `k`=3) with no shared source of truth -- a future FPR-target retune could update one and silently miss the other, so `FilterCandidates` keeps sizing to the old target while the property test keeps passing against the new one | NFR-12 | todo |
| M2.14 | `milestone-review` harvest: `SegmentKey`'s canonical constructor (M2.6) only catches `IOException` when validating `filterEncoded`; `MembershipFilter.RunLength.decode` can throw an unchecked `IndexOutOfBoundsException` on a pathological uvarint run (`pos+run` overflowing `int`, then `BitSet.set(from, negative-to)`). [notes](backlog-notes.md#m214) | NFR-12 | todo |
| M2.15 | `milestone-review` harvest: no `clusterTest` exercises a segment carrying a REAL, non-`N` membership filter tag through the plugin's own read path against a running OpenSearch node. `SearchableIT`/`ShardFanOutIT` build their `SegmentKey` via the legacy 5-arg constructor (always `N`); M2's own Design section notes a subscribed consumer never looks at the filter at all. Real-node coverage of a non-`N` tag is backend-round-trip only (M2.9), not plugin-read-path -- revisit once a recovery/GC-path consumer that actually reads the filter exists | NFR-12 | todo |
| M2.16 | `milestone-review` harvest: round-1 review of this very harvest (M2.10) found the original wording of this row wrong -- `Files.createTempDirectory` in every `LocalFsBinStore`-backed test (`LocalFsBinStoreTest`, `SearchableIT`, `ShardFanOutIT`, `RestartResumeIT`, `OffsetMonotonicityIT`, `DeleteAndVersionIT`, `MemoryFlatUnderTenXBodySizeTest`, M2.9's own `SegmentKeyBackendRoundTripTest`) does NOT violate testing.md rul ... [notes](backlog-notes.md#m216) | — | todo |

## M4 — sequencer and commit log

| ID | Task | Serves | State |
|---|---|---|---|
| M4.3o | `LeaseConfig.prefix` has a null check that nothing pins and no other guard, so `""` is accepted and `leaseKey()` returns `/ctl/lease/0.json` -- the BUCKET ROOT. Every cluster whose prefix is unset then leases against the same object: two clusters contend for one lease, and each can fence the other out of a commit chain it has no relationship with, having never heard of it. [notes](backlog-notes.md#m43o) | FR-11, NFR-11 | todo |
| M4.3p | The SLOT is a hardcoded `0` inside `LeaseConfig.leaseKey()`, on the record whose components ARE the configuration -- while the javadoc three lines up, and ADR-0007 itself, both say "raising S is a configuration change, not a format change". A reader who takes that at its word looks for the slot among the configuration and does not find it. [notes](backlog-notes.md#m43p) | FR-11 | todo |
| M4.3q | Make an AMBIGUOUS conditional write countable, so ADR-0027's accepted cost has a frequency rather than a shrug. That ADR accepts a self-fence window whose only stated route to being revisited is a measurement -- "the ADR that supersedes this one will have the frequency number that this one cannot" -- and until something counts them there is no such number. [notes](backlog-notes.md#m43q) | FR-11, NFR-9 | todo |
| M4.6 | ⚠️ SPLIT into M4.6a-d; this row is the source text and is not itself implemented. The four pieces are separable and one of them (M4.6a) closes a live invariant hole on its own, so carrying them as one commit would have held that fix behind lease wiring it does not need. [notes](backlog-notes.md#m46) | FR-11 | split |
| M4.6b | The seal protocol's two losing branches at the `CommitLog` layer: a fenced old leader treats its loss at `N+1` as proof it is fenced and stops; a new leader holding a valid lease REDRIVES to `N+2`, because stopping would surrender sequencing cluster-wide. [notes](backlog-notes.md#m46b) | FR-11 | folded into M4.6c |
| M4.7c | WIRE THE BATCHER INTO THE INGEST PATH, and split `DefaultIngest` so it fits. ⚠️ M4.7 built `BatchingSequencer` and nothing constructs one outside its own test, so the commit rate reduction is available and not yet TAKEN -- a reviewer named this and it is recorded rather than glossed. [notes](backlog-notes.md#m47c) | NFR-1 | todo |
| M4.8 | ⚠️ SPLIT into M4.8a-c; this row is the source text and is not itself implemented. It carries three separable pieces -- a record format, a write trigger, and a discovery mechanism that needs its own ADR -- plus a >5,000-checkpoint workload that is an acceptance constraint inside M4.8c rather than a fourth task. [notes](backlog-notes.md#m48) | FR-11 | split |
| M4.8b | ⚠️ SPLIT into M4.8b1-b2; this row is a banner and is not itself implemented. The source text is NOT preserved here -- the two sub-rows plus their review findings replaced it. [notes](backlog-notes.md#m48b) | FR-11 | split |
| M4.10 | SPLIT into M4.10b/c/d -- commit idempotency, keyed on `(podId, incarnationId, flushSeq)` per ADR-0036 (M4.10b, done). c carries the triple in both formats; d is the dedup; the end-to-end anchor moved to M4.10f when d was split. [notes](backlog-notes.md#m410) | FR-3 | split |
| M0.87 | `rm -rf` ON A FIXTURE DIRECTORY FAILS ON THE CI RUNNER with `Directory not empty` -- it unlinks the children and finds the directory repopulated. Twice, in `FreshCheckoutTest`, which tars ~700 tracked files into /tmp; it reproduces neither locally nor under CI's environment on a dev machine. Cleanup is best-effort now, so it cannot fail a test about the gate, but the CAUSE is unknown and a fixture may be leaking on every run. | — | todo |
| M0.84 | THE LICENCE GATE CAN PASS WHILE MEASURING NOTHING, three ways review measured: `GPL-3.0-only WITH Classpath-exception-2.0` is a valid SPDX expression that the deny list never matches (the parser splits with limit 2, then tests set membership), as does lowercase `gpl-3.0-only`; a non-`.jar` dependency is skipped entirely while the stamp still reports the set complete; and `seenLicence` is written but never read, so a licence-file orphan goes unreported. [notes](backlog-notes.md#m084) | — | todo |
| M0.85 | A PUSH WHOSE BASE PREDATES A FILE MAKES THE RANGE GATES JUDGE HISTORY. CI resolves CHECK_RANGE from `github.event.before`, so a 34-commit push put 25 archive rows written before `check-archive-row-size` existed inside its range -- the largest 18,845 characters against a 4,000 cap. Nothing was wrong with the gate or the rows; the range was. [notes](backlog-notes.md#m085) | — | todo |
| M0.82 | DECIDE WHETHER `check-diff-size.sh` SHOULD WEIGH TESTS LIKE PRODUCTION. It counts every added line under the source trees, so a commit pushed over the cap by review-MANDATED tests is refused for doing what review asked -- M4.10d hit that at 1147, ~450 production and ~700 tests. [notes](backlog-notes.md#m082) | — | todo |
| M4.10e | RECONCILE AN AMBIGUOUS COMMIT FROM THE CHAIN: a PUT that landed and lost its response is never recorded as applied, so the retry commits the same records twice -- measured twice by review. Split out of M4.10d at round 2 per review.md two-round-cap, after an attempt there was found BLOCKING. [notes](backlog-notes.md#m410e) | FR-3 | todo |
| M4.10f | INHERIT THE IDEMPOTENCY WINDOW ACROSS A TAKEOVER, so a replay that crosses a leader change is answered rather than committed twice -- the case a process-local map cannot reach. Carries the per-pod slots off the checkpoint the crossing ALREADY reads, so it costs zero extra requests. Owns the end-to-end anchor: invert `CheckpointWriterContentTest`'s `doesNotContainKey` tripwire. Split out of M4.10d at round 2. [notes](backlog-notes.md#m410f) | FR-3 | todo |
| M4.10g | BOUND THE IDEMPOTENCY WINDOW. It gains an entry per pod INCARNATION -- one per restart -- and nothing evicts, so a long-lived leader's window grows with the fleet's restart history. ⚠️ A BOUND WAS WRITTEN AND WITHDRAWN at M4.10d round 4: three mutations of its loop survived, one of which CLEARED the whole window at the threshold. [notes](backlog-notes.md#m410g) | FR-3 | todo |
| M4.22 | MAKE THE CHECKER SAY HOW MUCH IT EXAMINED. `Invariants.checkChain` returns an empty list both for a chain that held nothing wrong and for a chain that was never there -- measured: on an empty store it returns `[]` for any epoch and any prefix -- so a caller cannot tell CLEAN from UNREAD. [notes](backlog-notes.md#m422) | FR-11 | todo |
| M4.23 | AN UNSEALED ANCESTOR IS INVISIBLE WHEN EPOCHS WERE SKIPPED, and M4.16 records that as the NORMAL case. `checkLink` is applied only to the epoch the CONTINUE NAMES, never to the epoch the walk-back lands on and actually inherits from -- so a run of burned epochs hides an unsealed ancestor entirely. [notes](backlog-notes.md#m423) | FR-11 | todo |
| M4.24 | THE CHECKER'S OWN COST IS O(E-squared) and it decides whether M4.13's stated budget is reachable. `checkChain` per epoch times `firstAncestorWithDeltas` scanning every lower epoch measured 4,999 LISTs for ONE examination at epoch 5000, against a sweep budget of <60s for 1,000 seeds. [notes](backlog-notes.md#m424) | FR-11 | todo |
| M4.25 | A DANGLING JAVADOC IN COMMITTED CODE, and javac warns on every single build. `FaultInjectingStore.java:144`'s single-threading note sits above `/** Every fault this run injected, in order. */`, so two comment blocks stack and the first attaches to nothing: `warning: [dangling-doc-comments] documentation comment is not attached to any declaration`. [notes](backlog-notes.md#m425) | — | todo |
| M4.26 | THE OLDER FINDING-ARGUE PATH READS THE WORKTREE, so a wholly UNTRACKED `baselines/review.txt` silences blocking and major findings from both roles and the gate prints `ok`. [notes](backlog-notes.md#m426) | — | todo |
| M4.27 | M4.20 SHIPPED WITH TWO OF THE FIVE CONJUNCTS ITS ROW STATES AS ACCEPTANCE CRITERIA UNPINNED, found by a SECOND, independent test review after the commit had landed (`2e0f1d9`) -- the first test reviewer passed it. [notes](backlog-notes.md#m427) | FR-11 | todo |
| M4.28 | THE ARGUED-CAP `WARN` IS INVISIBLE WHERE IT FIRES. pre-commit prints a hook's stdout only on FAILURE unless the hook sets `verbose: true`, and `.pre-commit-config.yaml` does not -- verified by running `pre-commit run check-license-headers`, which printed `Passed` and swallowed that script's own `ok`. So an argued third round shows a plain green `Passed` and the warning nobody sees. [notes](backlog-notes.md#m428) | — | todo |
| M4.29 | `review.sh record` DOES NOT VALIDATE `--task`, which is a COMPLETE bypass of rule 12's round cap and leaves no trace. ⚠️ MEASURED by M4.21's round-3 review: recording both roles under `--task M9.7` with four prior rounds on disk gives `ok ... (round 1 of 2)`, exit 0; omitting `--task` writes `"task": ""` and gives `round 0 of 2` over two present verdicts -- a count the counter's own definition makes impossible. [notes](backlog-notes.md#m429) | — | todo |
| M4.30 | THE REVIEW PACKET SAYS "round 1 of 2" ON EVERY ROUND, AND SWALLOWS THE REFUSAL M4.21 ADDED, so the one number that decides whether rule 12 is about to bite is wrong in the document the reviewer reads. ⚠️ MEASURED by M4.21's round-3 review, which received a packet reading `This is round 1 of 2 for M4.21` above `=== THIS IS NOT ROUND ONE ===`. [notes](backlog-notes.md#m430) | — | todo |
| M4.31 | Key the argued round-cap exception to the STAGED CONTENT, not to how git RENDERS the diff. ⚠️ `check-reviewed.sh` decides an argument is present with `git diff --cached -- baselines/review.txt | awk '/^\+/'`, which asks "does this text sit on an added line" -- a THIRD question, neither "is the argument staged" (review.md rule fixed-or-argued) nor "is it new to this commit". [notes](backlog-notes.md#m431) | M4.21 | todo |
| M4.33 | TWO SKILL BODIES STILL DESCRIBE THE PRE-ESCAPE WORLD, and skills are the layer an agent actually reads. ⚠️ `.agents/skills/review/SKILL.md` § Resolution describes `baselines/review.txt` as holding "a staged entry naming the finding's id" -- true of the older finding-argue path, but M4.21's `rounds:<task>` key names a TASK, not a finding, and appears nowhere in the skill. [notes](backlog-notes.md#m433) | M4.21 | todo |
| M4.34 | THE REVIEW PACKET TRUNCATES SILENTLY AT ~37 KB, so a reviewer judges a diff it was never shown and is not told. ⚠️ MEASURED on M4.21's round-6 production review: `./scripts/review.sh context` cut off partway, dropping the last third of `ReviewRoundCapTest.java` and most of the backlog rows. [notes](backlog-notes.md#m434) | — | todo |
| M4.13b | THE TIME-SHAPED FAULT CLASSES M4.12 DOES NOT MODEL: delayed writes and reordered completions. Both need the injector to HOLD an operation and land it later, which it cannot do today -- it delegates inline on every verb, and the driver is a synchronous act-or-fail loop with no pending queue. A deferral mechanism and its drain policy are the work. [notes](backlog-notes.md#m413b) | FR-11 | todo |
| M4.13e | PARTITIONED LEADERS AS AN INJECTABLE FAULT, not only as a consequence. The store has no notion of WHICH pod is calling, so `unreachable` is a global coin flip and cannot cut one pod off while another stays connected -- the shape a lease fight takes. Needs pod identity threaded into the store, plus an `unreachable` arm on `putIfMatch`, whose `duplicatePut` draw is currently TAKEN AND DISCARDED so that class never reaches the lease path. [notes](backlog-notes.md#m413e) | FR-11 | todo |
| M4.13d | EVERY FAULT CLASS MUST CHANGE AN OUTCOME IN AT LEAST ONE SEED, reported by the harness -- or the simulation proves the simulator rather than the system. Needs `Result` to carry per-class evidence that a clean run and a faulted run of the same seed DIFFER. ⚠️ AND IT OWNS TURNING `withheldPut` ON: M4.13c measured that at 0.05 the sweep's seeds-committing-nothing reaches 3, the bound `aFAULTEDSWEEPKeepsCOMMITTINGRatherThanSTALLING` asserts, so the class needs a RE-BASELINE of that bound and not a relaxation. [notes](backlog-notes.md#m413d) | FR-11 | todo |
| M4.13h | `checkReader` RE-READS WHAT `checkChain` JUST READ. Both call `readChain` on the same `(store, prefix, epoch)` and `inheritedOffsets` recurses every ancestor, so a sweep running both pays it twice: MEASURED through `CountingBinStore` over an 8-epoch sealed-and-continued chain, 192 GETs / 64 LISTs becomes 384 / 128. Both are already quadratic in epoch count, which is M4.24. An overload taking the entries and the inherited map already in hand costs nothing to add. | FR-11 | todo |
| M4.13j | `Invariants.neverOpened`'s `sequence() == 0` CLAUSE IS UNCONSTRAINED, and M4.13a made it load-bearing: it now decides whether `checkReader` judges at all. MEASURED -- dropping it to a bare `instanceof Seal` leaves the whole `:sequencer` suite green. [notes](backlog-notes.md#m413j) | FR-11 | todo |
| M4.13k | `check-mutants` STRUCTURALLY CANNOT SEE AN INVARIANT CHECKER, because it filters `*/src/main/java/*` and this repository keeps its checkers in the test source set -- so M4.13a's deliverable passed the gate without one line of it being mutated, and its two majors were both found by a reviewer mutating by hand. Either PIT gets pointed at the checker classes, or the gate reports NOT MEASURED loudly enough that a commit whose deliverable is test-source cannot read as covered. ⚠️ Rung 3 work: the rule is a predicate over the tree. | — | todo |
| M4.13l | `Invariants.inheritedOffsets`' `if (depth > 1000) return inherited;` SILENTLY TRUNCATES the inherited base, and M4.13a made that decide a reader-side verdict. `ChainReplay` -- the production path it judges -- is iterative, uncapped, and documents that same 1,000 cap as MEASURED WRONG at 1,002 legitimate terms. Unreachable in today's tree; the defect is that the checker would disagree with production by TRUNCATING rather than by finding anything. | FR-11 | todo |
| M4.13o | `DefaultIngest` THROWS ON A BATCHED DELTA, the same defect M4.13n fixed in the checkers but on the APPEND path: it iterates `delta.runs()` over the delta the Sequencer returned, and `BatchingSequencer.commitBatch` hands the same FULL batched delta to every waiter. ⚠️ `allRuns()` is the WRONG fix here -- two pods on one stream give two runs and `firstOffsets.put` keeps the last; it needs `segments()` plus the caller's own key. Unreachable today: nothing constructs a `BatchingSequencer`. [notes](backlog-notes.md#m413o) | FR-11 | todo |
| M4.13p | THE REORDER THAT PRODUCERS CAN SEE IS PER-POD FLUSH ORDER, not segment list position, and M4.13f's `order` arm does not catch it. MEASURED through `commitAll`: a batch carrying one pod's `flushSeq=2` at offsets 0..2 and its `flushSeq=1` at 3..5 gives `checkChain == []` -- the same pod's earlier flush landed later. The attribution has been in the bytes since M4.10c and the grouping over `allRuns()` discards it. [notes](backlog-notes.md#m413p) | FR-11 | todo |
| M4.13q | THE `order` ARM IS PER-DELTA, so the same permutation one delta boundary out is still reported as `gap` plus `I2` -- the mislabel M4.13f removed inside a delta. MEASURED: RA at 4..6 in sequence 1 then 0..3 in sequence 2, disjoint and covering 0..6 once, reports `gap ... skipping 4` and `I2 ... assigned up to 7`. ⚠️ DECIDE FIRST whether it IS a defect: one shared cursor cannot rewind between commits, so the shape may be unreachable. | FR-11 | todo |
| M4.13 | THE 1,000-SEED RUN asserting I1-I5, every failing seed pinned as a named regression, T1 on MemoryBinStore so it runs on every commit, a stated budget of <60s and a configurable seed count. ⚠️ DECOMPOSED into M4.13b, d and e, which own the fault classes it cannot assert without; this row is the SWEEP and its budget. [notes](backlog-notes.md#m413) | FR-11 | todo |
| M4.14 | The compaction-trigger observable: a HISTOGRAM of commit-log index entries per stream plus the periodic top-K structured log event (Q17 / measurement M4), so M7's threshold is chosen from data. ⚠️ NOT a per-stream metric label -- `stream` is on observability.md rule 1's \"never a label\" list with a gate enforcing it | — | todo |
| M4.18 | `DefaultIngest` takes a `store`, a `prefix` AND a `Sequencer` that already has its own store and prefix, and nothing checks they agree. Before M4.6d the relationship was STRUCTURAL -- one constructor built the `CommitLog` from those same two arguments -- and injecting the seam turned it into a convention. [notes](backlog-notes.md#m418) | FR-11 | todo |
| M4.35 | `BatchingSequencer.close()` INTERRUPTS THE COMMITTER WHILE IT MAY BE INSIDE ITS OWN LEASE RELEASE, so the shutdown path is a CAUSE of the release failing -- the hazard `close` defends itself against, inflicted on the committer. Swallowed by `catch (Throwable)`, so shutdown reports success while the lease is held to its TTL. [notes](backlog-notes.md#m435) | FR-11, NFR-9 | todo |
| M4.36 | THE JOIN-TIMEOUT WARNING DESCRIBES SOMETHING THAT MAY NOT HAPPEN: the delegate close it announces is CAS-gated and may close nothing, and the same message fires when `join()` was INTERRUPTED rather than timing out, where "within 10s" is false. `close()`'s own drain net is also undetected -- but pinnable today, without a new seam. [notes](backlog-notes.md#m436) | FR-11 | todo |
| M4.37 | TWO CHANGES M4.7 MADE ARE UNPINNED, both measured surviving mutations: `terminalFailure()`'s message and cause, and the `catch (Exception)` -> `catch (Throwable)` widening. Production is correct today; the risk is that the round-6 major regresses silently on the QUEUED-caller path. [notes](backlog-notes.md#m437) | FR-11 | todo |
| M4.39 | THE FENCE IS UNBOUNDED, and M4.17 made ADR-0027's ACCEPTED false-empty reachable for the first time. That ADR accepts a self-fence whose cost is "exactly the remainder of the TTL, unshortenable" -- a bound that holds only because the lease can be retaken at expiry. Nothing retakes it in M4. [notes](backlog-notes.md#m439) | FR-11, NFR-9 | todo |
| M4.40 | THE interrupt-BEFORE-release ORDER IN `LocalSequencer.close()` IS UNPINNED, and its own comment calls that order load-bearing. The close/renewer race itself IS now pinned, but nothing constrains the two steps' ORDER: no seam exists between the post-tick `closed` check and `leases.renew()`. [notes](backlog-notes.md#m440) | FR-11 | todo |
| M4.41 | THE RENEW-RATE DETECTOR FIRES ~40% OF THE TIME for the mutation that matters. `multipliedBy(2)` is BIMODAL at 3/4 against an inclusive floor of 4, so the surviving band still contains the 2x that reinstates M4.17's own defect at a 9s/10s config. Two committed claims about it are unsupported. [notes](backlog-notes.md#m441) | FR-11, NFR-9 | todo |
| M4.42 | A CORRECTED CLAIM SURVIVES IN TWO DUPLICATES, one of them in PRODUCTION. M4.17 struck "keeps committing into a chain a successor has already sealed" from its cap argument as false -- `CommitLog` refuses that case -- and left the identical claim standing in the test file AND in `LocalSequencer`'s empty-means-fenced branch. [notes](backlog-notes.md#m442) | FR-11 | todo |
| M4.44 | A COMMENT DESCRIBES A COST THE TREE DOES NOT PAY. `tickAndAwaitProcessed`'s `parked.drainPermits()` justification says a mutation would die at a twenty-second deadline rather than at the assertion naming the defect; both roles measured it dying at ~5s at a third assertion, and the drain's premise is unreachable as staged. [notes](backlog-notes.md#m444) | FR-11 | todo |
| M4.45 | AN INVERTED GLOSS IN A TEST COMMENT, and it attributes the inversion to a named review. `FakeSequencerTest` says a missing sort produces the asserted order 1 run in 6, "i.e. a broken sort would pass CI most times; round-2 review measured exactly that" -- it would FAIL most times. [notes](backlog-notes.md#m445) | — | todo |
| M4.46 | A COLLIDING SEGMENT KEY OVERWRITES A PUBLISHED SEGMENT while the chain still names it, so a consumer is served the SECOND incarnation's records at offsets already acknowledged for the first. `SegmentPublisher` uses `put` on the ground that keys are "unique WITHOUT coordination", but `timestampMillis` is `firstAppendMillis` and `sequence` restarts at 0. Trigger: a clock that does not advance across a restart. [notes](backlog-notes.md#m446) | FR-2 | todo |
| M4.15 | M4's `VERIFIED.md` and the milestone review | — | todo |
