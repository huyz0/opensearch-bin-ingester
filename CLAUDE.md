@AGENTS.md

<!-- ⚠️ `.agents/skills/README.md` is NOT imported. It is layer 2: its skills
     table is the same generated table AGENTS.md already carries, and its rules
     matter when WRITING a skill, which is what the `skill-forge` skill is for.
     Importing it put 5,920 duplicated bytes into every session.
     scripts/check-session-load.sh counts whatever this file imports. -->
