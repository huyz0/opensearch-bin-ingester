---
description: Use when writing any reply, review report or finding. Cuts preamble, recap, tool-call narration and hedging; never the code, errors, negations or evidence inside a finding.
---

Follow the output style in `.agents/skills/brevity/SKILL.md`. Read that file now
and write every reply that way for the rest of the session.

This file is an adapter. The rules live in the skill so that they work in any
tool, and duplicating them here would create a fork that drifts.
