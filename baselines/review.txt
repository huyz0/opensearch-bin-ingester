# Review findings argued as non-defects. One per line:
#   <role>:<finding-id>  <why it is not a defect>
#
# The key carries the role -- `reviewer:R3` or `test-reviewer:R3` -- because
# the two reviewers number their findings independently, and a bare id staged
# for one of them used to silence the other's finding of the same number.
#
# An entry must be STAGED to count: an unstaged one suppresses a finding while
# leaving no trace of it. A growing list is itself a signal.
reviewer:M0.31-R3-1  Rule 12's remedy is 'split it'. The diff is check-gate-scope.sh, its test, the backlog row and this entry; splitting would put the negation test in a different commit from the two lines it constrains. The round count came from the subtlety of git's ignore semantics -- five wrong forms, each found by a reviewer running the gate rather than reading it -- not from commit size, which is what the rule's own M-1.1 precedent measures. Rounds 1-2 each found a real defect; round 3 found none, so the cap's purpose was served rather than evaded. Argued, not fixed, because the finding is about process rather than the change.
rounds:M4.21  Fourteen rounds against the cap of two, and this argues rather than splits because the diff cannot be decomposed: check-reviewed.sh, review_rounds.py, review.md and the two ReviewRoundCapTest files are one mechanism and its test, and a partial commit would ship an escape hatch either untested or undocumented. Rounds 1-2 found the original two remedy-message defects the task exists to fix. Rounds 3-13 each found a real defect in the fix for the previous round's defect -- two production bypasses (file-scoping, no-task verdicts, both with working exploits and now-closed by ReviewRoundCapArgumentValidityTest), one production gap (key anchoring, closed by ReviewRoundCapTest), and a long tail of prose making a claim the tree did not support, including twice by me: a measured number moved in the weakening direction and then mis-reported as fixed (round 6/7/8/9), and a wrong claim made unfalsifiable by deleting its number rather than correcting its mechanism (round 11/12). Round 14 passed both roles clean. This entry is itself the dogfood: it is the mechanism M4.21 built, used on the commit that built it, which is the reason this argues instead of skipping -- SKIP=check-reviewed would disable both-roles-present and no-unresolved-majors, and nine-plus M4 commits already used it silently. An argued entry stays in the tree, reviewed with the commit it excuses.
rounds:M0.53  Many rounds against the cap of two -- the count is left to `review_rounds.py`, because three drafts of this argument and of the backlog row carried one by hand and every one was stale by the next round -- argued rather than split, and the argument is about the DEFECT CLASS rather than the size. Every round but the first found the same shape of bug: a predicate asked of one end of a rename but not the other. Round 3 found that EVERY fixture pinned `git config diff.renames false`, so two fixes had been verified against a configuration nobody runs. Round 4 measured a WEAKENING relative to HEAD -- `git mv FooTest.java FooTest.java.bak` passed where HEAD refused -- because three subtly different path predicates were asked where the loop asks a fourth. Round 5 measured a SECOND weakening, in the other scanner: `tdd_scan` followed a rename from a source it had never gated, so adding `docs/drafts/FooTest.java` and then moving it into the test tier subtracted its ids from `fresh` and demanded no red record at all -- a two-commit, green-tree bypass of non-negotiable 3. Both scanners now ask ONE `reads()` predicate of BOTH ends. Round 6 put the question to ~45 differential fixtures -- both scanners, four `diff.renames` settings, both `--cached` and `CHECK_RANGE`, each run through HEAD's gate and this one -- and found no remaining input where this change is weaker than HEAD; its three majors were all outside the scanner logic. ⚠️ THIS ENTRY DOES NOT CLAIM CONVERGENCE, and an earlier draft of it did: it said each fix was smaller and in the same place than the last, and round 5 falsified that by finding a major in a file four rounds had already read. What is claimed is narrower -- that the remaining surface is one predicate now written once per scanner, and that a reader can check that claim by grepping `TEST_PATH.search`, whose only matches are the two `reads` definitions. ⚠️ AND THAT CLAIM WAS FALSE WHEN FIRST WRITTEN: round 6 ran the grep and found a third, spelled-out copy still live in `test_integrity`'s loop, which is exactly the stale-second-copy this entry says has been eliminated. It is folded into `reads` now, so the prescribed check returns the prescribed answer. ⚠️ SPLITTING WOULD SHIP THIS DEFECT CLASS RATHER THAN AVOID IT. The round-4 reviewer proposed three commits (rename-following / tier departure / package re-qualification); the round-5 reviewer, having measured the second weakening, disagreed and said so. Both defects lived in the RELATIONSHIP between a rename's two ends, and the two scanners share `rename_map`, `package_of` and `rekey` -- so a split puts the predicate in one commit and the call sites that make it correct in others, which is precisely the disagreement-between-predicates that rounds 4 and 5 both found. What no round has closed on is a `minor`; the eleven-round precedent rule 12 cites had rounds 9-11 doing exactly that.
rounds:M4.7a  Past the cap of two -- the count is left to `review_rounds.py`, because a count carried by hand in prose is stale by the round after it. Argued rather than split because the wire-format-change skill FORBIDS the split: "one commit changes the format, every reader, every writer, the fakes, the golden files, and the ADR", on the ground that a format change split across commits leaves the tree able to write bytes it cannot read. The diff is exactly that set and nothing else. ⚠️ EVERY ROUND FOUND A REAL DEFECT IN CODE, none in process and none in prose-about-review -- which is the distinction M0.53's row draws and the reason that task's tail was waste and this one's was not. Round 1: `SubscriptionHub` was updated for the KEY and not for the BYTES, so each run was paired with its own segment while every run kept the same `byte[]` -- and since `Push` carries the segment inline, that delivers another pod's records under the right label, silently. Round 2: the segment count became an allocation size, measured raising OutOfMemoryError past `recover`'s own `throws IOException`; and the count-bound fixture stopped at the count, so `remaining()` was 0 and the bound could not be told from an emptiness check. Round 3: the same allocation defect one field over, in the RUN count, reachable from the v0 path every bucket already holds -- fixed here rather than filed, because bounding one of two while a comment claimed the class was closed is worse than either. Round 4: passed both roles clean, no findings. ⚠️ THE FIXTURES WERE THE RECURRING WEAKNESS, not the format: every batched fixture was exactly two segments until round 3, so writing the count as a literal `2` survived three module suites, and both bound fixtures had zero trailing bytes. Arity and slack are now varied deliberately.
rounds:M4.7  Past the cap of two, argued rather than split because the subject is a THREAD and its lifecycle: a batcher whose window logic lands in one commit and whose failure handling lands in another ships a class that can hang, which is the defect every round here found. ⚠️ EVERY ROUND FOUND A REAL DEFECT IN CODE, and rounds 1-3 each found a real HANG. It is NOT true that none found a scope problem, and an earlier draft of this entry claimed that: the response to a prior round WAS a scope split, carving M4.7c out to wire the batcher into the ingest path, which the backlog records with its own blocker (`DefaultIngest` at 503 lines, and the reduction being cross-pod and therefore M5). Round 6's production reviewer caught this entry and the backlog telling different stories about that. Round 1: two BLOCKING defects, each measured rather than argued -- an unchecked throw from the public `WindowTimer` seam killed the committer while the batcher kept accepting, and `Pending.await` is `CompletableFuture.join`, which is UNINTERRUPTIBLE, so the caller could not even be interrupted; and a request enqueued around `close` landed in neither the queue nor a batch, reproduced within 102 attempts at 24 callers. Also that `commitAll` was a pass-through, which both restored the per-pod PUT rate on the interface's own PRIMITIVE and ran the delegate on the caller's thread against a single-writer `CommitLog` -- I2, uncatchable by any conditional write. Round 2: the first fix guarded the timer path only, so an ERROR from the delegate still killed the committer -- and that is not theoretical, since batching exists to make batches big and `CommitLog` encodes the delta TWICE per attempt. Round 3: `committer.join` throws immediately when the CLOSING thread already carries an interrupt, so the wait was skipped entirely and the lease was released under an in-flight commit; measured at `closeTookMs=0`. ⚠️ THE TESTS WERE WEAKER THAN THE CODE THREE TIMES, which is the other half of the round count: every batch test ran on an empty uncontended log, the attribution test was invariant under every permutation of segment-to-runs, and the queued callers -- as opposed to the one in flight -- were never exercised at all. ⚠️ TWO TESTS WERE DELETED RATHER THAN KEPT: a stress race test that caught its defect in 2 runs of 8, and a seam-based one whose seam sat before the check it meant to interleave with. A detector that misses more often than it fires cannot be red-recorded, and a production member existing for a test that does not work is worse than no test -- both are recorded in place. ⚠️ ROUND 6 FOUND THREE MORE, TWO OF THEM NEW PRODUCTION DEFECTS, so THIS ENTRY DOES NOT CLAIM CONVERGENCE -- an earlier draft's narrative stopped at round 3 and read as though it had, which round 6's test reviewer named. (a) The interval GUARD and the window TIMER asked different questions: the guard asks the `Duration`, the timer slept `toMillis()`, which TRUNCATES, so every interval in (0 ms, 1 ms) passed a check reading "must be positive" and then installed `Thread.sleep(0)` -- no window at all, which is the exact state `Duration.ZERO` is refused for, reached by a value the guard accepts. Measured at 98 PUTs against 7. (b) The committer's exit path handled the callers' futures and NOTHING else: it logged nothing at any level, told every later caller "this sequencer is closed" when nobody had called close, and never closed the delegate -- so `LocalSequencer` never reached `leases.release()` and the pod held a lease it could no longer commit through until the TTL. (c) A comment block contradicted the one 25 lines above it, claiming a deterministic test pinned a property that the earlier block correctly says is NOT pinned and whose seam was deleted; the stale block is gone. ⚠️ ROUND 6 ALSO CLOSED THE TWO MAJORS ROUND 5 RAISED AND THIS ENTRY HAD BEEN SILENT ON, which is the more damning half: the REAL sequencer's batched path had no test at all -- `log.commitAll(requests)` could be `log.commitAll(List.of(requests.getFirst()))` with the whole suite green, silently dropping every pod but one from a batch that is ONE conditional PUT -- and `close`'s interrupt-restore ORDERING was unpinned because every fixture's delegate had an interrupt-insensitive `close`. Both now have tests, red-recorded against exactly those mutations. ⚠️ `BatchingSequencer` REACHED 509 LINES fixing (b) and was SPLIT, not exempted: `PendingCommit` is now its own file, which is code-structure.md rule 1's own remedy ("split it; never raise the limit"). ⚠️ ROUND 7 FOUND TWO MORE MAJORS, BOTH CREATED BY ROUND 6'S OWN FIX, which is this task's recurring shape and the reason the count keeps climbing: the exit path added in (b) was written to do what `close` does and omitted BOTH of the protections `close` documents at length, neither omission visible to any of the new tests. (d) `closeDelegateQuietly` released the lease on a thread STILL CARRYING AN INTERRUPT -- `drainLoop`'s interrupt branch re-arms the flag before returning the cause, and `leases.release()` reaches `BoundedLock.takeOrFail`, whose `tryLock(bound, NANOSECONDS)` throws immediately when the flag is set. Measured `leaseReleased=false` while the new ERROR log announced the lease WAS being released so another node could take over: the fix printed a claim the tree did not support, which is the defect class this file exists to record. (e) The release was ordered BEFORE the queue was failed, inserting unbounded store I/O between the door shutting and the callers being freed, against `close`'s own documented order -- measured with a queued caller parked three seconds, and never completing at all when the close threw an Error, because `catch (Exception)` missed it and the `finally` aborted before `failAll`. Both are fixed by clearing the flag before the release, failing the queue first, and catching `Throwable`. ⚠️ AND THE FIXTURE WEAKNESS THAT HID (d) IS THE ONE ROUND 6 HAD JUST CLOSED ONE METHOD OVER: every delegate's `close` was interrupt-INSENSITIVE, so the hazard was unreachable from any test -- round 6 fixed exactly that for `close()` and did not think to ask it of the exit path it was adding in the same commit. ⚠️ A MINOR IS RECORDED RATHER THAN ENGINEERED AWAY: when the committer's own release fails, `close` skips the delegate and returns cleanly, so the operator learns it from the ERROR log and the closing caller does not learn it at all. The comment now says so instead of claiming close propagates it. ⚠️ ROUND 7'S TEST REVIEW PASSED with four minors and confirmed by mutation that both of round 6's carried majors are genuinely closed. TWO OF THE FOUR WERE FIXED RATHER THAN RECORDED, against review.md's default, because one was not really cosmetic: the `sleepFor` seam was pinned in both directions but the PUBLIC CONSTRUCTOR's wiring to it was not, so `sleepFor(interval.multipliedBy(4))` -- and, worse, installing a constant `() -> Thread.sleep(200)` while still calling `sleepFor(interval)` for its guard -- left the tree green. That second one turns a configured 5 s interval into a 200 ms window, 25x the PUT rate, which is the NFR-1 dimension this whole class exists to protect, so it is a cost defect wearing a minor's clothes. `theRealClockTimerActuallyWaitsItsInterval` now bounds BOTH sides at 300 ms. The second fix pins the once-only delegate close: no test had ever called `close()` on a batcher whose committer had already died, so `claimDelegateClose` could return true unconditionally and release the lease TWICE with everything green. ⚠️ A COMMENT OF MINE WAS THE ONLY OVERSTATEMENT THE ROUND FOUND, and it is corrected rather than deleted: the sub-millisecond test claimed that together with the real-clock test it pinned "the configured interval is actually consulted", which was false, because `sleepFor` is called there DIRECTLY and both its bounds bypass the constructor. ⚠️ TWO MINORS ARE RECORDED AND NOT FIXED: only one of the two drain nets is pinned, because the state where `close`'s own net does work is the join-timeout branch and the 10 s timeout is a hardcoded constant rather than a seam -- which also leaves its WARNING undetectable; and `awaitSet` polls where a latch would be exact. The first is a design signal about that constant and belongs in a row, not in this commit.
rounds:M4.38  Past the cap of two, and argued rather than split because SIZE IS NOT WHAT DROVE THE ROUNDS. The diff is 17 inserted lines across three docs files and changes no code at all. Splitting it means one backlog row per commit, three commits, six reviews -- and it would not have prevented a single finding, because every round found a defect in PROSE, in the same three paragraphs, which a split puts in front of the same reviewers in smaller pieces. The three rows are also one act: they file the findings of ONE landed commit, and the M4.38 done-row that records the filing is only coherent beside the rows it names. ⚠️ WHAT ACTUALLY DROVE THE ROUNDS WAS MY OWN DRAFTING, and that is worth recording because the cap is otherwise being told the wrong story. Round 1: M4.36 asserted a shared root cause (`Duration.ofSeconds(10)` not being a seam) that fixes neither defect it names. Round 2: the test recipe I had written into M4.36 pins NOTHING, because `close()` interrupts the committer before it joins, so the latch-based pin is broken by that very interrupt -- measured both ways, `CountDownLatch` survives and `Semaphore.acquireUninterruptibly` kills. Round 3: M4.35's claim that the release's store write dies with `ClosedByInterruptException` is FALSE -- the release issues `putIfMatch`, which opens no channel and completes with the flag still set, while only `putIfAbsent`, the ACQUIRE path, throws; both reviewers measured it independently. ⚠️ ROUNDS 2 AND 3 WERE THE SAME ERROR TWICE: I promoted a reviewer's UNMEASURED structural reasoning into a row as flat fact. A review comment is a hypothesis; a backlog row is a prescription a future session acts on, and the measurement belongs between them. The second instance came two hours after I had written up the lesson from the first, on the adjacent sentence. ⚠️ THREE CLAUSE-ATTACHMENT ERRORS, twice in sentences written to fix the previous one; the remedy applied was shorter sentences, and round 4 shows it was only partly effective, since "a latch would serve there" is still false and both reviewers caught it. ⚠️ THIS ENTRY CLAIMS NO CONVERGENCE. Round 4 passed both roles with four minors, all prose, all recorded in the commit body rather than fixed -- which is what review.md prescribes and what I should have done after round 2 instead of redrafting three more times.
M4.17-R1a  ARGUED, NOT FIXED, and filed as M4.39. The finding is correct and important: this tick makes ADR-0027's accepted false-empty reachable in production for the first time, and turns its cost from one TTL into the process lifetime, because `fenced` is a one-way latch and nothing re-runs `start` in M4. ⚠️ IT IS NOT FIXABLE INSIDE THIS TASK, which is the whole argument. Fencing on empty is mandated by `renew`'s own contract -- empty is how a holder learns it is fenced and it must stop immediately -- and from inside, a false empty is indistinguishable from a true one, so NOT fencing trades a liveness bug for an I2 safety bug. Bounding it requires `tryAcquire`, i.e. `start`'s acquire-seal-open, which is the supervisor tick the M4 SPEC and this file's own javadoc both assign to M5. ⚠️ WHAT THIS COMMIT ACTUALLY TRADES, said plainly rather than buried: a GUARANTEED failover every TTL per pod, for a RARE permanent stop that needs an ambiguous store write to trigger. That is a net improvement and it is also a new failure mode, and both halves are true. M4.39 carries the obligation to M5 and names the amended quantity ADR-0027's successor must state.
rounds:M4.17  Past the cap of two, argued rather than split because THE TICK AND THE FENCE ARE ONE BEHAVIOUR. A commit that adds a renew tick without acting on an empty result ships something strictly WORSE than no tick: the node renews, is told it has been fenced, ignores it, and keeps committing. ⚠️ THE WINDOW IS BEFORE THE SEAL LANDS, not after: once a successor has sealed, `CommitLog` already refuses, and this task's own test comment records a draft passing against a renewer-less tree for exactly that reason. The load-bearing gap is after the successor takes the LEASE and before its seal is written, where nothing else stops the old leader -- I2, bought for the sake of a smaller diff. `renew`'s own contract says an empty result is how a holder learns it is fenced and that it must stop immediately, so the two cannot land in separate commits and leave the tree correct in between. ⚠️ AND THE ROUNDS WERE NOT ABOUT SIZE. Round 1 found one design major and one test major; round 2 found that my FIX for the test major pinned the seam and not the wiring. Every one of those is a question about whether the tests constrain the code, which a split reproduces inside each piece rather than removing. ⚠️ THE SAME MISTAKE THREE TIMES, and it is mine rather than the task's: `sleepFor` was made package-private so a test could reach it, and a direct call never builds a `LeaseConfig`, so `sleepFor(leases.renewInterval())` could become `sleepFor(Duration.ofDays(1))` with the whole build green. That is verbatim the overstatement this file already records against `BatchingSequencer` -- "both its bounds bypass the constructor" -- reproduced one class over, with that lesson already committed and in front of me. Round 2 also showed one-sided bounds let `multipliedBy(4)` and a constant `sleep(200)` through; the wiring is now pinned through the PUBLIC `start`, two-sided, with a second sequencer at a different interval. ⚠️ NOT "no single mutation satisfies both", which an earlier draft claimed and round 3 measured false: `multipliedBy(2)` landed exactly on an inclusive floor of 3 and survived. The floor is 4 now, and the surviving band is stated in the test rather than denied here. ⚠️ A METHOD FAILURE IS RECORDED IN M4.40 because it caused more waste than any finding: several conclusions came from `./gradlew --tests X` printing BUILD SUCCESSFUL for a task Gradle had skipped as up-to-date -- a three-second test "passing" in 941ms -- while `tdd-red.sh`, which reads the result XML, had already refused to record that test twice. The tool was right and I disbelieved it. ⚠️ THIS ENTRY CLAIMS NO CONVERGENCE: round 2's test review found a major my round-1 fix had left open, and its remedy was measured by the reviewer rather than by me.
rounds:M4.43  Past the cap of two, argued rather than split because SIZE IS NOT WHAT DROVE THE ROUNDS. The diff is 17 lines across three docs files and changes no code. Splitting into one row per commit means three commits and six reviews, and would not have prevented a single finding: every one was prose about the same four minors of ONE landed commit, and the three filed rows plus the done-row that records the filing are only coherent together -- the done-row's whole subject is which of the four went where. ⚠️ WHAT DROVE THE ROUNDS WAS MY OWN DRAFTING, in two shapes, both of which then RECURRED INSIDE THE ROWS WRITTEN TO DOCUMENT THEM. Findings, not numbered by round because an earlier draft of this entry got the round order wrong and used "round 3" for two different hashes: the row filed "the two minors" when the commit recorded FOUR, losing the drainPermits one entirely -- the exact outcome the filing exists to prevent; M4.42 claimed one surviving duplicate when a second stands in PRODUCTION, on the branch that latches the fence; M4.41's remedy gave "honest ~13, mutant ~6" as measured when both were the 1000ms figures DOUBLED, and as written would have gone red on a correct tree, which the test reviewer reproduced as `3L to be less than 3L`; and the permanent copy still carried "the mutant is 7, not 6", itself one mode of a bimodal distribution, plus "BOTH ROWS" after three were filed. ⚠️ THE MEASUREMENT, ATTRIBUTED PER ROLE AND ONLY AS THE RECORD SUPPORTS IT -- an earlier draft of this sentence credited the test reviewer's series to production, invented a run count for it, and called the whole thing SETTLED, which is a non-negotiable-4 claim in the one file whose worth is that its entries are checkable, inside the commit filing three rows about numbers reported as measurements. THE TEST REVIEWER measured the 2x mutant killed in 3 of 7 runs (~43%: five class-scoped plus two full module runs), bimodal at 3/4, honest never below 6 including at load average 123. THE PRODUCTION REVIEWER re-measured independently, recording `fast=6, 7, 7` for the 2x mutant at a 2000ms window. ⚠️ AND THE TWO ROLES DISAGREED AT ONE HASH AND WERE NEVER RECONCILED, which is what the record supports and is more useful than the retraction an earlier draft of this sentence asserted: at `e8a3151c` the test verdict (05:38) says the floor of 4 kills `multipliedBy(2)` about 40% of the time, and the production verdict (05:40) says the floor kills it with 1.75x slack. The second was recorded two minutes after the measurement contradicting it and stands uncorrected. ⚠️ THAT DRAFT ALSO APPLIED THIS ENTRY'S OWN STANDARD UNEVENLY ONE CLAUSE APART: it dropped a twelve-run series because no verdict carries it, and kept a retraction no verdict carries either, inside the sentence promising to cite only what the record supports. M0.64 already files the general case -- a reviewer can report a verdict it never recorded -- and `baselines/review.txt` is read by sessions that have only `.harness`. ⚠️ THE RETRACTION THAT DID HAPPEN WAS THE TEST REVIEW'S, OF ITSELF, and it is the most useful fact in the series: that role proposed the floor of 4 from five runs all reading `fastRate=3`, then withdrew it from seven runs reading 4,3,4,4,3 plus two module-wide. An under-sampled reading corrected by the same reviewer that took more samples is exactly what the three rows this commit files are about, and an earlier draft credited it to the wrong role twice -- once by naming production, once by keeping the clause after the roles around it were fixed. My own "measures EXACTLY 3" was one run. ⚠️ THIS ENTRY CLAIMS NO CONVERGENCE, and states no round outcome, because a round fact carried by hand in prose is stale by the round after it -- `rounds:M4.7a` in this file says so and an earlier draft of this line proved it again. Findings kept arriving after each fix, and the two that arrived last were introduced by the fix before them and were both the same counting shape. They are recorded in the commit body rather than chased, which is what review.md prescribes and what I should have done earlier -- this is the SECOND commit this session where choosing to fix minors rather than record them produced the next round's findings.
rounds:M4.8  Past the cap of two, argued rather than split, and the argument is about WHAT THE ROUNDS FOUND. This is a decomposition: M4.8 marked `split` and three sub-rows written. Every finding across both rounds was a row PRESCRIBING A TEST THAT WOULD NOT DISCRIMINATE, which is the entire reason to review a decomposition before building it -- request-counting that deprecated the only assertion able to catch the key grammar; a >5,000-checkpoint bound that the chain-head walk the row itself lists passes with zero LISTs and 5,000 GETs; round-trip-plus-golden that a consistent field swap survives; K discriminators not mirrored for T. ⚠️ EVERY FIX WAS A SINGLE CLAUSE IN ONE ROW, which is itself the answer to "then split it": there is no smaller unit here than a row, and the rows are already the split. ⚠️ AND THE FINDING THAT MOST JUSTIFIES ONE COMMIT IS THE ONE ONLY VISIBLE ACROSS ALL THREE ROWS AT ONCE -- that the source row's first clause, the checkpoint's actual contents, had no acceptance criterion in a, b OR c, because M4.8a has no writer, M4.8c seeds keys by hand and M4.9 owns reading. Reviewing the rows in three separate commits is precisely the arrangement in which that gap is invisible. ⚠️ TWO CLAIMS OF MINE WERE FALSE AND ARE CORRECTED IN PLACE RATHER THAN SOFTENED: that this shape had made the round cap fire on every substantive task before it (`.harness` says M4.5 took one round and five others took two), and that `HashMap` iteration order is salt-randomised (it is `ImmutableCollections` that is; under a plain `HashMap` the mutation this row targets survives 100% of runs, not 1 in 720). ⚠️ THIS ENTRY CLAIMS NO CONVERGENCE.
rounds:M4.8a  Past the cap of two, argued rather than split, and the split is FORBIDDEN here: `wire-format-change` requires the record, the codec, the fakes, the golden file and the ADR in ONE commit, on the ground that a format spread across commits leaves the tree able to write bytes it cannot read. The diff is exactly that set. ⚠️ THE ROUNDS WERE NOT ABOUT SIZE, and the honest cause is two habits of mine. FIRST, I wrote five decode guards PRODUCTION-FIRST and only one had a test -- non-negotiable 3 inverted -- so review had to find the other four by mutation: a repeated podId last-winning, a `podLen` narrowing, a `partitionId` narrowing that ACCEPTED `0x1_0000_0001` as partition 1 and attributed one stream's offsets to another, and then the `< 0` half of all four bounds, where deleting it flips refuse into accept. SECOND, I introduced a FALSE CLAIM IN EVERY ROUND, each inside the fix for the previous one: the enumeration of routes reaching the decode wrap was wrong as "five", then wrong as "four" (it is six), and it now carries no count at all because nothing pins it and every value survived every mutation. ⚠️ WORST OF THE SET, and the reason this entry exists rather than a shorter one: `baselines/mutants.txt` shipped TWO FALSE EQUIVALENCE ARGUMENTS, claiming `streamCount < 0` and `podLen < 0` were message-only when both flip REFUSE into ACCEPT. A wrong equivalence argument is worse than an unargued survivor, because it launders a real defect as a known-benign one. Both are deleted and both halves are killed by test. ⚠️ WHAT THE ROUNDS BOUGHT, measured by the test review rather than by me: 45 of 51 mutants killed, 88.2%, with zero unargued survivors -- against 73.7% at round 3. ⚠️ THIS ENTRY CLAIMS NO CONVERGENCE.
rounds:M4.8b  Past the cap of two, argued rather than split, because THE THING BEING SPLIT IS THE SPLIT. This is a decomposition commit: M4.8b marked `split` and two sub-rows written. Splitting it again means one row per commit, and the finding that most justifies one commit is the one only visible across both rows at once -- that b2 landing before b1 is green in CI and permanently broken in production, which neither row stated until review found the clause missing. ⚠️ WHAT THE ROUNDS FOUND, named rather than counted, because a count in this file goes stale and an earlier draft of this entry proved it again -- it said "fifteen across two rounds" where `.harness` records 25 findings, 12 of them major or blocking, and M4.38's own row already records naming-not-counting as the remedy. MOST were rows prescribing tests that would not discriminate, which is the entire reason to review a decomposition before building it; the rest were a missing dependency clause, cost arithmetic wrong in both directions across three drafts, a stale cross-reference, a bound placed on the wrong quantity, and an ADR mischaracterisation. An earlier draft of this sentence called them all the first kind, which its own next clause contradicted. The two that matter most were both MY CLAIMS ABOUT THE CODE, not about the tests: `ChainReplay` does not decode every key under the log prefix -- it returns at `hop.upTo` and at the first `Seal` -- and that error had already propagated into ADR-0033 and the landed M4.8a row, where it made a plain takeover test look sufficient when the seal's key sorts before `ckpt/` and such a test never reaches the site at all. And the cost arithmetic was wrong in three successive drafts, in both directions. ⚠️ A NEW FAILURE MODE OF MINE IS RECORDED HERE because it is not the one earlier entries describe: TWICE I OVER-CORRECTED BY DELETION. Fixing the `applyChain` criterion deleted `chainEnd`'s coverage, leaving a fix applied to one site alone passing green; fixing the `seq` criterion deleted "distinct and strictly increasing" outright, leaving `seq = 0` passing everything. Both are restored in conditional form. ⚠️ AND A THIRD MODE, WHICH IS NEITHER: the REPLACEMENT for a deleted criterion asserted a code path it does not reach. "One fixture covers both" was false because `start` seals the ancestor BETWEEN the two calls, so the takeover that gives `chainEnd` a fixture destroys `applyChain`'s -- the fix looked like it restored coverage and did not. A fourth: correcting an over-specific criterion produced one both WEAKER and sometimes FALSE -- "the two fields differ" admits `(next, next - 1)` and fails legitimately for a stream at offset 0. The lesson is narrower than "check your claims": when a criterion is wrong, condition it rather than removing it, and check the replacement against the code path it names. ⚠️ THIS ENTRY CLAIMS NO CONVERGENCE -- round 2 returned two majors and both were defects in round 1's fixes.
rounds:M4.8b1  Past the cap of two, argued rather than split. NO ROUND FAULTED WHAT `isEntryKey` DECIDES -- true across every recorded verdict -- and the rounds went instead to tests that did not constrain it and to my prose about it. ⚠️ ROUND AFTER ROUND MEASURED A SURVIVING MUTANT THE PREVIOUS ROUND'S TESTS COULD NOT REACH, all in the same twelve-line method, NAMED RATHER THAN COUNTED: the canonical round trip, unconstrained because every end-to-end interloper is refused by an earlier guard; both length guards, deletable with the suite green; `startsWith(logPrefix)` narrowed to `startsWith(prefix)`; and `endsWith(SUFFIX)` narrowed to any suffix of `.delta`. ⚠️ THE LAST TWO ARE ONE SHAPE -- a guard pinned against DELETION but not against its OPERAND -- and the second was found only after the first was fixed, which is the argument for reading a survivor list as a hypothesis rather than an inventory. ⚠️ TWO EARLIER DRAFTS OF THIS ENTRY WERE THEMSELVES WRONG, in the same direction, which is why it now names artifacts a reader can check against `.harness/review`: the first dated the round trip to round 2 and called every later round prose-only, when round 1 carries it from both roles alongside the dead `CommitLog.isEntryKey` delegate; the second gave a count that was already stale when written, inside a clause asserting that no count is given. ⚠️ AND ONE MODE THAT IS NOT PROSE AT ALL: A MEASUREMENT READ WRONGLY. A narrowing of the prefix guard was written up as killed on the strength of a real twelve-test failure, when the kill came from hardcoding a prefix literal that most chains here do not use; the operand-only narrowing survives, and both reviewers measured it green. THE TELL WAS IN THE FAILURE LIST -- the assertion that broke was a POSITIVE control, "the epoch DOES see its own chain", while both cross-epoch assertions passed. A kill whose positive control fails is not evidence of the discrimination being tested for. Worse than not measuring, because it produced a confident false entry in `baselines/mutants.txt`, the one file whose header exists to warn about exactly that. It is now KILLED by test rather than argued, which is also how this commit already treated the same hazard for the guard deletions. ⚠️ SPLITTING WOULD NOT HAVE HELPED AND WOULD COST CORRECTNESS. The change is one predicate, its two call sites and the tests that pin it; the only seam available is landing the code and correcting its comment afterwards, which means knowingly committing a false comment -- and that comment is the one telling a future session which guard actually protects the walk. The `LogKeys` extraction is not scope creep either but the 500-line gate refusing the file, per code-structure.md rule 1, argued in the commit body.
rounds:M4.8b2  Past the cap of two, argued rather than split, and the reason is the OPPOSITE of M4.8b1's: there the code was right from round 1 and my prose was wrong; here the CODE was wrong, repeatedly, and review is what found it. Named rather than counted: a DATA RACE -- the ticker thread read `CommitLog.nextSequence()` and `offsets()` while the commit thread mutated them, and `CommitLog` carries no synchronization at all, so a skewed pair makes M4.9 resume at N with delta N-1 missing and every stream in it rewinds; a failed PUT leaving the delta counter at K, so every later commit retried inline at a thousandfold the intended rate exactly while the store was unhealthy; an IllegalArgumentException escaping `start` AFTER the renewer thread had begun, stranding a held and renewed lease so no node could take that term again; and a fenced node keeping a live writer able to PUT into the chain's own prefix. ⚠️ THEN MY FIXES REINTRODUCED WHAT THEY FIXED, which is the part worth carrying. Wrapping `observe` in `catch (Throwable)` left the counter reset in `catch (IOException)` alone, so an UNCHECKED failure took the identical rate defect down the sibling path -- measured at seven attempted PUTs for nine deltas at K=3 where three are owed. The same wrap made the capture order load-bearing, and it was wrong: `pendingSequence` was assigned before `pendingStreams`, which is the very skew the capture exists to prevent. Moving the reset into a `finally` then left `encode` OUTSIDE the try while the comment claimed every exit. Each fix was correct about the defect it named and blind to the one it created next door. ⚠️ AND FOUR TIMES A CORRECT DECISION CARRIED AN INVENTED REASON -- the dirty flag argued from NFR-2, which governs CONSUMERS; `putIfAbsent` argued as stopping a fenced leader overwriting its successor, which the epoch-in-key makes impossible; `CommitLog.offsets()`'s copy argued as guarding a cross-thread read the same commit removed, and which a HashMap copy could not guard anyway; and the NFR-2 argument left standing in a TEST javadoc after production retracted it. Every decision survived review; no reason did. ⚠️ A ROW THAT CATALOGUES AN ERROR IS NOT A ROW THAT FIXES IT: review found the retracted `putIfAbsent` rationale still asserted at its own site in `backlog-done.md`, three thousand characters above the bullet naming that exact instance as invented. Corrected AT THE SITE. ⚠️ SPLITTING WOULD LEAVE DEAD CODE ON BOTH SIDES. The writer, its two triggers and its wiring are one behaviour: a writer with no trigger is a class nothing calls, and a trigger with no writer has nothing to call. The seam that does exist -- the checkpoint RECORD -- was already split out as M4.8a and landed separately, and the recovery FILTER this depends on as M4.8b1.
rounds:M4.8c  Past the cap of two, argued rather than split. EVERY DECISION SURVIVED REVIEW AND SEVERAL OF THEIR REASONS DID NOT, which is the same shape `rounds:M4.8b2` records. The pointer, the body-not-sequence choice, `stat`-before-`get`, the lease rejection and two PUTs per trigger were all checked against the sources and all stand; but the per-ancestor cost clause and the ground given for the second PUT were both wrong and were corrected in rounds 2 and 3. An earlier draft of this entry said the cost arguments survived untouched, which was the flattering version. What the rounds found was that the DEFINING PROPERTY WAS PINNED BY NOTHING, in four independent ways, each measured green: `put` weakened to `putIfAbsent` froze the pointer at each epoch's first checkpoint while every cost assertion stayed green and M4.9 silently replayed the whole term; a pointer PUT per STREAM was invisible because the fixture's helpers filter `.ckpt` and the pointer ends `/LATEST`; the READER's epoch was hardcodable; and the pointer aimed at a CANONICAL ENTRY KEY survived everything, which is the M4.8b1 outage rebuilt out of the key this task adds. ⚠️ ONE MUTANT FAMILY APPEARED THREE TIMES IN THREE COMMITS -- a hardcoded epoch -- and I fixed each instance without checking its mirror: M4.8b2's review caught it on the checkpoint KEY, round 1 here caught it on the READER, and round 2 caught it on the WRITER'S POINTER, which the epoch test already in the tree could not see because it asserts on a `.ckpt`-filtered accessor. The lesson is narrower than "check the epoch": WHEN A MUTANT IS FOUND, FIND EVERY SITE OF ITS SHAPE, not the one reported. ⚠️ AND ONE PARAGRAPH OF MINE WOULD HAVE MISDIRECTED M4.9. It said discovery answers for the recovering leader's own chain and that a chain-wide answer is unnecessary. A leader acquires a FRESH epoch, so its own chain is empty at recovery and discovery returns empty at EVERY takeover; M4.9's row requires the CROSSING to be bounded; and a `Checkpoint` IS offsets plus a sequence, so a predecessor's is exactly what bounds it. The per-ancestor `stat` I dismissed as a cost replaces one GET per entry of every ancestor term. ⚠️ A LANDMINE FOR M4.13 WAS DEFUSED HERE rather than at the completion condition: `Invariants.readChain` decoded every key under the log prefix, and the simulation is green only because checkpointing never triggers in it. ⚠️ MY OWN NEW TEST CAUGHT MY OWN MALFORMED FIXTURE -- a chain committed to without being opened -- which is the invariant checker doing its job, and the reason to route a fixture through it rather than hand-build one. ⚠️ AND THE POINTER-FAILURE TEST FIRST USED A COMMIT WHERE IT NEEDED A TICK, reproducing an insufficiency the test reviewer had already taught me on the sibling path one commit earlier: a second commit re-arms the dirty flag by itself. ⚠️ SPLITTING WOULD SEPARATE THE ADR FROM THE MECHANISM IT RECORDS, which is the failure M4.0's rounds produced three times -- a mechanism chosen in a document and not built, or built and not recorded. The pointer, its maintenance by the writer, the reader, and ADR-0034 are one decision; the row asks for the mechanism AND the ADR in the same breath.
rounds:M4.9  Past the cap of two, argued rather than split. THE MECHANISM SURVIVED EVERY ROUND -- a checkpoint terminates the ancestry walk because its offsets are CUMULATIVE -- and review verified that induction against `CheckpointWriter` rather than accepting it: the writer snapshots `log.offsets()` after `open()` has merged the inheritance, so no checkpoint can predate what it inherited. What the rounds found was everything AROUND the mechanism. ⚠️ THE BOUND DID NOTHING ON THE ONLY PATH THAT MATTERS, and my own failing cost tests caught it: `LocalSequencer.start` calls `recover()` BEFORE `open()`, so the walk that reads the ancestry is `inherited()`, which STARTS at the predecessor -- probing only epochs reached by following a CONTINUE skipped exactly that one. ⚠️ HALF THE BOUND WAS UNTESTED: "plus at most K DELTAS" was invisible at K=1, where the newest checkpoint covers the whole chain, so a reader that skipped the tail entirely still answered correctly. So was the IN-LOOP termination, because every fixture checkpointed every term and the first-hop cross always won. And the barrier was unpinned in BOTH directions -- 9,999 admits every plausible off-by-one; it is now pinned at barrier+1 and barrier+2. ⚠️ MY TESTS WERE WEAKER THAN I CLAIMED TWICE, THE SAME WAY: offsets in a `RunCommit` are ABSOLUTE, not incremental, so replaying only the NEWEST ancestor already yields the final offset for any stream it touched. At the TERM level a mutant that stopped after one ancestor passed; I fixed that, wrote the lesson into the fixture's own javadoc, and then repeated it at the DELTA level inside the test written to close it -- where reading only the LAST uncovered delta passed. A lesson recorded at one granularity is not a lesson applied at the next. ⚠️ AND I CARRIED STALE NUMBERS UNDER THE WORD MEASURED, which is the failure THIS ROW'S OWN TEXT warns about, having already caught one draft doing it: the before-series came from the backlog row and describes an older tree, and its stat series was the LIST series written twice. Re-measured on one harness: 29 at one prior term and 284 at sixteen become 17 and 17. The correction makes the bound look BETTER, which is exactly why it needed checking rather than accepting. ⚠️ THE BUDGET MOVE NEEDED AN ADR, not a code comment: cost.md R18 says so, and M4.6e had already relaxed these same four assertions naming M4.9 as the reason -- this row was the promissory note, cashing it by moving them again. ADR-0035 carries the numbers, and records that two drafts of its rejected alternative cited the wrong evidence. ⚠️ SPLITTING WOULD SEPARATE THE BOUND FROM THE NUMBERS THAT JUSTIFY IT. The ADR must land with the measurement it records and with the code that produces it; a budget move documented in one commit and made in another is the shape cost.md R18 exists to prevent. The three file splits inside this commit -- `ChainEnd`, `CheckpointCursor`, and the cost/correctness suites -- were forced by the 500-line gate, on seams each file's own javadoc already declared.
rounds:M0.76  Past the cap of four, argued rather than split, and the reason is ONE failure of mine repeated in every round: A NUMBER I HAD NOT MEASURED. Round 2 said "~19 files", read off a truncated `head`. Round 3 said the archive's largest row was 11,481 -- the SECOND largest -- and gave 120 rows, the archive minus exactly the row it had missed, while `check-session-load.sh:9` had carried 18,845 in its own header all along. Round 4 said the withdrawn gate "missed 13 of 51 instances to line wrapping"; measured on both axes, joining lines finds ZERO extra and the entire 38-to-51 gap is CASE, which that gate's own `grep -Ei` already caught. The real wrap-only set is 11 of 61 and is a different set. ⚠️ FOUR WRONG NUMBERS IN A COMMIT WHOSE SUBJECT IS BOUNDING UNCHECKED PROSE, and the M0.77 row I wrote makes a point of "quote the method with the number" -- so each figure now carries the scope and the case-folding that produce it. ⚠️ THE ROUNDS ALSO KILLED A GATE: the withdrawn one flags `// every assertion here states a property, never a count.`, the remedy its own failure message demands, which is a fact about the rule and not about the regex. ⚠️ SPLITTING IS NOT AVAILABLE: what remains is one gate, its test and the rows recording why the second was withdrawn, and the withdrawal is only defensible WITH the measurement that killed it. ⚠️ THIS ENTRY CLAIMS NO CONVERGENCE -- round 4's major was a defect in round 3's fix, twice over. ⚠️ ROUND 5 FOUND THE SAME FAILURE IN TWO NEW FORMS, both worth more than the commit: a fabricated quotation I reported as removed SURVIVED MY OWN GREP BECAUSE IT WRAPPED across two comment lines -- the precise blindness that withdrew the second gate, reproduced inside the comment describing it -- and M0.77 attributed its numbers to "the withdrawn gate's own pattern" when that pattern is in NO COMMITTED OBJECT, the script having been withdrawn before it ever landed. Variations of scope, case and joining give 38, 47, 50, 51, 61 and 62 over this tree, so an unquoted method leaves the number unrecoverable; the regex is now written into the notes. ⚠️ AND ONE COUNT INVALIDATED ITSELF: "14 rounds: keys in the tree" becomes 15 as this commit lands, because it adds this entry.
rounds:M4.10a  Past the cap of two, argued rather than split, and ROUND 3 EXISTS ONLY BECAUSE OF PROSE -- the code passed both roles at round 2 and has not changed since. ⚠️ WHAT THE ROUNDS BOUGHT IS THE PIN, NOT THE FIX: the driver change was right in round 1, and both reviewers independently measured that it was UNPINNED. Uniqueness over `(podId, flushSeq)` is satisfied by a single fleet-wide per-attempt counter, so PER POD -- half the row's headline -- had no failing test; density 0..n-1 per pod is what kills it. And the `Issued` triple was built from sibling locals, so reverting only the request's `flushSeq` argument left the recording correct, the suite green, and the driver reissuing `(pod1, 0)` for two segments. Both were green against everything I had written. ⚠️ ROUND 3 WAS THREE FALSE CLAIMS OF MINE, all deleted rather than rewritten, because in M0.76 every round's fix produced the next round's finding and a deletion cannot add a claim: a method javadoc contradicting its own class javadoc, a backlog-notes section written in pre-fix present tense, and an overlap assertion described as a discriminator when it is an anti-vacuity guard. ⚠️ AND DELETING THE SECTION MISSED ITS DUPLICATE: the same facts sat inside the M4.10 row, which this commit falsifies and which is the next task a session picks -- the failure M4.8's own notes name, fixing one copy of a fact and missing its duplicates. ⚠️ SPLITTING IS NOT AVAILABLE: the driver fix and the test that pins it are one change, and the row exists because M4.10 cannot be tested until it lands.
rounds:M4.10  Past the cap of two, argued rather than split, and THE THING BEING SPLIT IS THE SPLIT: this is a decomposition commit carrying no code, so splitting it further means one row per commit. ⚠️ EVERY FINDING WAS A ROW ADMITTING A WRONG IMPLEMENTATION, which is the entire reason to review a decomposition before building it: eleven findings at round 1 across the two roles, six of them major. M4.10c hard-coded an answer to the ADR M4.10b exists to write, and the chain ALREADY carries a restart-surviving token (`SegmentKey`: timestampMillis, podShortId, per-pod sequence) that may void the whole row -- the most expensive item here, written as settled work before the deciding ADR existed. The key lives in TWO formats, not one. M4.10d's discriminator was a takeover when the defect is a POD RESTART, so dedup on the bare pair passed it. Skip-and-return satisfied "refuse" while turning a landed flush into a producer-visible failure. And the preserved source row landed under the WRONG ANCHOR still marked todo, so a session reading #m410d got M4.10b's job handed to it. ⚠️ ROUND 2 WAS A DEFECT I CREATED BY MERGING TWO FIXES INTO ONE SENTENCE: "BOTH formats change ... as a new KIND, not a version bump" is true only of the chain -- `Checkpoint` is not a `ChainEntry`, and ADR-0033 requires it never decode as one -- so the row forbade the version bump it needs and prescribed a kind byte that does not exist. The two mechanisms are now stated separately, on purpose. ⚠️ AND THE PRESCRIPTIONS STILL ADMITTED A NO-OP: record `lastAppliedFlushSeq`, checkpoint it, recover it, never consult it -- every named mutation killed, the parent row's actual harm still shipping. The assertion that catches it is on the OFFSETS, and every piece of guidance I had written pointed at the returned key instead. ⚠️ THIS ENTRY CLAIMS NO CONVERGENCE. ⚠️ WHAT THE BUDGET ACTUALLY BOUGHT, recorded because it is the finding rather than an excuse: the rows were answering the ADR's question. Every major in rounds 4 and 5, from both roles, was in prose prescribing a MECHANISM, and each was the fix for the previous round's -- "one GET and no growth" (there is no GET: the chain carries no pod attribution, and taking it reinstates the unbounded replay M4.9 removed), "zero I2 from Invariants" (measured: throws on the batched delta the same row mandates, and returns [] for the same segment key committed twice), "M4.10c is void" (the checkpoint's pods map changes regardless). None was in prose stating SCOPE. Round 6 cuts every mechanism guess and moves the end-to-end anchor OUT of the conditional row, because `Checkpoint.pods` already exists and a "no format change" finding would otherwise have taken the only anchor down with it. ⚠️ ROUNDS 7-9 WERE ONE HABIT, NAMED BY THE REVIEWER RATHER THAN BY ME: every fix added a REASON beside the correction, and the reason was the next round's finding. "Since `CommitDelta` carries no pod attribution" (false -- `SegmentKey` puts podShortId inside the segment key every `SegmentCommit` carries); a from-memory restatement of non-negotiable 6 that forbade the shipped architecture; then `cost.md R6` in its place, which is the INDEX axis -- pods appear on no rule's list, so no citation was available and the bullet now states the tree fact instead. ⚠️ AND THE LAST ONE WAS A REASON I WAS ASKED FOR: the uncheckpointed tail called a COST gap, when `ChainReplay.applyChain` already GETs every entry from the seeded checkpoint forward, so harvesting pod state there costs nothing -- and a chain missing `flushSeq` is an information gap by definition. Deleted rather than rewritten. The true reason, recorded here rather than lost: the fold exists because the checkpoint ABSORBS everything before it, so a replay older than the newest checkpoint has nothing left on the chain to read. ⚠️ WHAT CONVERGED, and it is the property rounds 4-5 lacked: the surviving assertions hold whichever mechanism M4.10b picks.
rounds:M0.79  Past the cap of two, argued rather than split, because THIS COMMIT IS ALREADY THE SPLIT: the brevity skill, its adapter and the two agent briefs arrived in the working tree from outside any task, `build-index.sh --check` reads the WORKTREE so it blocked every commit, and separating them from the M4.10b work in flight is what created this task. Splitting further means committing a skill without the index row that resolves it, or an index row pointing at a file the commit does not carry. ⚠️ ROUND 1 FOUND A CHANGE THAT COULD DEGRADE ITS OWN REVIEWERS, which is why both roles were asked to judge a rule binding them: the description named review reports IN scope while the body's out-of-scope list named "reports" and anything that "persists beyond the conversation", and a verdict is both -- so the reviewers pointed at the skill landed in the section telling them to write at natural length. And Never-cut protected a NUMBER but not the PROVENANCE of a claim, so "I did not run this; by inspection" was unprotected while the Hedging clause pushed to drop it -- non-negotiable 4 degraded by a style rule rather than by a bad reviewer. ⚠️ ROUNDS 2 AND 3 WERE MY OWN ERROR, NOT THE CHANGE: `git add -A` swept ADR-0036, M4.10b's 208-line deliverable, into the index. ONE sweep reached a reviewed hash -- staged tree `469567ea8610`, round 2 -- and both roles blocked on it; round 3 was spent re-reviewing after its removal, so the sweep cost two rounds. ⚠️ EXACTLY ONE REVIEWER CATCH IS RECORDED, and an earlier draft of this entry said TWICE and credited a reviewer with both: a second sweep did occur, at `77b8605b`, but I caught it myself and no packet or verdict exists for it. Inflating the mechanism's catch count is the failure the M0.76 entry below records. ⚠️ WHAT THE SWEEP WOULD HAVE COST: `check-commit-msg` stayed green while the tree recorded a wire-format decision under a skill-landing task, and `which-standards.sh` selects from the changed paths, so `cost.md` was in NONE of the four packets -- an ADR changing `CommitDelta` AND `Checkpoint` would have landed with no cost review. ⚠️ A SPLIT SEAM DOES EXIST and an earlier draft denied it: skill plus adapter plus index rows is self-consistent, and the `review/SKILL.md` paragraph plus the two briefs is self-consistent once that lands. Splitting at round 4 costs four fresh rounds and saves none, which is the real reason, not impossibility. ⚠️ THIS ENTRY CLAIMS NO CONVERGENCE.
